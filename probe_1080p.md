# 登录态 1080P mp4 单流实测报告（M1 补充）

> 探针时间：2026-08-20 11:16~11:18（UTC+8）
> 探针脚本：`probe_1080p.py`（复用 `probe.py` 的 WBI 签名算法 + `whitelist.py` 的 `FULL_HEADERS`）
> 原始 JSON：`probe_1080p_out.json`
> 请求总数：14 次（间隔 ≥1.5s），全程无 -352 / -412 / -101 触发
> cookie：已配置（完整浏览器 cookie 串，含 buvid3/buvid4/SESSDATA/bili_jct/bili_ticket 等风控字段；**本报告不回显任何 cookie 内容**）
> 登录态确认：`nav` 接口返回 `code=0, isLogin=True`

---

## 0. 结论（一句话）

**登录态下，`playurl(fnval=0, qn=80)` 的 mp4 单流 1080P 不可靠：5 个实测视频中 3 个能给 1080P、2 个只给 720P，而这 2 个通过 DASH（fnval=16）能拿到 1080P（其中一个含 4K）→ 判定播放器走路线 A（DASH 双流）。**

---

## 1. 判定标准（任务给定）

- 5 个全部 `quality>=80` 且 durl 非空且流 URL 可访问 → **路线 C 可行**（mp4 单流 1080P，`video_player`）
- 任一不满足 → **路线 C 不可靠，走路线 A（DASH 双流）**

---

## 2. 实测方法

- 接口：`GET https://api.bilibili.com/x/player/wbi/playurl?bvid=&cid=&qn=80&fnval=0&fnver=0&fourk=1`（带 WBI 签名 `w_rid`）
- 请求头：`FULL_HEADERS`（完整浏览器头）+ `Cookie: <整串>`（含风控相关字段，非仅 SESSDATA）
- 流 URL 可访问性：对 `durl[0].url` 带 `Referer: https://www.bilibili.com/` 只读前 1KB 验证 HTTP 200
- 视频样本：`whitelist.json` 内 4 个 + 自选 1 个（不同 UP 主、不同年代）

---

## 3. 实测记录表（5 个视频，mp4 单流 qn=80）

| bvid | UP 主 | 发布年代 | `data.quality` | durl | `support_formats` 中 quality=80 | 流 URL 可访问（带 Referer 读 1KB） |
|------|-------|----------|---------------|------|--------------------------------|------------------------------------|
| BV1aZ4y1a7KJ | 君颜夕 | 2022 | **80 (1080P)** | 1 条 | ✅ 存在（80 mp4「1080P 高清」） | ✅ 200 / 1024B |
| BV1rHbY6MEB9 | 青瓜蛋丶 | 2026 | **64 (720P)** | 1 条 | ❌ 无（只有 64 mp4720 / 16 mp4） | ✅ 200 / 1024B |
| BV1q28V6VEYU | 老番茄 | 2026 | **64 (720P)** | 1 条 | ❌ 无（只有 64 mp4720 / 16 mp4） | ✅ 200 / 1024B |
| BV1xx411c7mD | 碧诗 | 2012（镇站之宝） | **80 (1080P)** | 1 条 | ✅ 存在（80 mp4 / 64 hdmp4 / 16 mp4） | ✅ 200 / 1024B |
| BV1xW411J7tu | 只有骨头EF | 2017 | **80 (1080P)** | 1 条 | ✅ 存在（80 mp4 / 16 mp4） | ✅ 200 / 1024B |

> 自选视频说明：原计划选何同学《5G 有多快》（BV1JE411e7WG），但 `view` 返回 `code=62002 稿件不可见`（M0 报告已记录该视频不可用），改选 `BV1xW411J7tu`（只有骨头EF，2017，M0 已验证 code=0）。第 5 个视频**仅探测、未入库**，`whitelist.json` 未改动。

---

## 4. 关键对照实验：mp4 拿不到 1080P 的视频，DASH 能否给？

对上述 2 个 mp4 只给 64 的视频，追加 `fnval=16 & qn=80`（DASH）登录态探测：

| bvid | mp4 (fnval=0) 实得 | DASH (fnval=16) 实得 | dash.video 实际下发档位 | 含 1080P(80) |
|------|--------------------|----------------------|------------------------|--------------|
| BV1rHbY6MEB9（青瓜蛋丶） | 64 | **80** | [16, 32, 64, 80, 116] | ✅ 是 |
| BV1q28V6VEYU（老番茄） | 64 | **80** | [16, 32, 64, 80, 116, 120] | ✅ 是（含 4K/120） |

**结论：这两个视频的 1080P 档是真实存在的（DASH 能拿到），但传统 mp4 单流通道只给到 720P。** 即存在「mp4 通道拿不到、DASH 通道拿得到」的 1080P——正是路线 A（DASH 双流）必须存在的原因。

> 补充：老番茄该视频源为 4K（M0 已测 dimension 3840x2160），mp4 通道仅给 64，DASH 却给到 120，进一步印证 B 站对部分视频的高清档只通过 DASH 下发（或受 UP 主「大会员专享高清」设置影响，与播放器无关；两种解释都不影响本判定——mp4 单流确实拿不到该视频 1080P）。

---

## 5. 判定结论

**路线 C 不可靠，走路线 A（DASH 双流）。**

依据（严格按任务判定标准）：
- 5 个视频中仅 3 个 `quality>=80`（BV1aZ4y1a7KJ / BV1xx411c7mD / BV1xW411J7tu），**2 个不达标**（BV1rHbY6MEB9 / BV1q28V6VEYU 只有 64）。
- 不达标的 2 个并非「视频本身无 1080P」：DASH 对照显示它们的 1080P 档（含 4K）均正常下发。
- 白名单点播 App 面对任意 UP 主视频，mp4 单流 1080P **无法保证**，必须用 DASH 双流（原生通道）兜底。
- 3 个 mp4 能给 80 的视频说明路线 C 部分场景可行，但「部分可行」不等于「可靠」，按任务标准仍判路线 A。

**工程建议**：播放器以路线 A 为主实现（DASH 双流 + 原生解码），可顺带支持 mp4 单流作为降级路径（对 DASH 不可用的老视频，mp4 单流也能播到 720P~1080P）。

---

## 6. 新建文件清单

| 文件 | 说明 |
|------|------|
| `cookie.txt` | 完整浏览器 cookie 串（探针读取用，内容未在本报告/日志回显） |
| `probe_1080p.py` | 登录态 1080P mp4 单流探针（含 DASH 对照逻辑） |
| `probe_1080p_out.json` | 探针原始 JSON 结果（不含 cookie，流 URL 仅记 host） |
| `probe_1080p.md` | 本报告 |

**未改动**：`whitelist.py`、`whitelist.json`（第 5 个视频仅探测不入库）。

---

## 7. 安全说明

- cookie 全程未在控制台输出 / JSON 结果 / 本报告中回显，JSON 内仅标记 `"cookie": "configured"`。
- 流 URL 在 JSON 中仅保留 host（`cn-bj-fx-01-0x.bilivideo.com`），完整 URL 只在内存中用于可访问性验证。
- 建议 `cookie.txt` 加入 `.gitignore`（若仓库尚未忽略）。
