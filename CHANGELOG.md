# 更新日志（CHANGELOG）

本项目按 [SemVer](https://semver.org/) 约定：`主版本.次版本.修订号+构建号`。

`修订号` 用于 bug 修复与微调；`次版本` 用于新功能；`主版本` 用于不兼容变更。

所有版本变更按时间倒序记录；最新版本在最上方。

---

## v2.49.0 (2026-09-22)

### 单个视频多集（分 P）现在能用上下集切换了
这件事提过好几次，我们核了一遍：**历史上从来没有真正做过**。CHANGELOG 里两次"上下集"（v2.30.0、v2.37.0）说的其实都是"播放列表里换下一个视频"，而分 P（同一个 BV 号下的 P1/P2/P3…）从头到尾只有播放页那个不起眼的「选集」弹层能切，上下集按钮压根没接到这条维度上。

- **上下集现在优先切分 P**：视频有多个 P 时，上一集/下一集就在 P 之间走，并且**在首尾不跨界**（第一个 P 的「上一集」、最后一个 P 的「下一集」会置灰），不会再先切到别的视频上去——后者正是之前抱怨"切到其他番剧去了"的那类问题。通知栏的上一集/下一集走同一条逻辑。
- **取流不再先播 P1 再纠正**：以前从历史/搜索/信箱点进一个多 P 视频、而白名单条目里没存分 P 列表时，会**静默播 P1**（带 `?p=2` 也没用）——播放页虽然进页就请求了视频信息，却把"分 P 列表"丢掉了。现在会先把它补上再取流，缓存路径也一并补了。
- **底栏中间那行在多 P 时显示 P 维度**（如 `3/5 P`），不再显示成 `1/2` 让人误以为是视频条数。
- 进度记忆本来就是按 P 分开存的，不用改：切到 P3 退出再进来，回到的是 P3 的进度。
- **取舍（需要你知道）**：多 P 视频里上下集只在这个视频的 P 内走，走到头不自动跳到合集里的下一个视频——换视频请回列表点。这是刻意选的，避免重演"切到别的番"。

### 画面旋转做成了看得见、点得到的按钮
「没有手势旋转」核实下来是：**旋转的代码是实现了的**（双指连线转 ≥45° 会把画面吸附到 90° 档），但它在**真全屏之外一律不生效**、而且**没有任何入口**，只能靠那个很难想到的双指手势——等于用不出来。

- 全屏（以及横屏置顶）时画面右下角多了一排控制：**「旋转」按钮**，点一下画面 +90°，连点四次回正；旁边的复位按钮现在会**同时报出倍率与角度**（如 `复位 2.0x · 90°`），一眼能看出画面被转过。
- **横屏置顶态也放开了双指缩放/旋转**：以前只有"真全屏"才响应，而横屏置顶那块视频看起来同样是满宽的——很容易就是在那里试的，转了毫无反应。现在两种态都能转。

### 测试
- 新增 19 例：多 P 视频上下集切 P、首尾不跨界、**条目没存分 P 列表时先补齐再取流**（断言"取流确实在等分 P 列表"）、补列表不额外发请求、进度按 P 记忆、旋转按钮角度步进与连点回正、复位文案含角度、横屏置顶可变换。
- 测试总数 2471 → 2490。

### 已知限制
- 画面是原生纹理，widget 测试只能断言变换矩阵；**旋转/缩放在真机上是否真的把画面转过来，从来没有验证过**（v2.39.0 就留下的口子）。设备连上后会实测一次。
- 通知栏收起行在多 P 视频的首 / 末 P 会退化成「快退 / 快进」（原生契约要求"两边都有"才换行），底栏按钮本身是正确的。
- 横向滑动改进度目前没有阻尼（滑满一屏 = 100% 时长），与亮度和音量做过灵敏度处理不一致——此前只提过亮度/音量，这条没动。

---

## v2.48.0 (2026-09-22)

### 陈旧提示铺到搜索页与信箱，并修掉「同步失败长得像白名单为空」
- **搜索页「我的白名单」**：现在也有常驻的陈旧快照横幅（数据落后于云端时提示"你看的可能是旧数据、改动不会保存"，带「立即同步」）。
- **修掉一个误导**：这个 tab 以前把「同步失败」渲染成空态，文案写的是"白名单加载失败或暂无数据"——看起来就像你的白名单没了。现在失败会明确说明是同步失败、附上原始原因并给出「重试」；**真的没有白名单**才显示空态。
- **信箱**：同样加了陈旧横幅。信箱消费的正是白名单快照——快照旧了、名单里少几个 UP 主，他们的新视频就不会被发现，所以这里的提示有实际意义。注意这里的「立即同步」**只同步白名单**，不会顺带触发一轮完整检查（那可能要几分钟）。

### 测试
- 新增 12 例：搜索页与信箱的横幅在陈旧时出现、不陈旧时零占位（防把正常状态误报成离线）、失败态不再让位给空态、真的空仍是空态、横幅「立即同步」真的重新同步并让提示消失，以及信箱那条**「点『立即同步』后信箱全量检查的轮数保持不变」**。
- 测试总数 2459 → 2471。

### 已知限制
- 陈旧横幅仍未铺到 UP 主主页：那一页不读白名单快照，能提示的只有"已关注状态可能不准"，语义较弱，暂不做。

---

## v2.47.0 (2026-09-22)

### 「进 UP 主页经常网络请求失败」的根因修掉了
- **UP 主页首屏是唯一没有自愈能力的链路**：其它取数链路（视频详情、动态、专栏、搜索）撞到 B 站风控码 `-412` 时会**刷新签名 key、重新签名再试一次**，而 UP 主页的几条（UP 资料、粉丝数、视频列表、合集清单）撞上就直接放弃——页面层随后做的"重试"用的还是同一批已过期的签名，三次必然全败，于是整页显示「网络请求失败，请检查网络后重试」。**现在这几条也接上了同一套自愈**。
- **无签名的接口用另一种方式治**：粉丝数（`relation/stat`）与合集清单（`seasons_series_list`）根本不带签名参数，所以 `-412` 对它们只可能是"匿名 + 频率"造成的间歇风控，刷新 key 没有意义。这两类改为短退避后重发一次——时间差才有效，多打请求反而更容易被拦。
- **进页瞬间的请求风暴被削掉了**：进入 UP 主页时会并发拉起四条链，而每条都会各自去取一次设备指纹（`spi`）与签名 key（`nav`），且只判"缓存里有没有"、不判"有没有请求在飞"——同一瞬间会打出 2~3 次 `spi`、最多 2 次 `nav`。现在改为在途请求去重，首次进页两者**各只请求 1 次**。正常路径的请求量因此是**净下降**的，被风控的概率本身也随之下降。
- **失败文案不再骗人**：签名 key 响应异常（响应里没有 `wbi_img`）以前会被当成"网络故障"，显示「网络请求失败，请检查网络后重试」——现在单独归类为「B 站签名参数获取失败，请稍后重试」。真风控、真限流、真断网各自显示各自的原因。
- **粉丝数不再"一次失败就永远 `—`"**：`relation/stat` 失败原本是静默的，缓存里还会留下一条"粉丝数为空"的记录，之后每次进页只补拉一次、失败就静默放弃——风控期间该 UP 主的粉丝数会一直是 `—`。现在补拉也走退避重试，且失败不会把"空"当成已缓存的结果，下次进页会继续尝试；风控过去后自动恢复。

### 测试
- 新增 24 例，含：首次进 UP 主页 `spi`/`nav` **各 1 次**（改动前是 2~3 次 / 2 次）、视频与资料接口首次撞 `-412` 能自愈（刷新 key 后重签成功）、无签名接口遇风控时**不会白刷 key**（断言 `nav` 请求 0 次）、风控类与网络类的重试节奏各自成立，以及两条此前没有覆盖的降级路径——「资料失败但粉丝数成功 → 粉丝数照常显示、只在信息卡里留一行『简介加载失败』」与「网络故障时整页文案确实是『网络请求失败，请检查网络后重试』」。
- 测试总数 2435 → 2459。

### 已知限制
- 真实风控下的恢复率（700ms / 2s / 1.5s→3s 的等待是否够）只能在真机验证，测试用的是 mock 适配器。
- 持续真风控 / 真限流仍会失败，但会如实显示风控 / 限流文案并可点重试。

---

## v2.46.0 (2026-09-22)

### 「下载到一半报未知错误」这件事查清了
- 这个报错多半不是下载失败，而是**安装那一步被系统拦下**，却被显示成了「下载失败」。整条更新链路里只有一处会产出「未知错误」四个字——原生安装器的异常兜底；而弹窗把「下载」与「安装」放在同一个 try 里，于是安装失败也套上了"下载失败"的措辞。现在两者分开归因：安装阶段失败显示「安装失败：<具体原因>」，下载阶段失败仍显示原来的网络类文案，用户一眼能分清是哪一步出的问题。
- **补上「安装未知应用」的引导**：这个开关没开时安装必定失败（国产 ROM 上尤其常见）。现在安装前会先检查，未开启时直接说明「需要允许 amoTV 安装未知应用」，并给一个按钮直接跳到该设置页，不会再让人看到一个没有上下文的失败。
- **更新链路第一次有了日志**：以前整条链路一行日志都没有，原始异常被替换成固定中文文案后彻底丢失，出问题无法取证。现在检查 / 取地址 / 下载 / 校验 / 安装各阶段都有 `[update]` 前缀的日志，包含阶段、HTTP 状态码、异常类型与原始信息——**日志已脱敏**，token 与带签名的下载地址不会写进去。
- **已经下好的包不会白下**：安装失败后点重试，以前会**从 0 重下整个 68.5 MB**，现在会复用已下载且校验通过的 APK 直接重装；确实需要重下时另给「重新下载」。下载中途失败仍按原来的断点续传继续。
- **补两处完整性把关**：下载"干净结束"但字节数与声明不符时不再直接把文件送去安装（此前在缺少 SHA-256 的旧 Release 上会漏过去），而是保留半成品并提示重试；续传时校验服务器返回的起始偏移与请求一致，避免静默拼出错位文件。
- 另外给下载连接设了 15 秒的连接超时（原来没设，网络黑洞时进度条能冻住很久才报错）。

### 测试
- `UpdateDialog` 此前**零测试覆盖**，现在新增 11 例（安装失败归因、下载失败归因、权限引导与跳转、已下好包不重下、重新下载、安装通道未注册）。测试总数 2409 → 2435。

### 其它
- 修掉 `update_info.dart` 两处与实现不符的注释（版本比较是 versionCode 优先）。
- README 的「应用内版本更新」一节同步本批行为。
- 油猴脚本里一处形如真 token 的示例占位符改成明确的占位文案（避免发布安全扫描误报）。

### 已知限制
- 「点『去开启』是否真的落到各厂商 ROM 的『安装未知应用』页」只能在真机验证；已加应用详情页作为退路。
- 真实安装失败原因（签名不匹配 / 版本降级 / 存储不足）以及 15 秒连接超时在慢网下的体感，需要真机验证。
- `startActivity` 成功、但用户在系统安装界面上取消或被系统安全弹窗拦截：App 感知不到，属已知边界。

---

## v2.45.0 (2026-09-22)

### 播放中途报「播放失败（2001）」的根因修掉了
这个报错断断续续出现过很久。这次没有停在"加个提示、加个重试"，而是把两个真正的根因补上。

- **2001 原来被排除在自动恢复之外**：`2001` 不是 B 站的业务码，是 Android 播放引擎 media3 的错误码 `ERROR_CODE_IO_NETWORK_CONNECTION_FAILED`——而且它是"IO 异常的兜底桶"：除连接超时（2002）、cleartext 等几种之外，**其余一切 IO 异常（EOF、SSL、通用 IOException）都会归到 2001**。而原来的自动恢复判定是**按异常类型白名单**（只认超时 / 断连 / DNS 失败 + 特定 HTTP 状态码），于是**恰好这类"兜底桶"错误一个都进不了自愈通道，直接弹错**。现在判定同时看错误码：2001 / 2002 一律先走自动恢复（重新取流 + 断点续播），仍受原有的 3 次预算与 1s / 2s / 4s 退避约束，不会变成无限重试。
- **B 站下发的备用线路原来一条都没用**：接口在下发主线路的同时会为每条流给出 2 条 `backupUrl`（实测备用线路与主线路是**不同 CDN**，且用 Range 请求验证过都能正常取流）。旧代码只读主线路、恢复时又永远拿第一条，于是"某一台 CDN 出问题"就只能反复重试同一个地址。现在解析出候选线路，**故障时先在候选里换 CDN**（视频轨与音频轨成对推进，不会出现两轨签名来自不同批次的混搭），一批用尽才重新取流换一批。换源因此常常省掉一次接口请求，也少一次风控暴露。
- **多了一道兜底**：万一原生将来又漏判某类错误，Dart 侧在预算内也会先尝试一次自动恢复，仍失败才弹错——保证"真不可恢复的错误最多多花 1+2+4 秒，绝不会被藏起来"。
- 顺手把 `2001` 的错误码注释改对了（原来写成"读流超时"，实际是"IO 网络连接失败"；超时是 1003 / 2002），涉及原生、Dart 与 README。

### 测试
- 新增原生 JVM 单测 16 例（错误分类：2001 / 2002 判可恢复，2000 / 2005 / 解析 / 解码类判否，异常类型链与 HTTP 状态码策略独立成对）。现在跑 `./gradlew :app:testDebugUnitTest` 就能验证原生分类逻辑，不必再只靠"代码审查"。
- 新增 Dart 用例 27 例：候选线路解析 / 去重 / 游标轮转 / 耗尽换批的纯函数 22 例，加上**点播页恢复链路的接线测试 5 例**（真正触发 `onUrlExpired` / `onError`，断言按退避表换源、视频音频成对推进、位置续播、预算耗尽后放弃且不再发请求；2001 先自愈、3001 立刻弹错）。
- 测试总数 2382 → 2409。

### 已知限制
- 「真实网络抖动下 2001 → 换备用 CDN → 续播成功」只在 mock 通道里验证了接线，缺真机弱网现场；真实 CDN 可达性用 HTTP 探针旁证（主线路与备用线路均返回 206）。
- 主动预取（URL 到期前换源）**刻意不轮转备用线路**：实测同一批主线路与备用线路的 `deadline` 完全相同，轮转延长不了有效期，URL 将尽时必须重新取流才能拿到新签名。
- 直播路径现在也会把 2001 / 2002 交给直播页自己的重连预算处理，未实机验证。
- 离线缓存下载仍是"只取第一条线路"，本批未动。

---

## v2.44.0 (2026-09-22)

### 读侧不再让你看着旧数据而毫不知情
v2.43.0 给「写」上了门禁，但「读」还是老样子：断网或同步失败时你可能正在看几天前的快照，界面上却没有一句话告诉你。这批把读侧补齐，顺手修掉三个会误导人的地方。

- **数据时间不再伪造**：数据来源是本地导入快照（local）时，它根本没有"抓取时间"，旧代码却把"现在"填了进去——几周前的快照于是显示成今天的日期，看着比真实情况还新。现在这种情况明确显示 `数据时间未知（来源: local）` 并配提示性图标，一眼能看出这份数据不可信；有真实时间的来源（cache / Gist / 局域网）照旧显示真实时间。
- **陈旧/离线状态常驻可见**：只要拿到的快照被判定落后于云端，首页「合集」页与合集内部页顶部就会出现一条**常驻**横幅，说明"你看的可能是旧数据、改动不会保存"，并带「立即同步」按钮；同步成功后横幅自行消失。它不是一闪而过的 Toast——这种状态可能持续整个离线期。不显示时高度为 0，不占位、不影响布局。
- **「同步失败」不再伪装成「白名单为空」**：以前四个数据源全部失败时界面显示"白名单为空"，看起来像你的白名单被清空了（这正是之前"白名单 up 主时不时自动清空"那句投诉的界面来源）。现在区分三种状态：正在同步 / 真的没有白名单 / **同步失败（可能是离线）**，失败时给明确文案与「重试」入口。
- **错误与来源时间可以同屏**：以前同步失败的红字会把"数据时间（来源）"整行挤掉，现在两行并存。
- 新增的空态文案走可编辑文案库（设置页可改），文案总数 47 → 48。

### 测试
- 新增 15 例（`test/stale_read_visibility_test.dart`），锁住：local 无真实时间时不出现今天日期（其中一条**真跑一次 `sync()`** 断言服务层确实给 `null`，不是只测替身）、`stale: false` 时横幅必须不出现（防把正常状态误报成离线）、横幅「立即同步」真的会重新同步并让提示消失、合集内部页同样可见、同步失败空态与真空态互不串台、**同步失败时陈旧标记不被清掉**（警告不许因为一次失败就被藏起来）。
- 测试总数 2367 → 2382。

### 其它
- 顺手修正 `playlist_page.dart` 里一条与代码不符的过时注释（原写"本地缓存与网络同步并行、UI 立即展示缓存"，实际并没有这回事）。

### 已知限制
- 陈旧横幅目前只在首页「合集」页与合集内部页显示；UP 主页、信箱、搜索页的「我的白名单」tab 还没有这类标识（它们走各自的数据路径）。

---

## v2.43.0 (2026-09-22)

### 离线陈旧的本地快照不再冒充最新数据
- **问题**：断网冷启动时数据源按固定顺序回退（Gist → 局域网 → 本地导入 → 缓存），会落到几周前的本地导入快照上，用户看到旧数据却毫无提示，在旧数据上继续改动还可能覆盖云端较新的白名单。
- **修复**：数据源改为**按 `updated_at` 比新鲜度**——两边都能解析出时间时谁更新用谁，都不可比才回落到原来的顺序。
- **新增写入门禁**（`lib/sync/whitelist_freshness.dart`）：本地快照确证陈旧时**拦住所有白名单写入口**（新建/重命名/删除合集、移入移出、加入白名单等），提示"这次修改没有保存"并提供「立即同步」，且**不自动重放**你刚做的那次修改，避免在错误基线上改数据。`WhitelistWriter` / `UpownerWriter` 走的是"先 GET 再 PATCH"，GET 成功即证明基线最新，因此只需确认远端新鲜度即可解禁。
- **真机验证**：断网冷启动页眉显示"数据时间 2026-09-20（来源: cache）"而不是更旧的 local 快照；陈旧态下点新建合集会明确提示未保存、界面不出现假成功；恢复网络后写操作恢复正常。
- **已知限制**：只做"确证陈旧就拦住"，没做内容级合并；`updated_at` 被当作权威新鲜度；下拉刷新失败但内存里仍有旧数据时不触发门禁（未确证陈旧，风险与改动前一致）。

### 非全屏下的双指捏合不再被误判成拖动进度
- **问题**：非全屏时双指捏合缩放会被当成单指横向拖动，视频莫名跳到别的位置。
- **修复**：沿用指针跟踪机制，把"让位"从"仅全屏"改成**始终开启**；是否真正应用画面变换仍只由全屏决定。
- **验证**：旧实现下捏合确实会被误 seek（实测跳到 13625ms / 43796ms），修复后不再 seek；单指上/下/左右三向手势零回归。
- **已知限制**：多指手势无法用 adb 模拟，只能靠 `TestGesture` 多指 widget 测试覆盖。

### 评论区支持点赞
- 评论区每条评论可点赞/取消点赞，带乐观更新（点了立即变色）与失败自动回滚，重复点击幂等。
- 接口 `x/v2/reply/action`，`action` 是**位掩码**（bit0 赞、bit1 踩），所以判断"是否已赞"不能简单写 `action == 1`；取消点赞的参数是 `0`，与视频点赞的 `1/2` **刻意分开定义**，避免把"取消"发成"点赞"。
- 只在评论输入框可用的使用点（播放页内嵌评论区）可点；未开启写操作总开关时保持只读。
- **已知限制**：该接口路径与 `action=1/0` 取自社区文档，**未经真机写验证**（模拟器 B 站登录态已失效，且不允许重新登录），代码中已标注 ⚠️。

### 其它
- 新增测试 34 例（陈旧快照门禁 15 + 双指捏合 4 + 评论点赞 15），测试总数 2333 → 2367。

---

## v2.42.0 (2026-09-22)

**评论区能发评论与回复了（写操作第二阶段）：走统一的写出口、三处归属口径各自正确、失败不关输入弹层、`评论区已关闭` 会自动置灰入口；真机实测发出一条真评论并删除核对（评论区总数 510 → 509）；v2.42.0**

**A. 评论的写能力（发评论 / 回复）**

- **走既有的统一写出口**（v2.40.0 的 `_postAuth`）：注入 csrf、`bili_jct` 同时进 Cookie、表单编码、失败走统一错误映射、**绝不重试**。
- **归属口径必须调用方给，本层拒绝反推**：视频 = `type=1` + `aid`；专栏 = `type=12` + `cvid`；**动态 = 服务端 `basic.comment_type` + `basic.comment_id_str`**（相册型是 `11` + `rid`）——**绝不用 `type=17 + 动态 id`**（v2.31.0 实测那是 `-404`）。传错 type 不会报错、会静默挂到别的内容名下，所以这条写进了分区注释。
- **UI**：新增可选参数 `enableCompose`（**默认关**），只有**播放页内嵌评论区**开启；其余 3 个使用点（专栏 / 动态 / 独立评论页）渲染树逐节点不变。
  - 入口 = 区头下方一行「说点什么…」→ 点开**底部弹层**（多行输入 + `N/1000` 计数 + 取消/发送）。**不做常驻键盘输入框**：它会和列表抢屏幕、还会与整片横滑共用触摸区。
  - **发送失败不关弹层**（用户打的字还在，可直接再点发送），错误同时给内联文案 + 错误类提示条（后者不受「关掉底部提示」开关影响）。
  - 每条评论都有「回复」：回复根评论时 `root == parent == 它的 rpid`；回复楼中楼里的某条时 `root = 根评论 rpid`、`parent = 被回复那条的 rpid`；输入框预填 `@某人 ` 并把光标落在前缀之后。
  - 成功后关弹层 → **重拉第一页**（以服务端排序 / 置顶为准，不在本地插一条——本地插的位置十有八九会跳）。
- **`12002 评论区已关闭`**（读或写任一处遇到）→ 入口**置灰**并提示「该评论区已关闭」。
- **受写操作总开关控制**（默认关闭，与点赞 / 投币 / 收藏同一道闸）：关着一个写操作入口都不显示；设置页那条的标题与说明已更新到包含评论（并写明"评论会立刻出现在对方评论区、进对方消息通知"）。
- **与 v2.40.0 的排序横滑不冲突**：入口是**点击打开的独立路由**，横滑的 `Listener` 只旁听列表自己的指针事件，两者不共享手势竞技场。真机实测：横滑切排序不会误开弹层、点入口也不会切排序。
- **真机实测（发了真评论并已删除）**：在一条真实视频下发了一条顶层评论，正文写明是测试内容（41 字 ASCII）；`POST /x/v2/reply/add → code=0`，`rpid = 314671005761`；随后用 `deleteComment` 删除（`code=0`），**只读复核：评论区总数 510 → 509，热门与最新两个档位首屏都不再出现该 rpid**。⚠️ 如实说明：删除后**立即**重读仍能看到它（服务端读缓存），所以**决定性证据是总数回退**，而不是"列表里没看见"。写请求共 2 次（1 发 1 删），**没有重试**。

**B. 已知限制**

1. **回复根评论的 `root == parent == rpid`** 是按 B 站网页端行为推断的：真机只发了 1 条**顶层**评论（写配额用尽），这条路径只有单测 / 组件测试覆盖。
2. **评论点赞、删除自己评论的 UI、楼中楼富交互**（只做了 `@某人 ` 前缀 + `root`/`parent`）都没做。
3. **只有播放页开 compose**：专栏 / 动态 / 独立评论页保持默认关（想放开是各处加一行参数）。
4. **投稿 / 发视频明确不做**：需要 up 权限校验 + 分片上传 + 封面 + 分区 + 审核轮询 + 后台传输，量级是独立大版本，且与「看白名单视频」的定位无关。
5. ⚠️ **模拟器上的 B 站登录态在本次验证过程中丢失**（`shared_prefs` 里其它 key 也一起没了）。App 侧本次改动的 4 个文件都不写 / 不删任何存储，唯一会清会话的是既有的"凭据过期即清"逻辑（先于本版存在）；**最大的嫌疑是验证过程中跑了一次 `flutter test -d emulator-5554` 集成测试——它会重装 App 从而清掉数据**，但两个 `adb install` 是否直接导致 prefs 重建**没能确证**。这条只影响模拟器（需要重新登录一次），**不影响本版功能**。教训：**不要在模拟器上跑 integration_test**。

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **2333 例全绿**（v2.41.0 的 2282 → **+51**：写接口 25 + 发表与回复 23 + 播放页接线 3）；模拟器真机取证 8 张（`docs/screenshots/v2.42.0/`，本地留档不入库）——「说点什么…」入口、输入弹层（41/1000 计数）、发表成功提示、自己的评论在列表里、回复弹层（预填 `@某人 `）、总开关关闭时评论区**没有任何发表与回复入口**、开关打开恢复；真机 logcat 有完整的 `addComment → code=0 → rpid → 重拉首页 → del → code=0` 链路。

---

## v2.41.0 (2026-09-22)

**番剧可以「整季折成一张卡」了（同一季 ≥2 集折成一张，点开是这一部的分集列表、可从任意一集开始连播，左滑展开就回到逐集平铺、拖拽多选批量删除照旧）；并用 `archive/relation` 修掉了点赞 / 投币 / 收藏的初始态（实测发现收藏字段真名是 `favorite`，社区文档写的 `fav` 是错的）；v2.41.0**

**A. 番剧整季折叠成一张卡（用户：「不要让收藏一个剧或者番的时候需要把每集都收藏」）**

- **同一季 ≥2 集 → 折成一张整季卡**：封面取第 1 话、标题 = 季名、副信息「**N 集 · 已看 X/N**」、右上「整季」角标；卡片位置 = 首集在原列表里的位置（不重排语义）。
- **只 1 集不折**：把孤零零一条视频换成一张内容一样的卡，只多一层"点开选集"的点击；更要紧的是既有数据（普通视频、只收一集的新番）与**全部既有测试夹具**都落在这一支 → "不折"意味着它们的渲染**逐像素不变**。
- **点整季卡 → 弹层列出该季全部集**（标题 / 时长 / 已看标记）→ 点某一集**从它开始连播**。连播用的 `playlist` 与"点单条视频"走**同一个** `sortedSeasonEpisodes`（v2.37.0 那套同部识别），所以不会出现"整季卡点进去反而跨番"的分叉；标签传的是**季名**。
- **左滑「展开 / 收起」**：展开后季头保留 + 该季逐集平铺 → **拖拽排序 / 多选 / 批量移动 / 删除这些既有能力一条都不会丢**（需要精细操作时展开即可）。采用与同页子合集卡**同一套左滑语言**（露出动作块、点它才切换），而不是"滑过阈值立即展开"——后者在列表里一次不小心的横滑就会把整季摊开 / 收起。
- **多选模式下长按整季卡 = 整季一起勾选**（真机「已选 4 项」→ 底部「移动到合集 / 删除」直接可用，卡片仍保持折起）。
- **折叠只活在视图层**：不进数据模型、**不写 Gist**。理由：白名单是一份**跨端协议**（PC 端脚本与油猴脚本都在读写），加 `season_id` / `folded` 会让"同一部"出现**两份真相**（标题里的季名 + 新字段），而 PC 端只会把自己看不懂的字段原样保留；何况"折起来还是摊开"取决于屏幕宽窄与用户此刻想干什么，**根本不是数据的属性**。有专门用例钉住「折叠 / 展开都不触发一次保存」。
- **顺手修掉一个真机红屏**：展开态下季头与「第 1 话」同屏、封面是同一张图 → 两个同 tag 的 Hero → 框架抛 `multiple heroes that share the same tag`（既有的番剧用例第一次跑就抓到了）。现在整季卡的封面**不建 Hero 节点**，并用"季头内零 Hero + 无异常"双断言钉住。
- **未分类页同样生效**（未分类就是空合集名，走的是同一个页面与同一份折叠逻辑）——真机验证的正是未分类里的「是，大臣 第一季」。

**B. 修点赞 / 投币 / 收藏的初始态（`archive/relation`）**

- **探针实测（两轮，原始片段在 `relation_probe_out.json`）**：`like` 是 **bool**、`coin` 是 **int 枚数**（不是布尔）、收藏字段真名是 **`favorite`**——**社区文档写的 `fav` 是错的**。照文档读会让「已收藏」**永远显示「未收藏」**，这比不显示更糟（它看起来像确定的事实）。已改成优先 `favorite`、`fav` 兜底，并用真机正例验证（收藏夹里的视频 → `favorite:true` → UI「已收藏」）。
- 进入播放页时与 `view` **并发**拉一次 relation：**拿到就用真实态并去掉那句「状态未取到，重启后可能显示不准」的标注**；**拿不到就保留**降级标注 —— 不让"猜的"伪装成"服务端说的"。点赞 / 投币 / 收藏成功后只更新本地态，**不重拉接口**。
- 如实交代：探针**第一轮用本机 cookie 文件拿到的是 `-101 账号未登录`**（那份 cookie 早过期了），结论全部来自第二轮「真机内置探针」（App 自己发的那次只读 GET）。探针脚本留在仓库根，**内容已精确扫描确认不含任何凭据真值**（脚本注释里写明 cookie 只记长度、绝不落盘）。

**C. 已知限制**

1. **`coin` 的非零形态没有实测样本**（投币一律不做真机测试：不可撤回 + 扣真硬币），解析对 int 与 bool 两种形态都兜住，UI 只用「>0 = 已投币」。
2. **收藏的写路径没做真机写测试**（只读了既有已收藏的视频做只读正例）；写成功后的本地态更新由单测覆盖（与点赞共用一条路径）。
3. **整季卡不可拖拽**：它代表 N 条视频、在数据里没有单一 `order`，拖它只能说"整季一起搬"（另一个语义，本版不做）；要精确排序请左滑展开后逐集拖。
4. **展开后季头保留**（不是完全回到逐集平铺）：为的是给「收起」一个落点，否则折叠是一次性的。
5. **番剧集仍不显示写操作按钮**（pgc 的点赞 / 投币是另一套 `ep_id` 接口，本版不接）——所以折进整季卡的番剧，点开选集连播时也看不到写操作行。
6. 验证时打开过 3 条视频 → 播放历史**自然多了 3 条**（未清理：删历史不可逆，且这是 App 的正常行为产物，不是改动造成的数据污染）。
7. **本版对既有数据的 Gist 零改动**：验证前后 `updated_at` 未变、`videos=125 / collections=11` 一致；真机点赞后已用「取消点赞」还原（新进程重拉 relation 仍为 `like:false`）。

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **2282 例全绿**（v2.40.0 的 2250 → **+32**：折叠与选集 20 + relation 解析与写态 12）；模拟器真机取证 9 张（`docs/screenshots/v2.41.0/`，本地留档不入库）——未分类里「是，大臣 第一季」折成一张卡（「4 集 · 已看 4/4」）、点开选集弹层（共 4 集、正序、带已看勾）、从第 3 集开始连播（`是，大臣 第一季 · 3/4`）、点「下一集」到 4/4 后变灰、左滑展开逐集平铺、长按整季卡「已选 4 项」、播放页写操作行的降级标注已消失、真机点赞后 `已赞 272`、收藏夹里的视频显示「已收藏」。

---

## v2.40.0 (2026-09-22)

**评论区能在播放页左右滑切换「热门 / 最新」了；并落地写操作第一阶段——点赞 / 投币 / 收藏（总开关默认关闭、投币二次确认）；顺带实测修正了两处只有真机才知道的事：取消点赞要发 `like=2`（文档写 `0`，实测被拒 `-400`）、`view` 接口没有 `req_user`；v2.40.0**

**A. 评论区左右滑动（仅播放页内嵌评论区）**

- **左右滑切换「热门 / 最新」**：`mode=2` 就是「最新」——**探针实测确认**（`cursor.name` 分别返回「最新评论」/「热门评论」，`support_mode=[2,3]`），不是照文档猜的。
- 视觉反馈两处：区头行尾的 `热度 | 最新` 词条（当前档主色加粗、可点）+ 切换后浮层药丸「已切到「最新」」（1.4 秒自动消失）。
- **只启用播放页内嵌那一处**：用户是在看视频时抱怨"评论区不能左右划"，那里才是唯一有"继续看下去"语境的位置。专栏 / 动态 / 独立评论页保持原样（渲染树逐节点不变）。
- **实现上用 `Listener` 旁听原始指针，而没有加手势识别器**——这不是风格偏好：加一个识别器就多一个**手势竞技场成员**，小位移纵向拖动的 slop 行为随之改变（实测 20px 拖动实际滚到的距离变小），既有「滚评论隐藏信息块」的阈值防抖用例当场变红。也就是说**加识别器会悄悄改掉纵向滚动手感**；`Listener` 只旁听、不消费、不进竞技场。
- 与「单条评论左滑（复制等）」**不能同时上**：同一片区域只能有一套横滑手势，内层与外层会在竞技场里互抢——这一条已写进参数注释，留给将来选一个。

**B. 写操作第一阶段：点赞 / 投币 / 收藏**

- **落点**：播放页信息块里独立一行（三个「图标 + 词 + 计数」小按钮），**绝不动底栏**（底栏刚收窄成 8 个 40dp 按钮，第 9 个必压垮「选集 1/2」）。
- **写操作总开关默认关闭**：设置页新增「写操作」分区；关着时那一行**返回空盒**，信息块高度与改动前逐像素一致、三个按钮一个都不构建。
- **投币二次确认**（不可撤回 + 从账号里真实扣除硬币 + 会连带点赞，三件事都在确认框里说清）；点「取消」**一个请求都不发**。
- 失败 / 限流一律走**错误类提示**（不受「关掉底部提示」开关影响）；**记住上次选的收藏夹**（存本地 prefs，不写 Gist）；番剧集暂不支持（那是另一套 `ep_id` 接口）。
- **真机实测修正的两处（本轮最有价值的部分）**：
  1. **取消点赞要发 `like=2`，不是文档写的 `like=0`**——`like=0` 一律被拒成 `-400 请求错误`。换了 6 种请求形状（aid / bvid / csrf 位置 / 旧接口 / 表单编码）逐一排查才定位；`like=2` 实测 `code=0` 且服务端 `stat.like` 1522 → 1521。已按实测实现，并用一条**真机实测锁**用例钉住（谁照文档改回 0 会红）。
  2. **`65006 重复点赞` 必须当成功**——否则「重启后界面不知道点赞态 → 再点一次」会被当失败回滚，界面反而显示"没赞"。
- **⚠ 点赞初始态是降级方案**：`view` 接口**没有** `req_user`（匿名 curl、真机 logcat、用户真实浏览器三处独立确认），拿不到"我是否已赞"。所以只做**本次会话内乐观切换**，并在 UI 上**如实标注**「状态未取到，重启后可能显示不准」。**下一轮可修**：只读 `x/web-interface/archive/relation` 能拿到真实的点赞 / 投币 / 收藏态，成本很小。
- **产品边界（如实说明）**：**不做投稿发视频**（up 权限校验 + 分片上传 + 封面 + 分区 + 审核轮询 + 后台传输 = 独立大版本，且与「看白名单视频」的定位无关）；**评论下一轮**（要先有输入框 UI，且与评论区横滑抢同一片区域）。
- **真机验证纪律**：点赞与收藏**真机实测后已完全还原**（服务端 `stat.like` 1522 → 1521、收藏夹计数 168 → 167 回到原值）；**投币一个请求都没发**（不可撤回 + 扣真硬币，只做单测与 UI 层验证）。如实交代：为定位那个 `-400` 额外打了约 6 次超额只读请求。

**C. 已知限制**

1. **点赞初始态拿不到**（见 B 的降级方案）；重启后按钮可能显示不准，UI 已标注。
2. **`like=2` 只有单样本证据**（一条视频、一个账号、一台模拟器）：它现在是"有证据的实现"而非猜测，但不代表对所有账号 / 视频都成立。
3. **番剧集（番剧/影视）的点赞投币是另一套 `ep_id` 接口**（社区文档说法，未实测），本版在 UI 上直接排除。
4. **投币端到端没有服务端回执证据**（按纪律没真投），只有单测 + UI 层证据。
5. **`-400` 的最终归因是变体排查**：为什么 `like=0` 被拒（服务端策略还是别的），没有定论。
6. 排序横滑的门槛（56px / ≤500ms）与浮层时长 1.4s 是**手感取值**，没有用户测试支撑。
7. **单条评论左滑**（复制等）本轮**没做**，因为与整片横滑抢同一片区域（理由见 A）。

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **2250 例全绿**（v2.39.0 的 2189 → **+61**：排序横滑 13 + 写接口 27 + 播放页写操作 13 + 开关与偏好 8）；模拟器真机取证 11 张（`docs/screenshots/v2.40.0/`，本地留档不入库）——左滑切「最新」（`mode=2` 请求 + 浮层 + 区头变色）、右滑切回、点赞 `1521 → 1522` 后取消回 `1521`、收藏夹计数 `167 → 168 → 167`、投币确认框文案、"评论"按钮定位仍有效。

---

## v2.39.0 (2026-09-22)

**播放器四项：缓存了音频之后能切回网络看画面（新增「本地 / 网络」播放源切换）；播放窗口下方功能栏收 8dp（88 → 80 / 128 → 120）；竖屏视频进全屏不再被锁横屏；全屏支持双指缩放（1–4x）与旋转（吸附 90°）；v2.39.0**

**A. 缓存了音频后能切回网络看画面（用户：「我需要一个切换本地播放和流媒体播放的东西」）**

- 新增播放源状态（`local` / `network`，**默认 local**，与改动前行为一致）：缓存命中的判定多了「源为本地」这个前提，`network` 时强制走 `fetchPlayUrl` 的既有路径。
- **入口在「缓存操作」菜单第一项**（不加第 9 个底栏按钮）：已整段缓存时是「切到网络流播放（看画面 / 更高清晰度）」，**仅音频缓存时**副标题明确写「本地只缓存了音频，要看画面得用网络流」——这正是用户遇到的场景。
- **进度无缝**：切换前取**真实播放位置**（`await player.getPosition()`，不用 tick 缓存值，它最多落后一帧），重新设源时直接定位，不从头重播（真机实测 0:03 → 0:21 续播）。
- **失败回退**：切网络失败（限流 / 断网）→ 提示「网络取流失败，已回到本地缓存播放」并**把源真正装回去**（不是只改字段——只改字段会出现「字段指向网络、实际播着本地」，菜单也会骗人）。
- 换视频复位为 `local`；**同一视频内切分 P 不复位**（无缓存时 `local` 自然回落网络，不会死路）。

**B. 播放窗口下方功能栏收 8dp（用户：「功能栏太高，比例需要改进」）**

- 进度条行 44 → **40**、按钮行 44 → **40**（图标 20 → 18、间距 2 → 1）；**上下集行 40 不动**（它是可点且不误触的下限）。合计 **88 → 80 / 128 → 120**（竖屏 16:9 视频区 231dp 上的占比 38.1% → 34.6%、55.4% → 51.9%）。
- **顺手消掉一处硬编码**：字幕悬浮基准原来写死 `118 / 100`（是按"控制行 88"手算出来的，注释里也这么写）→ 现在由**常量推导**（可控态 = 控制行高 + 全屏 30 / 非全屏 12 → **110 / 92**），底栏再改也不会与字幕脱节。`_buildSeekRowOnly` 占位与上下集行偏移量引用同一常量。
- **不动视频区高度**：`compactEmbedded` 阈值 230 与实测 231.19 只差 1.2dp，碰一下就会翻转分支（收起中央播放簇）。
- **首次为底栏加了绝对高度的几何断言**（此前没有任何测试盯着它）。

**C. 竖屏视频全屏保持竖屏（用户：「竖屏视频全屏之后还是竖屏，而不是现在的旋转九十度」）**

- 进全屏时**按视频宽高比决定锁向**：竖屏 → 锁 `portraitUp`；横屏 → 锁 landscape 两向（现状）；**接近 1:1 → 不下命令**（保持当前方向，避免抖）。
- **宽高比未就绪时的兜底**：全屏按钮在首帧未就绪时也能点，此时**不锁**；`onPrepared` 拿到真实宽高比后**若正在全屏就补一次锁**——这条顺带覆盖了「全屏中切集 / 换源到另一个方向的视频」。
- **退出全屏的既有行为没动**（放开三向，既有断言 `exitOrient.length == 3` 仍绿）。真机实测：竖屏视频全屏后画面满宽、上下留黑，不再转 90°。

**D. 全屏双指缩放（1–4x）+ 旋转（吸附 90°）**

- **只在全屏生效**（非全屏视频区只有 231dp，且会与竖屏横滑 seek 抢手势）。画面包 `ClipRect + Transform`（缩放 + 旋转），**手势层与字幕 / 弹幕层在 Transform 之外**——所以缩放后「点画面显隐控制层」的命中区不变形，字幕 / 弹幕也照旧贴屏幕（与 B 站同层级，有意为之）。
- 缩放范围 1.0–4.0、旋转**吸附到 90° 步进**（不留 37° 这种歪画面）；**缩放态下单指拖动 = 平移画面**（iOS / YouTube / B站 的通行做法），`scale == 1 && rotation == 0` 时**完全走原来的三向判定**。
- **复位**：缩放态下底栏上方出现「复位 N.Nx」（不进 8 等分那一行）；退出全屏 / 换视频 / 切集自动复位。
- **⚠ 实现上的关键取舍（值得记）**：最终**没有用 `ScaleGestureRecognizer`**。本机 Flutter 源码（`gestures/scale.dart:731-741`）里，它在**单指**拖动过 slop 时也会 `resolve(accepted)` 赢下手势竞技场，同场竞争的 Pan 直接判负——而 `onScaleStart` 是赢下之后才回调的，那时再按 `pointerCount` 让位已经晚了，三条**用户验收过的** seek 回归用例必挂。改用 `Listener` 旁听原始指针事件、双指自己按 pointerId 配对计算：单指完全落在原识别器手里（零回归），双指才起变换。

**E. 已知限制**

1. **④ 的多指部分没有真机证据**：`adb shell input` 不支持多点触控（手搓 evdev 多点协议不可靠），所以真机只验了「单指三向 + 双击 + 长按 2x 都没被偷走」，缩放 / 旋转 / 复位由**双指 widget 测试**（10 例）覆盖。**缩放在真机上的手感（跟手程度、4x 上限是否够用）没有实测结论。**
2. **旋转的语义是「画面在视窗内旋转并被裁切」**（B站 / 多数播放器行为），**不做**「旋转后交换宽高比让画面铺满」。
3. **非全屏双指捏合仍可能被判定成单指拖动而 seek**——**改动前就如此**，本轮按「不动单指语义、不改非全屏行为」的边界没有动它。
4. 两条既有用例按**设计契约变更**调整了断言（非削弱）：进度条触摸目标高 44 → 40（B 段明确要求收到 40）；横屏置顶用例补泵到 `onPrepared`（原先只泵 300ms，宽高比未知时新契约按设计"不锁"；补泵后断言值不变，也与真机顺序一致——先看到画面再点全屏）。
5. 字幕悬浮基准只做了纯函数级断言（要在 widget 层真拉到字幕需跑通字幕接口 + 切轨道，成本高于直接测这条几何关系）。

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **2189 例全绿**（v2.38.0 的 2163 → **+26**：播放源切换 3 + 底栏几何 4 + 全屏方向 9 + 缩放旋转 10）；模拟器真机取证 9 张（`docs/screenshots/v2.39.0/`，本地留档不入库）——仅音频缓存无画面 → 菜单切网络流 → **画面出现且从 0:21 续播**；底栏 80 / 120dp 两态像素量算吻合；竖屏视频全屏保持竖屏（logcat `全屏方向锁 aspect=0.56 → portraitUp`）；全屏单指横滑仍 seek、双击仍播放暂停、长按仍是 2x；断网切网络流 → 提示并回到本地播放、没有停在错误页。

---

## v2.38.0 (2026-09-22)

**搜索结果可以直接点开看了（视频与番剧卡：点整卡 = 补元数据 → 直接进播放页，只播不写白名单；尾部「加入 / 导入」按钮原样保留）；合集（含子合集）可设封面与简介；首页合集卡左滑新增「移动」，可把一级合集嵌成子合集，不用再绕管理面板；v2.38.0**

**A. 搜索结果可直接点击观看**

- **视频卡 / 番剧卡点整卡 → 直接播**：视频卡补 `fetchVideoMeta` 后进播放页；番剧卡取 `fetchPgcSeason` 的**首集**后进播放页。**只播不写白名单**，尾部「加入白名单 / 整季导入」按钮**原样保留**、语义不变。
- **为什么点整卡不顺手写白名单**：写入是一次 4–9 秒的 Gist PATCH（见 v2.35.0），把它挂在「看这个视频」上等于让用户在看片前被动改数据；UP 主页 / 收藏夹 / 评论跳转 / 信箱早就是这个规矩。
- 番剧首集可能是会员 / 付费集 —— **不替用户挑免费集**（挑集会让「第一集」这个直觉失效），播放页已有试看 / 会员提示。
- 取不到可播集、或补元数据失败 → **一句明确提示，不跳空白页**。
- 卡片尾部加「直接看 ›」提示：放在尾部而不是副信息行，是为了**不变高**——此前放第三行会让既有「交错入场 ≥6 条」的用例变红（卡片一高，ListView 懒加载就少建一条）。

**B. 合集（含子合集）可设封面与简介**

- `CollectionInfo` 新增可选 `cover` / `desc`（`toJson` 仅非空输出、`copyWith` 透传，**旧数据零迁移**）。
- **⚠ 同批修掉一个静默数据丢失**：`renameCollection` / `deleteCollection` / `moveCollectionUnder` 三处都是**重新构造** `CollectionInfo` → 加字段后若不改，「改个合集名就把封面简介清空」。现已改成 `copyWith` 透传，并有专门的防坑回归用例钉住。
- **封面优先级**：合集自设 `cover` > 合集内第一个视频的封面（即原来的行为）> 占位图标；子合集卡有 cover 显示 40×40 图、无则保持文件夹图标（**不**回落视频封面，40×40 认不出层级）。简介为空则整段不占位。
- 入口：管理面板每行新增「编辑封面与简介」（既有三个图标与顺序未动）；子合集卡左滑新增「封面」。
- **为什么 v1 只支持填图片 URL、不做「从相册选图」**：白名单整份存在**一个 Gist 的文本 JSON** 里，而**每一次写操作都要 PATCH 整份**；图片塞 base64 会让每次写入膨胀到十几 MB（过不了 Gist 单文件上限，「改个名字」也会变成几十秒的请求），存本地路径则换设备 / 重装即失效 —— 而白名单是跟着 Gist 跨设备的数据。URL 是唯一零成本且可跨设备的方案。

**C. 首页把合集移动到另一个合集（成为子合集）**

- 首页合集卡左滑从两块变**三块：移动 / 重命名 / 删除**（删除仍在最右，既有两块位置未动；顺序与合集页子合集卡一致），走的是与管理面板同一条动作、同一套防环与级联。
- **为什么不是「直接拖到另一张卡上」**：首页同级重排用的是 `ReorderableListView`，它内部走 `MultiDragGestureRecognizer` + 自己的 `_DragInfo extends Drag`，**不是 `Draggable`** → `DragTarget` 收不到它的事件，拿不到「手指悬在哪张卡上」。做真拖拽只能把首页整套拖拽机制换掉，代价是丢掉自动滚动 / 松手回弹 / 拖拽代理样式 / 长按语义，还要自己处理「拖到卡上 = 嵌套、拖到卡间 = 同级重排」两套落点判定 —— 回归面远大于收益。**如需真拖拽，建议独立立项。**
- 360dp 实测：首页三块合计 228dp ≤ 卡片可用宽，不挤坏；子合集卡四块会把卡片推出大半，故那一处把动作块宽收到 60dp（仍 ≥48dp 触摸目标）。

**D. 已知限制**

1. 番剧**会员集**的试看 / 会员提示分支没在真机看到（点到的首集是免费集），只有既有代码 + widget 测试覆盖。
2. **「真拖到卡上」没做**（理由见 C）；要做需换掉首页的 `ReorderableListView`，是独立立项的量级。
3. 搜索结果卡上「直接看」与防连点**共用一个闸门**：极端情况下（补元数据途中去点另一类卡）第二次点击会被忽略。
4. 封面编辑对话框**没有封面预览**：填完保存后才在卡片上看到效果。
5. 老数据（`CollectionInfo` 没有 cover / desc）**不迁移、不回填**，行为与改动前一致。
6. **仓库不再提交每个版本的功能取证图**（本地留档）：每版二十多张、十几 MB，仓库不再随之膨胀；已跟踪的历史图不受影响。发布流程新增 `app/tools/release_all.sh`（版本自增 → 写 CHANGELOG → 安全扫描 → 提交推送 → 只打 arm64 → 上传 → 校验，一条命令走完）。

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **2163 例全绿**（v2.37.0 的 2118 → **+45**：搜索点击 9 + 合集元数据 15 + 渲染 12 + 首页移动 9）；模拟器真机取证 17 张（`docs/screenshots/v2.38.0/`）——搜 `anime` 点卡直接进播放页、**播过的视频不在白名单里**（`run-as` 读缓存 `videos=125` 未变）、番剧卡点进《雾山五行 第1话》、首页卡显示自设封面 + 简介、首页左滑三块、目标列表防环、落库后「含 1 个子合集」、子合集卡封面 + 简介 + 左滑四块；测试数据（两个 `zztest*` 合集）已清理，设备数据核对回原状。

---

## v2.37.0 (2026-09-22)

**视频卡片显示播放量（拿到才显示，白名单里的老视频多数没有 → 不显示，`view` 为空时文案与改动前逐字符一致）；番剧上下集不再跨番（根因：整季导入逐集 `addVideo` 且 `order` 恒 0、`added_at` 逐集递增 → 在合集里被排成「第43话 → 第1话」的倒序块，走到块末的「下一集」就是列表下一条 = 另一个番剧）；v2.37.0**

**A. 视频卡片加「播放量」**

- **`WhitelistVideo` 新增可选 `final int? view`**（播放量），严格照既有可选字段（`epId` / `pubdate` / `desc`）的约定：
  - `fromJson` 容错：脏类型 / 负数 → `null`（**0 是合法值**——真·零播放，照常保留）。
  - `toJson` **仅在非空时输出**（未知不回写多余字段，保持数据干净）；`copyWith` 原样透传（合集移动 / 重排不丢播放量）。
- **两个写入点**（都改成「能拿到才写」）：
  - `_videoFromVlist`（UP 主页视频列表）读 `j['play']` —— 这个值原本**拿到了却丢掉**；
  - `videoFromMeta` 读 `meta['stat']['view']` —— 字段名**实测确认**，不是猜的（1 次只读 `curl .../x/web-interface/view?bvid=…`，响应片段 `"stat":{"aid":80433022,"view":105989388,…}`）。
  - 两处都走新增的 `_nonNegativeInt`（非负整数才要，0 算有值），缺失 / 脏类型 / 负数 → `null`。
- **卡片副信息行**（`video_tile.dart`）新增顶层纯函数 `fmtVideoSubtitle(...)`：`时长 · UP主` 之后**按需**追加 `发布时间`、`N 播放`；**仅当 `view != null` 才拼最后一段**。抽成纯函数是因为这一行是既有测试的**逐字符**锚点，抽出来才能把「有/无 view」×「有/无 pubdate」四种组合直接断言，不用每次 pump 一棵树。
- **格式化复用专栏页的 `fmtArticleCount`**（`12345` → `1.2万`、`1.2亿`），与搜索页的播放量口径一致——**没有**引 `search_page`（页面互相 import 会成环，而且那边是私有 `_fmtPlay`）。
- **已知表现（本轮新增行为的主要部分）**：白名单里**绝大多数老视频在导入时根本没存过播放量** → 卡片**不显示这一段**，观感与改动前一样；`view == null` 时整行文案与改动前**逐字符一致**（既有断言锚在这句文案上）。
- **UP 主页那一行本轮未改**：那里是自绘 `ListTile`、不走 `VideoTile`，所以**不显示播放量**；本轮按「别扩」处理。

**B. 番剧分集：上下集不再跨番**

- **根因**（用户原话「点上集下一集的时候给我切到其他番剧去了」）：上下集导航用的「列表」就是合集页展示序（`WhitelistData.sortedVideos` 的 `order` 升序 + `addedAt` 倒序兜底）。而整季导入（`whitelist_writer` 的 `importPgcSeason` → `videoFromPgcEpisode`）**逐集 `addVideo`、不写 `order`（恒 0）**、`added_at` 逐集递增 → 整季在列表里是一整块**倒序**（第43话 → … → 第1话）；走到这一块**最末**（第1话）时，「下一集」就是列表里的下一条 = **别的番剧**。
- **改法（零 schema 变更、零迁移、对已存在的整季数据立刻生效）**：新增纯函数按**「标题里 `第N话` 之前的季名」**识别同一部（`seasonGroupKeyOf` / `seasonEpisodeIndexOf` / `sortedSeasonEpisodes`），**只取同部、组内按集号正序**；合集页在构造 `PlaylistContext` 前用它替换列表与下标（只接「点单条视频播放」这一条路径，多选 / 批量 / 子合集下钻都不是「从某个列表开始连播」的语义）。末集天然是组内最后一条 → `_canPlayNext == false` → **永不跨番**。
- **为什么按标题前缀分组、而不是加 `seasonId` 字段**：加字段**只能救以后新导入的集**，用户**现在已经躺在白名单里的整季**依旧没有该字段，修不了本次要修的数据，还得再补一套「缺字段时的回退」。而 `videoFromPgcEpisode` 生成的标题结构稳定（`季名 + 集数标签 + 副标题`），「`第N话` 之前」天然就是季名 —— 零迁移、对老数据立刻生效。理由已写进注释。
- **边界与取舍（都是有意为之）**：
  - 集号解析支持 `第1话` / `第12话` / `第5.5话`；`14(OVA)` 之类**安全回退**（挂到最后，按 `pubdate` → `added_at` 升序）。
  - **季名不同严格分开**（`XX 第一季` / `XX 第二季` 各走各自的正序）；**只有季名完全相同**（罕见）才会并成一组 —— 已知可接受表现。
  - `epId == null` 的**普通视频行为与改动前逐字符一致**（返回原列表，合集页照旧整列表排序）。
  - `epId != null` 但**认不出同部的孤立特别篇** → **宁可不给上下集也不跨番**（返回只含自己一条，播放页整行不构建，不是两个死按钮）；这是行为变化，属有意取舍。
- **真机取证**：真实病例是未分类里的「是，大臣 第一季」第 1–4 话 —— 改动前第1话落在 `40/65`、点「下一集」跳到**【碧蓝之海 第三季】第10集**（落到 `41/65`）；改动后 `1/4` → 「下一集」是同部第2话，走到第4话时「下一集」**变灰**（`4/4`）。43 集规模用**自造测试数据**验（`アニメ · 1/43`），**没有写 Gist**，测试数据用完**原样还原并校验哈希一致**。

**C. 已知限制 / 未验**

1. **老视频不显示播放量是设计表现**，不是 bug：白名单里存的旧数据没有这个字段，只有本轮之后新导入 / 新加入的视频才有；旧条目不迁移、不回填。
2. **UP 主页那一行不显示播放量**（自绘 `ListTile`，不走 `VideoTile`），本轮按「别扩」未改。
3. **43 集规模的「点下一集真的切过去」没能真机验成**：自造数据的 `bvid` 是假的、取流失败（`请求错误 BVGD01/999001`），该规模的「切换 / 禁用」只有 **widget 测试**覆盖；真机证据是 `1/43` 这个标注（它直接反映 playlist 的内容与顺序）加真实 4 集病例的完整行为。
4. **`epId != null` 但认不出同部的孤立特别篇会失去上下集** —— 相对改动前的行为变化（改动前它会把「下一集」指向合集下一条，可能跨番），属有意取舍。
5. **分组只在合集页入口生效**：UP 主页 / 收藏夹 / 搜索这些入口本来就不传 `epId`，现状无影响（也是既有设计——那些列表的序是观看时间序 / 相关度序，跳「下一个」会到不相干的视频）。
6. **OVA / 特别篇的归位依赖「同列表里存在同季名的其它集」**：列表里一集同部正片都没有时它借不到季名，就落到第 4 条的取舍。

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **2118 例全绿**（v2.36.0 的 2075 → **+43**：番剧分组 18 + 卡片文案 6 + 模型 7 + writer 2 + upowner 4 + 播放列表 6）；**实测确认 `data.stat.view` 存在**（1 次只读 `curl`，响应片段 `"stat":{"aid":80433022,"view":105989388,…}`）；模拟器真机取证（7 张截图在 `docs/screenshots/v2.37.0/`）：未分类里整季排成倒序块的根因现场 → 改动前第1话点「下一集」跨到【碧蓝之海 第三季】第10集 → 改动后 `1/4` → 末集 `4/4`「下一集」变灰、卡片带播放量的副信息行、无播放量卡片的副信息行与改动前一致、43 集 `アニメ · 1/43`。

---

## v2.36.0 (2026-09-21)

**底部提示可以关掉了——设置里新增「显示底部提示」开关（默认开，只静默 `info` 类；错误提示与带「撤销」的提示永不静默）；信箱 Tinder 卡片支持连续滑 / 垂直快速上下切（根因是每次手势后 200–400ms 输入死区 + 飞行期间新手势被静默吃掉，改用「幽灵层交接」把锁定窗口 260ms → 160ms、竖直动画改成距离-时长等比（两向都是 1.80 px/ms）、上滑取回锁输入 400ms → 120ms、撤销改可连撤（`_undoStack`，上限 20 条），并顺手在写入服务内部加串行闸门，堵住「连滑两张右滑静默丢一条」的 read-modify-write 覆盖）；v2.36.0**

**A. 设置里可关掉底部提示**

- **新增 `lib/widgets/app_snack.dart`**：统一入口 `AppSnack.show(context, msg, {kind, action})` + `enum SnackKind { info, error }`。
  - **只有 `info` 类受开关控制**；`error` 类**永不静默**（关了「保存失败」会让人以为写盘成功）；**带 `SnackBarAction`（撤销）的永不静默**（关掉等于把撤销入口弄没了）——这两条理由写进了文件头注释。
  - 统一 `hideCurrentSnackBar()`（旧代码有的 hide 有的不 hide，连划卡时会排队堆叠）。
- **新增 `lib/services/ui_prefs_store.dart`**（`showTips`，key `ui:show_tips`，**默认开**，照既有 store 范式）；`lib/main.dart` 接线；`lib/widgets/manage_panel.dart` 新增「界面提示」分区 + 开关（文案说明「错误提示与带撤销的提示一定会显示」）。
- **迁移了 7 个文件的每日噪音**：信箱（已取回 / 已撤销 / 已全部标记已读…）、合集页、离线缓存页、日程页（**带「撤销」的永不 gate**）、首页（收藏夹 / 导入 / 更新检查…）、UP 主页、设置页（GitHub / 翻译配置已保存）。

**B. 信箱卡片：连续滑 / 快速上下切**

- **用户原话**：「**卡片在跳过的时候自由度在于可以垂直上下快速切换。需要可以连续跳过而不是现在单个判断**」。
- **根因**：每次手势后有 **200–400ms 输入死区**（`_busy = _deciding || _anim != null`），其中**上滑取回是两步动画 ≈400ms**；而且飞行期间的新滑动会被**静默吃掉**（`_onDragStart` 没查 `_busy`，松手也早退）→ 手感是「划了没反应」。
- **改法：幽灵层交接**——一次滑动分两段：**推进段**（后层往前顶，这段仍挡手势）→ **推进到位那一帧把飞出的那张从队列摘出去，交给画在整叠牌之上、`IgnorePointer` 的幽灵层继续飞完**。交接点由「两边 ticker 同帧 `forward` + 时长比例」精确落在卡片栈的 u=1，**从根上排除层位跳变**。
  - `kDurAdvance` **260ms → 160ms**（这就是「松手 → 能拖下一张」的等待）；交接后 `_busy` 立刻为 false。
  - 顺带修掉一个真陷阱：原 `_commitExit` 会无条件交接、顶掉还在飞的旧幽灵（那张卡会「半屏处消失」）——现在只在本次确实交接过时才清状态、幽灵落地会重试交接。
- **竖直动画改「距离-时长等比」**（新增纯函数 `inboxExitMotion`）：原竖直距离 1.4×屏高但时长与水平同为 320ms → **竖直速度是水平的 2.2 倍**（「嗖一下没了」）；现竖直只推 1.0×屏高、时长 508ms → **两向速度都是 1.80 px/ms**（测试断言误差 ≤1%）。
- **上滑取回锁输入 400ms → 120ms**（第一步降到 `kDurQuick`）；`_springBack` 200ms → 320ms（不再比飞出更急）；**回弹 / 取回滑入途中可以被手指接管**（从画面当前那一帧接着走；exit 不接管，因为那一下已经记了已处理 / 写过 Gist）。
- **撤销改可连撤**（`_undoStack`，LIFO，上限 20 条）——单张撤销位在「连续跳过」下按一次就用完。
- **✓ 顺手堵了一个真实数据隐患**：`WhitelistWriter.addVideo` 是「GET 整份查重 → PATCH 整份」的 read-modify-write，**连滑两张右滑会各写一份「只含自己那条」的快照，后一次整份覆盖前一次 → 静默丢一条**。现在在**写入服务内部**加了串行闸门（`_chain`），**覆盖面比只改信箱更广**：搜索页「加入」、UP 主页长按、链接导入、整季导入全都受保护；判定（出栈 / 记已处理）仍立刻生效，只有写盘排队。

**C. 已知限制 / 未验**

1. **17 处裸 `showSnackBar` 未迁移**（清单见 A 段），仍不受开关控制：`player_page.dart` 3 处、`search_page.dart` 1 处（这两个文件本轮被禁止改动）、`followings_import_page.dart` 3、`comment_list.dart` 2、`favorites_import_dialog.dart` 1、`pgc_import_dialog.dart` 1、`expandable_text.dart` 1、`login_page.dart` 1、`image_viewer_page.dart` 1、`favorite_videos_page.dart` 1、`dynamic_detail_page.dart` 1、`article_page.dart` 1。
2. **SnackBar 视觉没动**：仍统一黑底，`kind` 只用于 gate、**未用于配色**。
3. **入场账本仍整本 `clear()`**（沿用原设计）：理论上「窗口变化时另一张卡可能重播一次淡入」，本轮未改共享组件 `staggered_entrance.dart` 的 API。
4. **「错误类照弹」只有单测背书**：真机上没等到错误条（模拟器请求像挂在超时上），无真机截图。
5. **竖直「只推一屏高」** 在极矮屏 + 用户滚过内容的极端组合下理论上可能留一条边，未复现。
6. **连滑时延无真机量化**：`adb shell input swipe` 连发会丢手势、节奏最快也在 300ms 以上，所以真机只能证明「划出去的手势都被判定了」，证明不了 160ms 与旧值 320 / 400ms 的差别——精确时延由 widget 测试断言（新用例的等待窗口 200ms / 140ms，**在旧实现上必然失败**）。
7. `_springBack` 改成 320ms（与飞出同长）；若观感觉得「回得慢」，改回 200ms 是一行的事（它可被新手势中途接管，不锁输入）。

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **2075 例全绿**（v2.35.0 的 2056 → **+19**：设置开关 / `AppSnack` gate 11 + 信箱连滑与串行写 8；**既有 46 例信箱手势测试一个字节都没改**，`test/inbox_swipe_test.dart` diff 为 **+311 / −0 纯新增**；开关默认开 → 原有 SnackBar 断言全绿）；模拟器真机取证（截图 + logcat 在 `docs/screenshots/v2.36.0/`）：设置开关默认开 → 关 → **冷启动后仍是关**（落盘）→ 关时「取回」无提示、开时提示照常；**连续下滑 4 张判定全中**、**上下交替 4 轮 = 8 次手势全部判定且无损往返**（同一 `bvid` 每次都回到栈顶）。

---

## v2.35.0 (2026-09-21)

**「创建合集 / 加入合集」不再干等——白名单写入改成乐观更新 + 后台串行队列（实测「点击 → 界面刷新」从干等一次 Gist PATCH 的 5.6–9.1 秒变成 0–2ms）；并新增「启动时读剪贴板，命中 B 站视频链接直接进播放页」（默认开、可关、同一链接只跳一次、b23.tv 短链真机联网解析）；v2.35.0**

**A. 修「创建 / 加入合集有延迟」**

- **用户原话**：「**为什么创建和加入合集会有延迟**」。
- **根因**：写白名单的路径是「**先 `await` 一次 GitHub Gist PATCH（把整份白名单传上去），写完才 `setState` 刷新界面**」。实测那次 PATCH 要 **5.6–9.1 秒**（5833 / 6251 / 9092 / 5600 ms），所以用户点完只能干等，等待期间界面还停在旧状态——用户看到的「延迟」就是这段等待。
- **改法：乐观更新 + 后台写 + 串行队列**
  - **乐观更新**：用户操作**先在内存生效并立刻 `setState` 刷新界面**——实测「点击 → UI 更新」**0–2ms**；
  - **后台写**：远端写改成**异步**进行，不再挡 UI；
  - **串行队列**（新增 `lib/services/whitelist_write_queue.dart`）：**单飞 + 只保留最后一份待写快照**——同一时刻**只有一次 PATCH 在飞**，飞行期间新来的快照**合并覆盖**旧的，避免 read-modify-write 互相覆盖；**串行排空之后**才提示，一串连续提交**只喊一声**；
  - **远端写失败不回滚**：本地已生效，回滚会让用户刚做的事凭空消失；只给一条错误类提示「**未同步到 Gist（本地已生效）：…**」；
  - 落库收口在 `playlist_page.dart` / `collection_page.dart` 的 `_saveAndRefresh`（只留一处真相）。
- **修复前后实测对比**：修复前——点击后 1.3s **仍看不到新合集**，2.1s 反而弹了一次错误 SnackBar；修复后——新卡片在 **PATCH 还在飞的时候**就已经出现在界面上（两张「PATCH 飞行中、界面已更新完」的证据图）。
- **真机验证**（模拟器上配好**真实 B 站登录态 + GitHub token**）：真实 Gist 写入**成功**（并用 `curl` 独立读回复核，`updated_at` 与设备写入时间**毫秒级吻合**）；`am force-stop` **重启后新建的合集仍在**；**飞行模式**下写失败 → **本地照样生效** + 提示，远端确实没变；**测试数据已清理**。

**B. 启动时读剪贴板 → 命中 B 站视频链接直接进播放页**

- **用户需求**：复制一条 B 站视频链接，下次打开 App **直接播**。
- **行为**：冷启动（等首页就绪后）**只读一次**剪贴板；命中视频链接就**直接 `push` 播放页**（**可返回**）；没有链接**完全不打扰**（不提示、不跳转）。
- **识别形态**：完整 `bilibili.com/video/BV…` 链接（**含带 query**）、**`b23.tv` 短链**、**分享文本包裹形式**（`【标题】 url`）、**裸 BV 号**。
- **`b23.tv` 短链走真机联网解析重定向**，落点与 `curl -L` 一致、**连 `?p=1` 这种定位参数都带过去**。
- **番剧 `ep` / `ss` 链接识别但不跳**：App 没有通用番剧播放入口，识别出来按「不是可播的视频链接」处理。
- **同一个链接只跳一次**：按**剪贴板原文**去重，**跨启动持久化**。
- **设置里可关**：新增「**启动时播放剪贴板里的视频**」开关，**默认开**；**关掉后连剪贴板都不读**。
- **首页被别的页面压住时**先把结果存着，**退回后再跳**。
- 新增文件：`lib/utils/clipboard_link.dart`（纯解析）、`lib/services/clipboard_link_probe.dart`（探测流程）、`lib/services/clipboard_link_store.dart`（开关 + 去重键）；接线在 `lib/main.dart` / `lib/pages/playlist_page.dart` / `lib/widgets/manage_panel.dart`。

**C. 已知限制 / 未验**

1. **乐观写队列只罩住本队列**：`WhitelistWriter.addVideo`（搜索页「加入」/ 信箱右滑 / UP 长按 / 整季导入等）仍是「**先 GET 整份查重、再 PATCH 整份**」的 read-modify-write；乐观写把竞争窗口从「一次点击」**拉长到整个 PATCH 期间**（实测 **5.6–9.1 秒**）。代码注释里已标 **TODO，本批未修**。
2. **离线时的陈旧快照风险（既有隐患，不是本批引入）**：离线冷启动时白名单来源**优先用 `local`（本地导入快照）而不是 `cache`**，实测会显示**陈旧数据**；在这种状态下做写操作，乐观写**可能把陈旧快照整份 PATCH 上远端**。已实测「**离线启动确实显示陈旧数据**」这一半；「**会污染远端**」是**推断、未实测**（不敢复现）。**建议单独立项**。
3. **`b23.tv` 短链只有成功路径在真机验过**；解析失败 / 落点异常的分支**只有 widget 测试覆盖**。
4. **番剧（`ep` / `ss`）链接「不跳」只有单测覆盖**，真机未试。
5. 「**首页被压住 → 结果先存着、退回后再跳**」**只有 widget 测试覆盖**，真机未造该场景。
6. **未跑 `flutter build apk --release`**（本轮是功能取证），**未做数据迁移**。

- 测试 / 验证：`flutter analyze` **0 issue**；全量 `flutter test` **2056 例全绿**（v2.34.0 的 2014 → **+42**：剪贴板解析 26 + 启动接线 11 + 乐观更新 4 + 写队列 5；**既有测试文件一个字节未动**）；模拟器真机取证：延迟侧「点击 → UI 更新」**0–2ms**（`[wq] 收到落库请求 → 本地已生效`）而远端写 **5833 / 6251 / 9092 / 5600ms** 成功、`am force-stop` 重启后新建合集仍在、飞行模式写失败仍本地生效；剪贴板侧冷启动直接进播放页、**1080P 真的在播**（`onPrepared 1920x1080`，登录态生效）、`b23.tv` 短链冷启动进播放页、开关开 / 关 / 落盘 / 恢复、无链接不打扰。截图在 `docs/screenshots/v2.35.0/`（正式图 28 张 + `_work/` 中间图，后者被 `.gitignore` 覆盖、不进仓库）。

---

## v2.34.0 (2026-09-17)

**日程页新增「导入 Excel」——把 WPS 里维护的表格直接导进 App（选文件走系统文件选择器、不申请任何存储权限、零新增依赖手写 xlsx 读取器）；导入前先给预览 + 覆盖确认，取消不改表；v2.34.0**

**A. 日程页新增「导入 Excel」**

- **用户原话**：「**希望做一个能导入 excel 的入口**」——他平时在 WPS 里维护 `超级代办 大三上.xlsx`，希望这份表能直接进 App 的日程页，不用手动一格格敲。
- **入口**：日程页标题区新增「**导入 Excel**」，与既有的「加行」「加日期列」**并列**。
- **选文件走 Android 系统文件选择器（SAF / `ACTION_OPEN_DOCUMENT`），不申请任何存储权限**：走 `Intent.ACTION_OPEN_DOCUMENT` + `CATEGORY_OPENABLE` 拿到 provider 授予的临时读权限（`FLAG_GRANT_READ_URI_PERMISSION`），因此 **Manifest 里不加任何 `READ_*` / `MANAGE_*` 权限**——这是选 SAF 而不是「自己扫 SD 卡」的核心好处。
- **5 MB 上限，两道防线**：先查 `OpenableColumns.SIZE` 直接拒绝；有些 provider（云盘 / 部分国产 ROM）返回 `-1` 或缺失，再**边读边数**、超限即中断。两条路都返回明确的 `FILE_TOO_LARGE`。
- **新增原生 `ScheduleImportPlugin.kt`**（MethodChannel `bili_whitelist/schedule_import`，176 行）：`pickXlsx()` → `{name, bytes}` 或 `null`。
- **⚠️ 为什么这里只能用已弃用的 `startActivityForResult`**：本项目 `MainActivity` 继承 `FlutterActivity`，而它继承的是原生 `android.app.Activity`、**不是** androidx 的 `ComponentActivity` → **拿不到 `ActivityResultRegistry`**，`registerForActivityResult` 在这条继承链上用不了。已在代码注释里写明这条约束与原因（换成 `ComponentActivity` 会动到 Flutter 的 Activity 契约，代价远大于收益）。
- **用户取消 → 返回 `null`** → Dart 侧判为取消、**不弹任何错误**（用户主动按返回键不该看到红字）。
- **零新增依赖：复用项目已有的 `archive`（原用途是解 tar.bz2 模型包），手写一个最小 xlsx 读取器**（`lib/services/xlsx_reader.dart`，887 行）。不引 `excel` / `spreadsheet_dom` / `xml` 的理由写在文件头：xlsx 本质就是 **zip + XML**，为这点需求引进三个包会把**整条依赖链**绑进来（`excel` 会连带 `archive`＋`xml`＋`collection` 等，且与已有 `archive` 版本可能打架），而它替我们省的**不到 300 行**；另外 `archive` 的 `ArchiveFile.content` 是**懒解压**，用户文件里那 **500KB 内嵌图片根本不会被解出来**——不引重型库反而更快。
- **覆盖的单元格形态**（都是拿真实文件 + 合成构造逐一钉过的）：
  - `t="s"` **共享字符串**：**多个 `<r>` run 拼接**成完整文本、`xml:space="preserve"` **保留前后空格**、注音 `<rPh>` **丢弃**（不然「汉」会变成「汉hàn」）、实体与 Excel 控制字符转义 **`_x000D_` 还原**；
  - `t="inlineStr"`（WPS 导出常用）、`t="str"`（公式结果）；
  - **数字**：`3.0 → '3'`（不显示 `3.0`）、`1E3 → '1000'`（不显示 `1E+3`）；
  - **布尔 / 错误值**（`t="b"` / `t="e"`）当文本读，不抛；
  - **`r` 属性缺省**：按前一格列号 +1 推断；整行都没 `r` 就按出现顺序推断；
  - **空单元格与纯样式单元格跳过**；**只有底色没有文本的格子保留**（用户拿底色当标记用，丢了就是丢了信息）；
  - **命名空间前缀容错**（`<x:c>` 这种也要认）；
  - **畸形输入一律不抛**：坏 zip / 缺 entry / XML 截断 / 空表 → 返回 `null` 或**已解析的部分**，绝不把异常泄到 UI。
- **WPS 的 `=DISPIMG("ID_xx",1)`（单元格内嵌图片的假公式）当空处理**：这不是真公式，WPS 用它挂单元格内嵌图片，图片本身在 `xl/cellimages.xml` + 附件里；把裸公式字面量画到网格上**比留空更像 bug**，所以按空处理（这条也解释了下面 200 vs 201 的差 1）。
- **颜色还原走 `s → cellXfs[s].fillId → fills[fillId].fgColor/@rgb` 的间接层**，**逐通道容差 ≤ 16** 映射到 `kScheduleColors` 色板——**用户那 5 色是精确命中**（色板前 5 色当初就是照抄他的实际色值，同序同值），容差只是为了扛 WPS/Excel 存储时的微小色差。**`theme` / `indexed` / 图案填充一律判无色**：还原它们要靠 `theme1.xml` 的色轮 + `tint` 调色，**猜错颜色比没有颜色更烦人**（他会以为自己表里就是这么着色的）；**也不扩色板**（扩了会让「导进来的色」和「App 里能选的色」不一致）。
- **转 `ScheduleData` 的口径**：
  - **列头 = 第一个「有文本」的行**（**前导空行跳过**——真实文件里常有一行空行占位）；
  - **列头文本原样导入**：`星期一9.14` 这种保持不动（不做任何重排或规整）；
  - **空列头兜底 `第N列` 且不丢列**（宁可给个粗糙列头，也不能把他表里的列悄悄扔掉）；
  - **数据行按 `r` 行号补空行**（Excel 里跳过没内容的行号也要补上，否则行会整体上移、和原表错位）；
  - **上限 500 行 / 64 列**，超出**截断**并在**预览弹层里明确告知**（网格没有虚拟化，不设上限会直接把 UI 卡死——但静默截断会让用户以为表没导全，所以必须说）。
- **多工作表可选**：文件名带 `WpsReserved*` 前缀的 **WPS 保留表过滤掉**（`WpsReserved_CellImgList` 这类是 WPS 内部记账用的，列出来只会让人困惑）；**隐藏表保留**，让用户自己看到、自己决定（App 不替他猜「隐藏 = 不想导」）。
- **导入前有预览 + 覆盖确认**：弹层显示「**将导入 N 列 × M 行，其中 K 格有底色**」+ **红字**「会**替换**当前日程（当前表里的 **X** 个非空格子会被清空），**此操作不可恢复**」；按钮 = **取消 / 覆盖导入**。**取消不改表**。导入成功给 SnackBar。
- **每种错误各给一句明确提示**：非 xlsx（用 **zip magic `PK\x03\x04`** 拦——改名的 txt 在这里就被挡住）/ 坏文件 / 没有可导入的工作表 / 空表 / 超 5MB；**用户取消不弹任何提示**。
- **顺手核实的一处（结论是「不用改」）**：列头日期解析 `parseScheduleMonthDay` **原本就认 `星期一9.14`**——它的正则**只看数字与分隔符，与「周 / 星期」前缀无关**。已用临时脚本实测确认，所以**没有改任何逻辑**，只补了 **2 条用例 + 注释**把这条口径钉住（防止以后有人「顺手」加个前缀判断把 `星期一9.14` 判成不可解析）。
- **夹具是合成的**：`app/tools/make_xlsx_fixture.py`（openpyxl 生成 → `app/test/fixtures/schedule_sample.xlsx`，**6 KB**，文本全是通用词：`写代码 / 交作业 / 背单词 / 看书 / 跑步`）；畸形 zip 用 `ZipEncoder` **在测试里现造**。**没有把用户真实 xlsx 或其实质内容提交进仓库**（提交前复核 `校党校 / 实验报告 / 量子力学 / 尹老板 / 门票 / 复习 / 看伊朗 / 超级代办` 在夹具里**计数全为 0**）。

**B. 验证**

- **`flutter analyze` 0 issue；全量 `flutter test` 2014 例全绿**（v2.33.0 的 **1964 → +50**：xlsx reader 31 + 导入链路 17 + 模型 2）。
- **模拟器上用真实文件跑通**（把用户的 xlsx 推到 `/sdcard/Download/` 后经系统选择器导入）：App 报 **22 列 × 48 行 / 130 格有底色**，与 **Python（openpyxl）独立解析完全吻合**（黄 61 / 灰 51 / 绿 15 / 红 2 / 蓝 1）；**非空格子 200（App）vs 201（Python）的差 1 就是那个 `DISPIMG` 内嵌图片格**——它在 App 里**故意当空**，Python 读到了裸公式字面量。
- **截图可核对**：表头 `第1列 / 星期日 / 星期一9.14 / 星期二`（空表头兜底 + `星期X` 原样）、A 列 `1..12`、黄 / 绿底色正确、**滚到底部能看到「想看清单」那几行内容与文件一致**。
- **取消 / 失败 / 覆盖三种情形都实测**：取消（选择器返回键 + 预览弹层取消）→ **表逐像素不变**；失败（txt 改名的假 xlsx）→ **明确提示且表不变**；覆盖（原 200 格 → 3×3 合成夹具）→ 覆盖成功、非空格子数随之下降。
- **取证完成后设备上的 xlsx 已删除**（`ls /sdcard/Download/*.xlsx` = 0）。
- 截图在 `docs/screenshots/v2.34.0/`（9 张正式图 + `_work/` 中间图，后者被 `.gitignore` 覆盖、不进仓库）。

**C. 已知限制 / 未验**

1. **原生 5MB 上限那条路没在设备上真跑**（没往模拟器推 >5MB 的 xlsx）：`FILE_TOO_LARGE` 与「`SIZE` 返回 `-1` 时边读边数」**只有 Dart 侧 mock 测试 + Kotlin 编译通过，分支未经真机验证**。
2. **Kotlin 侧没有单测**（项目也没有这类基础设施），靠**编译 + 真机跑通**验证。
3. **只验了 AOSP 的 DocumentsUI（API 35）**，**没验国产 ROM / WPS 自带选择器**——为它们加的 `EXTRA_MIME_TYPES` 兜底**未经实测**。
4. **`onDestroy` 收尾挂起 Dart 调用的分支未实测**（选文件期间 Activity 被销毁）。
5. **解析在 UI 线程**（几十毫秒，用户文件量级），**接近 5MB 的大文件解析耗时未测量**、未做 isolate。
6. **导入后「加日期列」只在单测里验过**（`星期一9.14 → 周二 9.15`），**真机没点那颗按钮**。
7. **本轮不做导出**（用户没要求；导入已通，导出可以后续用同一套读取器的反向逻辑加）。
8. **表头行的底色不进 App**（`ScheduleColumn` 没有颜色字段），所以「K 格有底色」**只统计数据区**。
9. **`DISPIMG` 内嵌图片格只能当空**（WPS 专有，图片导不进来）。
10. ⚠️ **`docs/screenshots/v2.34.0/` 里有 2 张截图（`05_imported_grid_head.png` / `06_imported_bottom_watchlist.png`）含用户真实日程内容**（校党校 / 实验报告 / 想看清单…）——这是**私有仓库**、用于证明真实文件导入成功；**如实记录这条**（若用户要求换成合成数据截图，替换即可）。

- 测试 / 验证：`flutter analyze` **0 issue**；全量 `flutter test` **2014 例全绿**（v2.33.0 的 1964 → **+50**）；模拟器用**用户真实 xlsx** 实测导入结果与 **Python（openpyxl）独立解析逐项吻合**（22 列 × 48 行 / 130 格底色，唯一差异是 `DISPIMG` 格）。

---

## v2.33.0 (2026-09-17)

**新功能：「UP 主」与「历史」之间新增第 3 个 tab「日程」——一张可编辑的表格网格（点击格子编辑文字与底色 / 长按右拖像 Excel 一样自动填充 / 加行加列删行删列），数据只存本机、绝不进 Gist；v2.33.0**

**A. 新增「日程」页（可编辑表格网格）**

- **用户原话**：「可不可以在 up 主页和历史页中间加一个日程页面。我可以里面编辑日程，就像【金山文档 | WPS云文档】超级代办 大三上一样。我可以在里面标色，**长按右拖可以像 excel 一样自动填充**，**点击日程可以标色**、当然还有**删除添加**什么的功能。」
- **原型结构决定了这是「单元格独立」而不是「一行一个任务」**：用户提供的 `超级代办 大三上.xlsx` 解析结果——表头行 = 星期 + 日期**拼在一起**（`星期一9.14` 这种）、**单元格 = 独立文本 + 独立底色**、**同一行里的格子可以是完全不相关的事**（有一行依次是 `门票` / `修改门票` / `尹老板`）→ 原型是**手工网格**。App 照此实现：每格独立存 `{text, colorIndex}`，**行只是格子的容器、没有任何跨格语义**（不是 to-do 列表、不做勾选、不做完成态）。
- **色板 7 色，前 5 色逐字沿用原型实际色值**（`ScheduleCell.colorIndex` → `kScheduleColors`，**一处定义**）：黄 `FFFF00`（原型 61 格）、灰 `BFBFBF`（51 格）、绿 `92D050`（15 格）、红 `FF0000`（2 格）、蓝 `00B0F0`（1 格），**同序同值**，让他在 App 里标出的色和原表格里一模一样；后补橙 `FF9933` + 紫 `CC99FF`。
- **存「下标」不存 RGB**：写 RGB 的话，以后想调色板（换一套更协调的色 / 加深浅档）就得写数据迁移、把用户表里所有旧色逐个映射一遍；存下标则**「改色板定义」天然生效、已存数据零迁移**（注释里写明**只能往后追加、不能插队重排**——重排会改掉已存数据的显示色）。
- **越界下标 → 降级为无色**（不收敛到最后一个颜色）：**宁可少一个底色，也不把格子染成用户根本没选过的颜色**（他改过色板、旧数据越界时这点尤其重要）。
- **颜色不赋语义**：原型里「黄 = 待办」这类推断是**用户私有的**，**App 里不做任何硬编码**，只给一组色板让他自己用（`黄` 在注释里只作示例记录，不参与任何逻辑）。
- **`lib/models/schedule.dart`**：`ScheduleData{columns, rows}` / `ScheduleColumn{id,label}` / `ScheduleRow{id,cells}` / `ScheduleCell{text,colorIndex}`，**不可变 + 纯函数**（增删改都返回新实例，便于单测直接断言「改完长什么样」），**`fromJson` 任何脏输入都不抛**（截断 JSON / 结构全错 / 类型不对一律安全降级）。
- **`lib/services/schedule_store.dart`：只存本地 `SharedPreferences`（key `schedule:data_v1`），绝不写 Gist**。不写 Gist 的三条理由：**1) 污染结构**——Gist 里躺的是白名单（合集 / UP 主 / 视频），PC 端脚本按 schema 解析，塞一棵计划外的子树要么被忽略、要么在旧版脚本上触发解析失败；**2) 隐私**——Gist 默认公开（用户配置的 gist_id 大多 public），「大三上要交实验报告 / 校党校 / 面试」不该跟着白名单一起公开；**3) 无合并策略**——多设备并发编辑同一张表最后写入者赢，会静默丢数据。
- **三种情形分开处理**（这是 `load()` 存在的全部意义）：
  1. **没存过 → 给默认表并立刻落库**：列头锚在「本周一」，落库后**下次打开不会又跳一周**；
  2. **存过能解析 → 原样返回**（哪怕是**用户自己删空的表**——那是他的数据）；
  3. **存过但坏了 → 给默认表但不覆盖盘上坏值**：万一是手改坏的，**用户还有机会捞回来**，等他真的编辑了新表自然会把它换掉。
  读取 / 写入失败一律**静默不抛**（`debugPrint` 兜底）。
- **`lib/pages/schedule_page.dart`**：**表头行 + 左侧行头列冻结**（横向滚看更多日期、纵向滚看更多行，**表头与行头跟随镜像**、用户拖的永远是网格主体）；单元格 **96 × 44dp**（96dp 大致横排下「抄实验报告」这种 5 字中文，411dp 宽的机器上「行头 44 + 3 列」正好铺满首屏）、文本超出省略；**首次进入给一张可用的默认表 = 本周一起 3 列 × 8 空行**。
- **点击单元格 → 编辑面板**（改文字 / 选底色 / 「清除此格」）——即用户说的「**点击日程可以标色**」。
- **长按单元格 → 进入填充模式 → 拖动经过的格子实时预览 → 松手把源格的「文本 + 颜色」填过去**——即用户要的「**长按右拖像 Excel 一样自动填充**」；**四个方向都支持**（目标区 = 源格与当前格**张成的矩形**）；**源格只有颜色时只填色**；松手给「已填充 N 格」+「**撤销**」。
- **源格完全空白时不启动填充**（只提示「这个格子是空的，没有可填充的内容」）：Excel 会用空白覆盖，但**这是用户唯一的私人数据，手滑抹掉一整片代价太大**，所以这里**刻意偏离 Excel**。
- **加行 / 加日期列 / 删行 / 删列（二次确认）**：新列日期 = 最后一个「能认出来」的列头 **+ 1 天**（能识别 `周一 9.15` / `9月15日` / `9-15` 等写法，**认不出就退化到今天 +1**）。
- **修掉一个测试抓不到的缺陷**（真机取证时发现）：`Column` 默认居中致整块表**水平居中**、左侧凭空多一条灰带、表头底色也铺不满，已改 `CrossAxisAlignment.stretch` 并**补了像素级回归用例**。

**B. 底部导航 4 → 5 个 tab**

- **顺序变成：合集 / UP 主 / 日程 / 历史 / 个人**（「日程」插在第 3 位，正是用户要的「**up 主页和历史页中间**」）。
- **把散落的硬编码 tab 下标提成具名常量**（`kTabCollections` / `kTabUpowners` / `kTabSchedule` / `kTabHistory` / `kTabPersonal`，定义在 `playlist_page.dart` 顶部、**下标唯一真相**）——这是本轮**最易漏**的地方，插一项会让后面所有下标静默错位：
  - 改了 `playlist_page.dart` 的 `_tab` 初值、`_reloadTab` 的分支、`PageView` children、`NavigationBar.destinations`、**页脚文案**（原来**写死**「合集 / UP 主 / 历史 / 个人」）、以及多处类文档注释；
  - 改了 `history_page.dart` / `watch_stats_page.dart` 的文档注释（index **2→3**、**3→4**）。
- **全仓确认没有「从别的页面 jump 到历史页 / 个人页」的入口需要改**（`initialPage` 与网格无关的分页 `PageController` 未动）。
- **既有导航用例从 4 项加强到 5 项**（不是削弱）：`collection_page_test.dart` 的标签断言改为 `['合集','UP 主','日程','历史','个人']`，并新增 `日程（可编辑表格）` tooltip 断言。

**C. 已知限制 / 未验（如实告知，勿当成 bug）**

1. **拖到视口边缘不会自动滚动**：拖出可见区只**收敛到最近一格**并实时预览，**不会带动画滚屏去填更远的格子**（Excel 有 auto-scroll，本轮没做）。
2. **撤销只有一级，且只有「填充」给了撤销入口**；**删行 / 删列不可撤销**。
3. **只做了单指路径**：多指 / 中途第二指按下**未验**。
4. **只在 API 35 模拟器验，没在物理真机上验**：**触感与真实手指长按的容错未验**。
5. **横屏 / 分屏 / 暗色未验**（App 目前只做浅色纸面）。
6. **只做了一张网格**：原型里的**第二块表**与底部「**想看清单**」**没有并进来**（用户可用**加行**表达）。
7. **色板 7 色比原型多橙 / 紫**（超出原型 5 色）；若要回到 5 色，**删数组末两项即可**（**存的是下标，删末尾安全、不要插队**）。
8. **持久化只在本机**：**不做跨设备同步、不做导出 / 导入**（理由见 A 段的「不写 Gist」三条）；卸载 / 清数据即丢，**这是刻意接受的取舍**。

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **1964 例全绿**（v2.32.1 的 **1896** → **+68**：模型 / 存储 **30** + 页面 **28** + 导航 **10**）；Android 模拟器实测 **20 张截图**（`docs/screenshots/v2.33.0/`，每张都回读过）——**空白初始态**（3 列 × 8 行，列头 `周一 9.14` / `周二 9.15` / `周三 9.16`）、**编辑面板 8 色块**、**5 色同屏**（黄 / 灰 / 蓝 / 红 / 绿）、**长按右拖的松手前预览帧 + 松手后横铺三格**、**向下拖一列 + 「已填充 2 格」+「撤销」**、**加行 / 加列**（新列 `周四 9.17`）、**行头菜单删行**、**列头菜单 + 删列二次确认 + 删后该列数据消失**、**横向滚表头跟随而左侧行号列冻结**、**纵向滚行号跟随而表头冻结**、**`am force-stop` 冷启动后日程仍在**（落盘证据：`run-as` 读到的 `FlutterSharedPreferences.xml` 里 `c:3` 是**下标不是 RGB**，且**白名单结构一个字节没动**）；其余 4 个 tab 逐个点过都正常、页脚已显示 5 项

---

## v2.32.1 (2026-09-17)

**语义修正：v2.32.0 把信箱卡片的上下滑理解错了——本版把「下滑」从「跳过」改成「稍后」（只推后不判断，之后还能再看到），把「上滑」从「加入」改成「取回」（按后进先出把最近推后的那张拿回栈顶）；用户原话「下滑不是跳过，而是暂时不判断，先看后面的卡片，可以上滑回来」；v2.32.1**

**A. 下滑 = 稍后（defer）**

- **v2.32.0 的理解有误**：上一版把上下滑做成「**上滑 = 加入白名单、下滑 = 跳过**」，是**一对「加入 / 跳过」的判定动作**（见下方 v2.32.0 的 B 段，**历史条目原样保留、不回改**）；**用户看到后当场纠正**——**下滑不是跳过**
- **现在的行为**：**下滑 = 稍后**（`InboxSwipeAction.defer`）——当前这张**推到队尾**、**不做任何判断**
- **「不做任何判断」的确切含义**：**不进「已处理」集合、不写 Gist、不取元数据**，所以**之后还会再出现**（**队首随之换成下一张**，也就是用户要的「**先看后面的卡片**」）
- **与「跳过」的区别**：左滑跳过是**记入「已处理」**、该条**永不再出现**；下滑稍后是**零副作用**、只是**换了个位置继续排**

**B. 上滑 = 取回（restore）**

- **现在的行为**：**上滑 = 取回**（`InboxSwipeAction.restore`）——把**最近一次推后的那张**拿回**栈顶**
- **LIFO**：可**连续取回、逐张按后进先出回来**（先推 `A` 再推 `B`（`A` 被埋得更深），**连续上滑的取回顺序是 `B` → `A`**）
- **同样不写任何东西**：取回**不是判定**，不写 Gist、不标已处理

**C. 保留与边界**

- **左滑 = 跳过（仍记已处理）、右滑 = 加入**：**一字未改**；**底部两个按钮、撤销（`_undo`）语义也不变**
- **★ 删掉了 v2.32.0 那个误导性的 `const bool kInboxSwipeUpMeansLike`**：它的**字面语义已经错了**（上下滑现在**不是**一对可以互换的「加入 / 跳过」）——改成**四向四行为的唯一映射点 `_actionForEdge`**（`right→like` / `left→skip` / `down→defer` / `up→restore`），并在注释里**引用用户原话**、写明「**这里不该再有把上下滑对调的布尔开关**」
- **新增 `enum InboxSwipeAction { like, skip, defer, restore }`** 与一条「**稍后栈**」`_deferred`（**LIFO**）
- **边界（都不判定、不提示、弹回原位）**：
  - **没有可取的卡时上滑什么都不做**——且**不亮「取回」浮层**（**亮着却什么都回不来会让人以为卡丢了**）
  - **队列只有 1 张时下滑什么都不做**（「推到队尾」**就是原地打转**）
  - **已被判定的卡会从待取回里摘掉**
  - **同一张反复下滑时在栈里挪到栈顶、不留两份**
- **浮层文案与语义对齐**：下滑显示「**稍后**」（**刻意用浅底 + 描边**，与三个**实心「判定」标记**在视觉上分开，**因为它不是判定**）、上滑显示「**取回**」（**实心陶土**）；**竖滑两个都居中不倾斜**（与左右滑的**贴边 + 倾斜**区分开）
- **动画**：下滑 → **当前卡往下飞出、下一张顶上**；上滑 → **当前卡不飞出**（先弹回原位）、**被取回的那张从下方滑回栈顶** + 一条「**已取回「…」**」轻提示 —— 目标是让人看出「**有一张卡回来了**」，而不是「**当前卡被我判定了**」
- **保留下来的**：**双向跟手、主导轴判定、四向共用同一套阈值**（**距离 > 屏宽 30% 或 速度 > 700px/s，快速轻扫即可判定**）、**下拉刷新从卡片外的留白发起**

**D. 已知限制 / 未验（如实告知，勿当成 bug）**

1. **真机上没验「队列只有 1 张时下滑无动作」**：模拟器里队列有 **55+ 条真实未读**，**无法在不破坏真实数据的前提下造出恰好 1 张的队列** → 该边界**只有单元测试覆盖**
2. **深层队列的 LIFO 顺序没在真机整条验**：只在**顶 3 张**上验了（截图 + logcat）；**完整来回由 `_deckOrder` 断言覆盖**（3 张队列）
3. **取回动画的「当前卡不飞出」是测试逐帧断言、不是真机逐帧验证**：真机只抓到 **mid-drag 单帧与结束帧**（**320ms 动画抓不住 `screencap` 的启动延迟**）
4. **取证时在模拟器本地数据上做了一次真实左滑**（把一条标记为「已处理」），另有**动手之前**的 prefs 快照可对照

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **1896 例全绿**（v2.32.0 的 **1890** → **+6**；`test/inbox_swipe_test.dart` **41 → 47**，其中**改掉 9 条断言错误语义的用例**、**新增 / 重写 14 条**）；Android 模拟器实测（截图 13 张 `docs/screenshots/v2.32.1/` + logcat 原文）：**下滑 → 顶卡换成下一张、撤销按钮仍是灰的**（**证明稍后没进「撤销位」、没被判定**）；**上滑 → 那张回到栈顶 + 轻提示**；**LIFO 铁证（logcat）**：先推 `BV1Kgen6dEzS`、再推 `BV1F4ep6XENi`，**连续上滑的取回顺序是 `BV1F4ep6XENi` → `BV1Kgen6dEzS`**（后进先出）；**没有可取的卡时上滑无动作**（松手后还是同一张、队列一个字节没变、无浮层无提示）；**浮层**：下滑亮浅底描边「稍后」、上滑亮陶土实心「取回」；**左滑跳过 → 该条从牌堆消失**（仍是「已处理」）；**右滑加入 → 被 Gist 门禁拦住并弹提示**（模拟器无 token，预期）；**卡片外留白下拉 → `RefreshIndicator` 正常**

---

## v2.32.0 (2026-09-17)

**通知卡片改成整张自绘——收起态一行标题 + 一行按钮，六个按钮全部一眼可见可点、再也不需要点箭头展开；信箱卡片补上「上下滑」手势（上滑 = 加入、下滑 = 跳过）；⚠️ 但箭头本身消不掉，且不是 `MediaStyle` 造成的（有对照组实测），详见 A 段；v2.32.0**

**A. 通知卡片改成整张自绘（需求：「换个渲染卡片的方式」实现「不用点箭头展开」）**

- **上版的问题**：`MediaStyle` 的**收起行只能放 3 个按钮**，其余要点**展开箭头**才看得到——用户不满意
- **做法**：**弃用 `MediaStyle` 与 `PlayerNotificationManager`**，改为 `NotificationCompat.Builder` + **`setCustomContentView` 自绘两张 48dp 两行卡片**（标题行 + 按钮行），**自建广播接收器**
- **结果**：**收起态 = 一行标题 + 一行六个按钮全部可见可点**（上集 / 快退15s / 播放暂停 / 快进15s / 下集 / 关闭）；**单集或合集头尾时自动变四个、直播时两个**，**无灰按钮**；**播放 / 暂停图标随状态互换**；**按钮宽度用 `weight` 平分**（**写死 dp 会被右侧箭头条压掉**）
- **新增文件**：`res/layout/notification_playback_card.xml` + 6 个 drawable（`ic_notif_play` / `pause` / `rewind` / `fastforward` / `close` / `small`）；**后两个（`close` / `small`）必须 App 自己持有**——`media3-ui` 的资源在应用 `R` 里取不到（`nonTransitiveRClass`）
- **Dart 侧零改动**：既有 **22 例通知契约测试原样通过**

**⚠️ A1. 关键发现一：展开箭头消不掉，而且不是 `MediaStyle` 造成的（本轮做了对照组）**

- **本轮实验直接推翻了「箭头是 `MediaStyle` 带来的」这个假设**：用 **`adb shell cmd notification post` 发的一条最普通的系统通知，同样带箭头** → **箭头是 Android 15 通知行的固有 UI**
- 唯一**没有箭头**的是系统「**媒体卡片**」（**必须绑 `MediaSession` token**），但**实测那里我们自己配的按钮一个都不渲染**（而且**单集时系统只给 ⏮ 不给 ⏭**，那个 ⏮ 是「**回到本条开头**」而不是「上一集」）
- **所以本轮交付的是「所有按钮一眼可见可用、不再需要点箭头」，不是「箭头消失」**——请勿按「箭头应该没了」来验收

**⚠️ A2. 关键发现二：收起态给自定义布局的盒子实测只有 ≈271dp × ≈48dp**

- 盒子左右各被系统吃掉一块：**左边 36dp 被系统小图标徽标占、右边 56dp 被箭头条占**（API 35 模拟器实测）
- → **一行放不下「标题 + 6 个按钮」，必须两行，且总高精确 48dp**（**标题 16dp + 按钮 32dp**），实测**零裁剪**
- 按钮宽度因此**必须用 `weight` 平分**（固定 dp 会被右侧箭头条压掉甚至挤没）

**⚠️ A3. 取舍：48dp 放不下三行**

- 48dp 装不下「标题 + 副标题 + 按钮」三行，所以**标题与「UP 名 · 状态」放在同一行左右分栏**——**代价是长标题会截断**（**右侧信息优先保留**）

**A4. 逐条实测确认保留的行为（都没有回退）**

- **点通知回 App** ✅；**耳机 / 蓝牙媒体键仍能控制** ✅（走 `MediaSession`，**「不绑 token」的决策不变**）；**锁屏可见** ✅
- **划掉的既有语义不变**：**暂停态可划掉、播放态是 `ONGOING_EVENT` 划不掉** ✅
- **快退 / 快进恰好 15 秒** ✅；**封面 / 标题 / UP · 状态文案** ✅；**`✕关闭` 仍能停止播放并撤下通知** ✅

**B. 信箱卡片支持上下滑（需求：「完善一下上下滑 tinder 卡片的手势，选择可以快速跳过或者选择判断对象」）**

- **现在的行为**：**上滑 = 加入白名单（同右滑）**、**下滑 = 跳过（同左滑）**；**左右滑的既有语义不变**
- **卡片双向跟手**（原先只跟水平位移），**旋转仍按水平分量**（**倾斜手感不变**）；松手按**主导轴**判定，**四个方向共用同一套阈值**（**距离 > 屏宽 30% 或 速度 > 700px/s**）→ **快速轻扫即可判定**；**退出动画按实际方向飞出**（上滑往上飞、下滑往下飞）；**浮层徽标与拖动方向一致**（垂直主导时**居中且不倾斜**——原先贴左右边缘的摆位在竖滑时看不出方向）
- **★ 动作映射集中在唯一一处**：`lib/pages/inbox_page.dart` 里的 **`const bool kInboxSwipeUpMeansLike = true;`**——**改成 `false` 即翻转上下滑的语义**（判定、飞出方向、徽标渐显、撤销 fly-back 全都从这一处取结论）。**用户原话「上下滑」有歧义，故在此明示当前取的是「上 = 加入」；要反过来只改这一个常量**
- **新增 12 例测试**：四向判定 / 四向快速轻扫 / 阈值内弹回 / 斜向按主导轴 / 上滑后撤销 / 浮层摆位 / 上下拖不带走滚动

**⚠️ B1. 唯一一处既有行为变化（下拉刷新的发起位置变了）**

- 为了让上下滑跟手，**卡片必须在手势竞技场里赢过外层滚动容器** → **纵向拖动会被卡片吃掉，下拉刷新只能从「卡片外的留白」发起**（**左右拖动不受影响**）
- **原有 3 条下拉刷新用例的断言语义未改**，只改了 fling 的**起点**（从卡片中心挪到**卡片上方的留白**）——**行为变化已如实记录，不是测试将就实现**

**C. 已知限制 / 未验（如实告知，勿当成 bug）**

1. **箭头仍在**——原因见 A1，**有对照组证据（普通系统通知同样带箭头）**；**本轮交付的是「按钮一眼可见可用」，不是「箭头消失」**
2. **直播态的通知卡片没做端到端取证**（模拟器无中文输入法，搜「直播」两次都无结果、进不去直播页）；已验的是**同一套 `GONE` 机制在本机生效** + 直播 gate 的**两行代码** + **v2.27.0 的直播链路未改**
3. **收起态盒子高度只在 API 35 模拟器上量过**——若某些 ROM（**MIUI / HyperOS 改过 SystemUI**）盒子更小，**最坏是按钮行底部被裁几 dp（触控变矮）**；**按钮不会消失或移位**（宽度用 `weight` 自适应）
4. **信箱「加入」写 Gist 的端到端未验**（模拟器无 token）；**点卡片真正 push 到播放页**在真机上只验到「**点按链路被触发**」（离线态 `fetchVideoMeta` 失败）
5. **竖向飞出动画与「撤销从下方飞回」没能拍成真机图**（**320ms 动画抓不住 screencap 的启动延迟，机器上无 ffmpeg 抽帧**）——由**开动效的 widget 用例**覆盖
6. 真机未验**斜向拖动（按主导轴）**与**多指同时按在卡片上**
7. **`media3-ui` 依赖现在已无人使用**（`PlayerNotificationManager` 整段弃用），但按约束**本轮没动 `build.gradle.kts`**——**依赖暂未摘除，留待后续决定**

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **1890 例全绿**（v2.31.0 的 **1878** → 通知卡片 **+0**（Dart 未改）→ 信箱手势 **+12**）；Android 模拟器实测：**通知卡片**收起态 `uiautomator` 数到**封面 + 6 个按钮共 7 个可见可点节点、坐标全部落在箭头条左侧**，`dumpsys notification` 里 `contentView=.../0x7f0d0039`（= 新布局），**播放↔暂停图标互换 / 单集变 4 个按钮 / 锁屏显示 / 耳机键 / ±15s / ✕关闭 / 点通知回 App 全部通过**（截图 `docs/screenshots/v2.32.0/`：`notif_card_vod_playlist.png` / `notif_card_vod_paused.png` / `notif_card_single_video.png` / `notif_card_lockscreen.png`）；**信箱手势**四向 logcat 原文 `[inbox] 判定 edge=_SwipeEdge.up → 加入 … drag=Offset(0.0, -144.8)` / `down → 跳过` / `right → 加入` / `left → 跳过`，**下滑走完全链路（卡片出栈、下一张顶上来、撤销能恢复）**，**上滑被 Gist 门禁拦住（模拟器无 token，属预期）——只验到「识别为 like + 门禁提示 + 卡片弹回」，飞出方向与撤销方向由开动效的 widget 用例钉住**（截图 9 张）

---

## v2.31.0 (2026-09-16)

**动态终于能点进详情页了（正文 + 图片宫格 + 视频投稿卡 + 转发原文 + 只读互动数据 + 整条评论区），并修掉「部分专栏图片不显示」——正文解析失败时不再把原始 JSON 当纯文本画到屏幕上；⚠️ 本轮探针推翻了原定的动态评论口径，详见 A 段；v2.31.0**

**A. 动态详情页（需求：「动态页点击动态可以进入详细动态」）**

- **新增 `lib/pages/dynamic_detail_page.dart`**：**骨架照专栏页——正文当 `CommentListView` 的 `header`、评论区是唯一滚动者**（**不 `shrinkWrap`**），因此支持**下拉刷新**，不会出现「内层列表抢滚动」
- **正文块**：作者行 + **全文（不折叠）** + 图片宫格（可点开大图）+ 视频投稿卡（可进播放页）+ **转发原文（含原文的图片 / 视频，画在引用块内部）** + 互动数据（点赞 / 评论 / 转发，**只读**，**三项全 0 时整块不渲染**）
- **首屏不空白**：从列表点进来时**先立即渲染手上那条**，后台再拉详情补全；**补全失败静默**（正文已在手上，弹错误反而更差）。**没有 `initial`**（例如从别处直达）时才先 loading 再拉，失败给**可重试**错误态
- **新增 `BiliApi.fetchDynamicDetail(id)`**：照抄 `fetchUserDynamics` 的 WBI 签名 / `-412` 换 key 重试 / 错误码分类，**任何失败一律返回 `null` 且 `debugPrint`，绝不上抛**（调用方只关心「有没有拿到」）
- **`DynamicItem` 新增**：`DynamicStat{like, comment, forward}`、`commentType` / `commentId`、以及**转发原文的图片 / 视频字段**；解析一律**宽松**（缺失即安全默认值，不抛）
- **`DynamicCard` 新增可选 `onTap`**——**不传时行为与改动前完全一致**（其它调用点零影响）；把 `_DynamicImages` / `_DynamicVideo` / `_DynamicAuthorRow` **提升为公开**供详情页复用（**同一份实现只留一处真相**，避免两边各自维护一份宫格 / 视频卡）

**⚠️ A1. 动态评论的正确口径（本轮最重要的一条事实，别改回「17 + 动态 id」）**

- **原定方案是错的**。原计划用 **`type=17` + 动态 `id`** 取动态评论；**探针实测直接推翻了这个方案**。**正确口径：用服务端在 `basic` 里给的 `comment_id_str` 当 `oid`、`basic.comment_type` 当 `type`**——**服务端给什么就用什么**，不要自己拼
- **四种组合的实测结果（同一台机、同一条动态）**：
  - `type=11 & oid=<comment_id_str>` → **`code=0`**（`replies=3`，`all_count=43`）✅
  - `type=17 & oid=<comment_id_str>` → `-404 啥都木有` ❌
  - `type=11 & oid=<动态 id>` → `-404` ❌
  - **`type=17 & oid=<动态 id>`（= 原方案）→ `-404`** ❌
- **实现**：`basic` 缺失时才回退 `type=17 + int.parse(id)`（**有专门用例钉住这条回退口径**）。**真机三例**：`aid=117218029342436 / 408967939 / 1242312165069684774`，评论区 `total` 与 `module_stat` **完全一致**（**1790 / 50 / 204**）
- **一句话结论：别改回 `17 + dyn id`。** 那个组合在真机上是 `-404`，改回去会让评论区整块空掉

**A2. 顺手修掉一个会让本功能失效的坑：动态卡正文吃掉了点击**

- 动态卡正文此前用**可选择文本**（`SelectableText`），它会**吃掉正文区域上的点击**——用户点到文字上时**只有卡片四周的留白能进详情**，主观感受就是「点动态没反应」
- 改为**不可选择** + **保留长按复制兜底**：`ExpandableText` 新增 `selectable: false`，并在传了 `copyTip` 时给**完整态**也挂上 `onLongPress`（折叠态原本就有长按展开）。**其它调用方不传 `copyTip`，行为逐字节不变**

**B. 专栏图片修复（需求：「修复部分专栏中图片无法正常显示的问题」）**

**⚠️ B0. 探针结论（如实交代：这是防御性修复，不是已证实的现场主因）**

- **用户贴的那张图，带 / 不带 `@progressive.webp` 后缀现在都是 404** → 「部分图不显示」里**确实存在「图本身已被删除」这一类，不可修复**
- **「正文不是合法 JSON」这个猜想，没能在真实数据上复现**：唯一成功拉到的一篇是 `type=4`（opus 型）**纯文本**内容，`jsonDecode` 抛异常属预期行为
- **但这条路径的危害已实测复现**：把「解析失败就退回 HTML 分支」改回去后，页面级用例**直接报「泄漏了 ops」**——**原始 JSON 真的会被当纯文本画到屏幕上、还能被用户复制出来，并且图片全丢**。所以这个洞**必须堵**，但它**不等于**已证实是现场主因

**B1. 六项修法**

1. **`parseBiliDelta` 失败打日志**：带 **cv 号 / 入参长度 / 异常原因**，原因**截断 120 字符**（够定位、不淹日志）
2. **坏转义宽容修复后重试一次**：只修**字符串内部的裸控制字符与非法转义**；**正常 JSON 第一次就成功、根本进不到这里**；修完还要**再 `jsonDecode` 验一次**，验不过**当没修**（不把「修坏」的结果递给下游）
3. **解析失败不再把原始 JSON 当纯文本画**：改为一句「**正文解析失败…**」提示 + **从原文里捞图片**（图片后缀白名单、去重、**封顶 100 张**）。**取舍**：**只救图片不救文字**——结构已不可信，硬切出来的半句话**比一句明确提示更误导**
4. **图片加载失败后剥 `@…` 后缀重试一次**：**正常路径不改写 URL**（主决策不变），只重试**一次**、**不会打转**
5. **`src` 是 `data:` 内联占位时改用 `data-src`**；站内**相对路径（前导单斜杠）补 `https://i0.hdslb.com`**
6. **`image_urls` 图集兜底**：正文**一张图都没有**且 `imageUrls` 非空时，在正文后**补上图集**（**正文已有图时不重复**），并使用**如实的说明句交代来源**

**B2. 测试**

- **新增 15 例**（专栏图片修复批次全套），其中 **3 条在修复前实测为红**（含 `泄漏了 ops` 那条）

**C. 已知限制 / 说明（如实告知，勿当成 bug）**

1. **「图被删（404）」这一类不可修复**——App 只在失败占位里如实显示「图片加载失败」
2. **「坏 JSON 兜底」与「去后缀重试」两条路径，在真机上没有触发样本**（模拟器上那篇 17 图专栏**首帧全部加载成功**；该 UP 的专栏都是**老 HTML 型**，走不到 Delta 分支）——**这两条只有单测覆盖**
3. **`image_urls` 图集对 `type=4`（opus 型）专栏可能是动态封面 / banner 而非正文配图**，此时图集里会出现「正文没提过的图」——已加说明文案交代来源，属**已知取舍**
4. **动态评论的 `comment_type` 取值不硬编码**（服务端给什么用什么）；**`basic` 缺失时回退 `17 + dyn id`**——有专门用例钉住该回退口径，但**真机上未遇到该分支**
5. **探针超预算，如实记录**：**专栏探针 6 次上限、实际 7 次**（超 1 次，原因是 `-509 请求过于频繁` 触发重试）；**动态探针 11 次 / 上限 5 次**（**严重超**，因 `feed/space` 对本机 IP 持续 `-352`，后改用**已知 dyn id** 才拿到数据）
6. **动态详情未验证**：返回列表后再点**另一条**的连续往返；**未逐一实测** `type=11` 之外的其它 `comment_type`

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **1878 例全绿**（v2.30.0 的 **1831** → 专栏图片 **+15** → 动态详情 **+32**）；Android 模拟器取证 10 张（`docs/screenshots/v2.31.0/`）——**动态详情**：点卡片进详情、图文动态、评论区（含楼中楼与触底翻页）、**转发动态（原文的视频卡画在引用块内部）**全部正常；**回归**：动态列表正常、**点图仍进大图（列表与详情两侧都验）**、**点视频投稿仍进播放页**；**专栏图片**：白名单 UP 的一篇 **17 图专栏全部正常渲染**、评论区正常、**点图进大图（`2/17`、图集下标一一对应）**正常

---

## v2.30.0 (2026-09-16)

**播放页与通知栏都能「上一集 / 下一集」了——合集页与 UP 主页进播放时底栏多一行切集按钮，通知收起行放上「上一集 / 播放暂停 / 下一集」（展开态 5 键），并顺带修掉一个「切集后旧视频进度被写进新视频」的真 bug；v2.30.0**

**A. 播放页：同合集 / 同 UP 主主页的「上一集 / 下一集」**

- **新增 `lib/models/playlist_context.dart`**（`PlaylistContext{videos, label}`，只读无状态）；`PlayerPage` 新增**可选**参数 `playlist` / `playlistIndex`——**不传时界面一个像素都不多**
- **底栏顶部多一行**：`[‹ 上一集]  合集 · 3/5  [下一集 ›]`；**头尾禁用但保留可见、不循环**（到头就把按钮抽掉会让位置每次跳动，禁用态才是「还有没有上一集」这个问题的答案）；**播放列表只有 1 条时整行不渲染**（不放死按钮）
- **切换复用已有的 `playVideo()`**（换 bvid 停旧播新的既有实现），并补上它漏掉的 `_resetWatchBaseline()`；邻居**缺 cid 时先用 `fetchVideoMeta` 补齐**，失败**只提示、不打断当前播放**
- **接线入口只有两处**：**合集页**（传整个合集 + 被点下标）与 **UP 主页**（传其视频列表）。**故意未接**：历史记录 / 每日历史 / 搜索 / 信箱 / 收藏夹 / 评论跳转——这些列表的序是「**观看时间序 / 相关度序**」，跳「下一个」会到不相干的视频；**UP 主页的「合集 / 列表」分区也故意不接**，同一个 bvid 常在两处出现，用错列表会让「下一集」跳到不相干的视频
- **顺手修掉一个真 bug**：`_saveExitProgress` / `_saveProgress` / `_writeHistory` 在 **await 之后**才读 `_video` / `_currentPageIndex`，而 `playVideo` 会在同一个事件循环里把 `_video` 换成新视频 → **旧视频的播放位置被写进新视频的进度 key**，同时旧视频的进度丢失。该代码路径此前**没有调用方**（休眠），上下集一上线就会 100% 命中。已改为**同步快照后再写**

**B. 通知栏的上一集 / 下一集**

- **做法**：继承 `PlayerNotificationManager` 覆写 `getActions(Player)` 与 `getActionIndicesForCompactView(List<String>, Player)` + 自实现 `CustomActionReceiver` + 两个新 drawable（`ic_episode_previous.xml` / `ic_episode_next.xml`），把自定义 action 塞进**收起行**。绕开内建 `ACTION_NEXT` 的原因：单 `MediaItem` 的 ExoPlayer 拿不到 `COMMAND_SEEK_TO_NEXT`，那个内建 Next **永远生成不出来**
- **有上下集时**（`hasPrev && hasNext && !isLive`）：**收起行 = `上一集 / 播放暂停 / 下一集`**；**展开态 = `上一集 / 快退15s / 播放暂停 / 快进15s / 下一集`**。真机（模拟器）实测点收起行的「下一集」**真的换集并起播**（logcat 有完整链路）
- **到头 / 单集 / 直播时完全回退**成原来的 `⏪ ⏸ ⏩ ✕`（**不放灰按钮**）
- **可用性由 Dart 推给原生**：`updateNowPlaying` 新增 `hasPrev` / `hasNext`，**缺省 false** 保持旧调用方可用；`_playNeighbor` 公开为 `playNeighbor` 供通知链路与底栏共用
- **没有**用会话的 `seekToNext()` / `seekToPrevious()` 代替切集：单 `MediaItem` 时前者不存在，后者是「**回到本条开头**」——**实测过**（1396349 → 0）

**C. 修「上下集行」的布局缺陷（真机实测发现）**

- **现象**：`Row` 的三个孩子都是 `flex:1`，标签占掉 1/3、两个按钮各只 1/3，`Flexible` 用不完的份额变成**行尾死区**（横屏最多 **234dp**）→ 点视觉上「下一集 ▶」附近（屏宽 **75%** 处）**落空**
- **修复**：改成两个 `Expanded` 各拿真正半屏 + 标签用 `Stack` 居中浮层（`IgnorePointer`，**不吃点击、不参与宽度分配**），标签有明确上限 + 省略号；触摸目标从内容 18dp 变成**整行 40dp**
- 顺带：有上下集行时中央控制簇 `Transform.translate` **上移一行高**，消除与中间标签的视觉穿插；**横滑 seek 浮层也空出同样高度**，避免进度条上下跳

**D. 平台限制 / 已知限制**

1. **通知的「折叠」消不掉**：不绑 `MediaSession` 时 MediaStyle **必有**展开视图，**而且与按钮数量无关**——实测把 action 砍到 3 个并全部映射进收起行，**展开箭头依然在**。所以本轮的做法是「**把最想点的 3 个按钮放进收起行**」，而不是「不折叠」
2. **绑 `MediaSession` 能实现「不折叠」**（系统媒体卡片常显 + 可拖进度条），但实测**我们自己配的 4 个按钮一个都不渲染**，且单集时系统**只声明 `ACTION_SKIP_TO_PREVIOUS`、没有 `SKIP_TO_NEXT`**，那个 ⏮ 点下去是 `seekToPrevious()` = **回到本条开头**（1396349→0）。综合取舍后**留在「不绑」这条路**。B 站之所以有一排可用按钮，**最可能是因为它挂了真正的播放队列（多 `MediaItem`）**——这是**推断**（未反编译其 APK）
3. **展开态只有 5 个槽位**（framework-res 的 `notification_template_material_big_media.xml` 只 include action0..action4）：有播放列表时共 6 个 action，因此 **`✕` 关闭让出槽位**（仍注册、只是不渲染；用户可回 App 停止、退出播放页暂停，暂停态通知可划掉）
4. **「同专栏上下集」本轮没做**：专栏正文里的视频链接目前**一律丢给系统浏览器**、Delta 的 `video-card` 还被解析器显式跳过 → 「同专栏的视频列表」这个数据**不存在**，需要先做「正文视频站内播放」。另外**历史 / 搜索 / 信箱 / 评论跳转**也**故意不接**播放列表（语义不同）
5. **通知 action 顺序只在 Kotlin 层，Dart 单测观测不到**（仓库无原生单测框架），本轮的证据是 **`dumpsys notification` + 截图手工取证**；**没有为它造假测试**
6. **未验**：直播下的通知形态（代码 gate `!isLive && hasPrev && hasNext` 保证走单集那条已实测路径，但**未真机跑直播**）；锁屏 / 系统媒体卡片；窄屏（<360dp）/ 平板 / 折叠屏；**「划掉通知」手势**（`adb input swipe` 试了 3 组参数没触发 `onNotificationCancelled`，所以**不宣称已实测**，已验证的替代路径是从播放页返回）

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **1831 例全绿**（v2.29.0 的 1809 → 上下集 **+11** → 通知 / 几何 **+11**）；Android 模拟器取证 20+ 张（`docs/screenshots/v2.30.0/`）——上下集行在**竖屏 / 横屏置顶 / 全屏横屏**三种形态都在且不挡既有按钮；点「下一集」真的换源起播（`播放列表切集 下一集 3 -> 4/5` → 新 `fetchPlayUrl` → 新 `onPrepared`）；第 1 集「上一集」禁用（连点零日志）；**从历史记录进入没有这一行**（未接入口零影响）；**进度记忆正确**（切走前旧视频 68386ms 已落盘、新视频从自己的位置起播，即 A 里那个 bug 已修）；通知 `dumpsys` 原文 `actions=[上一集, Rewind, Pause, Fast forward, 下一集, Stop]`、`compactActions=[0,2,4]`，展开态 5 键对称；**单集 / 合集末条回退成 4 个 action（含 ✕）**与改动前一致，快退/快进仍 15 秒、✕ 仍能停止并撤下通知

---

## v2.29.0 (2026-09-15)

**缓存可以只存音频了（30 分钟视频从 200~500MB 降到 15~36MB）、「缓存管理」弹层升级成独立的「离线缓存」页并补上占用管理；合集改成「路径式多层嵌套」——⚠️ 这是对 v2.28.0「移动到其他合集 = 并入并删除源合集」的语义更正；另修冷启动直奔「个人」页时缓存入口不显示计数；v2.29.0**

**A. 缓存：仅音频下载 + 独立「离线缓存」页 + 占用管理**

**A1. 只缓存音频（新功能，本轮最大的省空间手段）**

- **实测对比**：30 分钟视频整段缓存 200~500MB，仅音频 15~36MB。真机（模拟器）实测：17:37 的视频只音频 = **5.4MB**，1:21 的视频整段 = 1.8MB
- **播放怎么实现**：**把音频文件当 `videoUrl` 传给播放器**。原生 `DashExoPlayer.prepare` 在 `audioUrl` 为空时只用 `videoUrl` 建一个 `ProgressiveMediaSource`，而 Dart 侧 `videoUrl` 是必填位置参数——音频 `.m4s` 本身就是合法单流（AAC），走同一个单流分支就**有声音、能 seek，原生一行不用改**（模拟器实测：AAC 解码器 `c2.android.aac.decoder` 起、`onPrepared 0x0 duration=1056468`（无视频轨 → 宽高 0）、拖动进度从 4:34 跳到 15:34 后继续播、`state=PLAYING` 全程 `error=null`）
- **音频档位按码率挑最低档（顺手修掉一个隐性浪费）**：`dash.audio[]` 的**顺序不稳定**（实测有时 30216/64kbps 在前、有时 30232/134kbps 在前），此前取 `.first` 等于把「省多少空间」交给运气。现解析时一并收下 `id`/`bandwidth`，**仅音频时挑 bandwidth 最小的一条**；码率缺失时退回第一条（与改动前一致）。**整段缓存的既有行为不变**
- **入口**：播放页「离线下载」菜单新增「**仅缓存音频（省空间，无画面）**」（多 P 另有「仅缓存音频（全部 N 集）」）；已整段缓存过的集还会多一项「**改为仅缓存音频**」，重下即覆盖并**当场删掉旧的 `.video.m4s`**（不靠用户手动回收）
- **角标区分**：列表角标在「缓存全是仅音频」时显示「**已缓存音频**」（多 P 部分缓存为「已缓存音频 n/m」），点进去没画面时不至于以为播放器坏了；播放页在仅音频播放时用封面 + 「仅缓存了音频：无画面，可正常听声音」顶住画面区（否则纯黑屏）
- `CachedVideo` 新增 `audioOnly` / `cid` / `durationMs`；**旧索引缺字段一律走默认值**（`false`/`0`/`0`），`version` **不升、不写迁移**

**A2. 独立的「离线缓存」页（取代原「缓存管理」弹层）**

- **为什么是独立页而不是「专门放缓存视频的合集」**：缓存视频**不一定是白名单视频**——历史记录、搜索、收藏夹、评论列表里的视频都能进播放页下载，这些 bvid 在 `WhitelistData.videos` 里**根本不存在**，没有对象可以挂合名字段；做成特例合集还会自动污染主页卡片、合集管理面板、统计、Gist 同步与 PC 端 CLI。故缓存列表自成一页（数据源 = 本地索引，与白名单互不干涉）
- **页面结构**：顶部「缓存占用」（分项）→ 按 **bvid 分组**（组头：封面 + 标题 + UP 名 + 条数 + 总大小 + 「仅音频」标记）→ 每条分 P：**播放 / 单删**，副信息 = 大小 · 缓存时间 · 「音频」/「视频」；页脚「清空全部缓存」。视觉走 `context.palette`/既有 token、无阴影，空态用 `AppStateView`（`empty.cache`，seed = cache）
- **从离线页进播放页一律用 `CachedVideo` 现场构造 `WhitelistVideo`**（bvid/cid/title/cover/upName/duration 都在索引里了）：白名单数据在播放页上游的 State 里、靠 Gist 网络拉取，而本页的使用场景恰恰是「没网」——为了拿一个更「完整」的对象让入口在最需要它的时候失败不划算
- **播放页配合改一处**：**无分 P 信息时不再把 `initialPageIndex` 夹到 0**（原实现恒夹 0）。离线页拿不到整条 pages 列表，夹到 0 会让「点第 2 集」去命中第 1 集的缓存；缓存键本来就是 `(bvid, pageIndex)`
- 入口（「个人」页 → 离线缓存）文案带上实时概要「**N 个视频 · X**」；旧的 `_CacheManageSheet` 已整体搬进新页，**不留两套**

**A3. 缓存占用管理（「能不能压缩存储」的正面回答）**

- **结论：不做转码压缩**。缓存下来的 `.m4s` **本身就是 H.264/AAC 有损压缩流**，zip/tar 再压只能省 0~3%；重新转码降码率要引入 ffmpeg 到缓存链路、耗时约半个视频时长、有损、失败还要清半成品。真正能省的就是 A1 的「只缓存音频」与下面的「中转音频清理」
- **分项占用 `diskUsage()`**：媒体文件（索引内在册的实际文件长度，不信记账值）+ **中转音频**（`audio_tmp/`）+ **残留**（`.part` / 孤儿，含个数）。占用口径是「这两个目录真占的磁盘」，三者分开展示——三条来源不同、清理入口也不同
- **清理中转音频 `SherpaAudioSource.cleanTmpAudio()`**：`audio_tmp/` 里是实时转写的临时 m4s 与 **16k 单声道 wav（32kB/s，30 分钟一集约 57MB，比音频流本体还大）**，而此前**全项目没有任何删除入口**。代价是下次转写要重新下载+转码（几秒）；**不动**「离线缓存 audioPath 存在就复用」那条判断（它复用的是 `video_cache/` 里的音频，不属于中转目录）
- **回收残留 `reclaimOrphans()`**：删 `video_cache/` 下不被索引引用的文件与 `.part`（下载中途崩溃/进程被杀留下的），**不碰**索引内文件、**不碰**在途下载（登记了 `_writing`，避免把正在写的半成品删掉导致 rename 失败）、**不碰** `audio_tmp/`
- `audio_tmp/` 的路径**只有一处定义**（`SherpaAudioSource.tmpAudioDir`），占用统计复用同一函数，不在别处重拼路径

**B. 合集改成「路径式多层嵌套」（⚠️ 语义更正：v2.28.0 的「移动到其他合集」是「并入」，会删掉源合集）**

- **⚠️ 先看这条：这是对 v2.28.0 的语义更正——同一个按钮，点下去的结果和上一版完全不同。** v2.28.0 的「移动到其他合集」是**「并入」**：把源合集的视频全部挪进目标合集，**然后删掉源合集本身**。澄清后的正确语义是**嵌套**：把源合集**挂到目标合集下面**，**源合集不被删除**，它的视频、它的子孙合集**级联跟着走**，在目标合集里可以**继续打开**它，还能一层层继续嵌套下去。**升级后「移动到其他合集」不会再丢合集、也不会再合并顺序**
- **合集的身份 = 路径字符串**（分隔符 `/`，如 `动画/2024冬`）：`WhitelistVideo.collection` 存**完整路径**，`collections[].name` 同步升级为路径。**合集名不允许含 `/`**（层级只能靠挂载，不能靠命名）；**旧数据零迁移**——不带 `/` 的名字天生就是顶层合集，v2.28.0 及以前的数据读进来行为与从前逐字一致，`version` **不升、不写迁移**
- **数据层全是纯函数**（`lib/models/whitelist_video.dart`，不碰 IO，便于单测）：新增 `moveCollectionUnder(data, source, target)`（`target` 传空串 = **移回顶层**，这不是「未分类」——合集不能变成未分类）与 `createSubCollection(data, parentPath, name)`；`renameCollection` / `deleteCollection` 改为**级联**；新增路径工具 `collectionParentOf` / `collectionLocalName` / `collectionDepth` / `collectionAncestors` / `collectionDescendants` / `collectionChildrenOf` / `isCollectionUnder` / `validateCollectionName` / `collectionDisplay`
- **并入语义已彻底删除**：纯函数 `moveCollectionInto` 及其用例整段移除，确认框里「并删除…这个合集」「无法在 App 内撤销」的旧文案**全局 grep 已消失**——同一件事绝不留两套相反语义
- **级联规则**：**重命名 / 移动**都级联改子孙路径与其视频的 `collection`（不级联就会留下一堆指向不存在合集的引用）；**删除父合集时直属子合集上提一级**（`甲/乙/丙` → `甲/丙`，**不连带删除子孙**，子孙里的视频也保留），被删合集的**直属视频**按老规矩落回未分类；**目标下同名冲突抛 `CollectionException`**（不自动改名、不合并）
- **防环**：不能把合集移到**它自己或它的子孙**下面；目标选择器里这些项**根本不出现**（模拟器实测：自己 / 子孙 / 当前父级都不在列表里）
- **`sortedVideos(路径)` 改成精确匹配**：父合集页面**只显示直属视频**，不混入子孙合集的视频——否则父页面里两套 `order` 会打架、排序退化成不可预期。要看子孙视频就点进子合集
- **UI — 首页 / 管理面板**：首页**只显示顶层合集**（`depth == 0`，子合集在自己的父合集里），合集卡副信息加「**含 N 个子合集**」；合集管理面板同样只列顶层
- **UI — 合集页（重写）**：AppBar 标题是**局部名**（不是全路径）；非顶层时标题下有一行**可点的「返回上级」面包屑**；列表顺序**先子合集卡片、后直属视频**；子合集卡**左滑 = 移动 / 重命名 / 删除**；页内可**「新建子合集」**；拖拽排序**不带动子合集卡**（`reorderCollections` 只做同级重排，传进的名字必须是现有名单的一个**排列**，混入别层或想借重排偷改路径一律抛错）
- **UI — 视频「移动到合集」的目标选择器**：**按层级缩进 + 全路径**展示（`collectionDisplay` 把 `甲/乙` 渲染成 `甲 / 乙`；顶层没有 `/`，展示与原来逐字一致），**可选任意层级**，且**不提供「未分类」**（与「删除合集」语义重复）
- **新增公共对话框模块 `lib/widgets/collection_dialogs.dart`**（首页与合集页共用）：抽出来的理由是**这些弹层的文案就是语义本身**，只留一处真相，才不会出现「首页改对了、合集页还写着旧文案」。旧的内联弹层已整体删除，**不留两套**

**C. 修复：冷启动直奔「个人」页时「缓存管理」入口不显示缓存计数（真机发现）**

- **现象**：冷启动后直接进「个人」页，设置区里的入口只剩「缓存管理」四个字（应为「缓存管理（N 个视频 · X）」），得先访问过会初始化缓存索引的页面才补上
- **根因**：缓存索引是**懒加载**的——`DownloadManager.init()` 原先只由合集页 / 离线缓存页 / 播放页触发，而入口文案直接读 `DownloadManager.cached`；启动时没有任何人预热，索引自然还没进内存
- **修复**：`lib/main.dart` 启动流程加一次 `unawaited(preheatCacheIndex())`（即 `DownloadManager.instance.init()`）。选「启动预热一次」而不是「入口自己加载」的理由：索引只是一份几十 KB 的 JSON，读盘代价远小于让用户看到一次错误的空计数；而且入口的任何宿主都自动受益，不必各自记得 init
- 三条硬要求都落在 `DownloadManager.init()` 自身：**幂等**（内部 `_indexLoaded` 标记保证只读一次盘，各页面后续 `init()` 直接返回）、**失败静默**（文件不存在 / 索引损坏 / 目录读不到都在内部被 catch，视为空索引）、**不阻塞首帧**（调用方 `unawaited` 不 await，它自己异步落地）

**D. 已知限制 / 说明（诚实披露）**

- **合集嵌套只在模拟器验到「UI 呈现与导航」这一层，真机写操作没闭环**：模拟器上**没配 GitHub token / Gist ID**，所有写操作被配置门禁拦住（新建 / 移动 / 重命名 / 删除点下去不落库）。**级联算法由 51 例模型单测覆盖、落库内容由 widget 测试逐字段断言**，但真机上「点 → 写 Gist → 重进仍在」这条闭环**没有跑过**。测试期间**用户的 Gist 零写入、既有合集与视频一点没动**
- **仅音频缓存「人耳听到声音」没能验**：链路已实测通过（17:37 的视频只存下一个 `.audio.m4s`、索引 `videoPath:""` / `audioOnly:true`、断网后从离线页播放 → `c2.android.aac.decoder` 在解、进程内 `AudioTrack` 在跑、`state=PLAYING` / `error=null`、拖动进度正常），但**模拟器的音频后端本身写不到宿主**（`ranchu pcm_writei failed`，宿主侧没有可用音频输出设备）→ 证据是 **App 侧音频链路完好**，不是人耳确认
- **PC 端 `whitelist.py` 未适配路径式合集**：它把 `collection` 当**普通名字**读（`_ensure_collection` 按名去重、`rename` / `delete` 按名匹配），**遇到带 `/` 的路径会当一整个名字处理**；App 侧能读能显示，但 PC 端对子合集的级联行为**未验证**（本轮没改 Python 端）——**用 App 建了多层嵌套后，动 PC 端 `collection rename/delete` 前请先 `collection list` 看一眼**
- **不做转码压缩**（理由见 A3）：缓存下来的 `.m4s` 本身就是有损压缩流，再压只能省 0~3%；重新转码要引 ffmpeg 进缓存链路、耗时约半个视频时长、有损、失败还要清半成品。**真正的省空间手段就是 A1 的「仅缓存音频」**
- **不做缓存上限 / LRU 自动清理**：本轮只做「看得见 + 手动清」；上限策略涉及「删谁」的产品决策，另开
- **离线页的 `cid` 依赖索引新字段**：v2.28 及以前缓存的条目 `cid=0`，从离线页进播放页**仍可正常播放**（缓存命中不看 cid），但那条记录拿不到弹幕/字幕
- **多 P 的离线页播放不传 `pages`**：合成 pages 需要**未缓存分 P 的 cid**（拿不到），故离线页进入的播放页**不显示选集菜单**，请从离线页直接点要播的那一集

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **1809 例全绿**（v2.28.0 的 1723 → 缓存批次 **+36** → 合集嵌套 **+46** → P2 修复 **+4**；其中合集模型层单测 **51 例**逐条覆盖路径工具 / 新建 / 移动 / 重命名 / 删除 / 精确匹配 / 同级重排 / 旧数据零迁移）；Android 模拟器**截图取证 49 张**（`docs/screenshots/v2.29.0/`）——缓存批次：离线页、分项占用、仅音频菜单/确认/进度、**仅音频离线播放**与拖动进度、角标「已缓存音频」、入口概要、**整段缓存离线播放的回归**（视频轨 `c2.goldfish.h264.decoder` + 音频轨都在，`onPrepared 852x480`）；合集嵌套批次：首页只列顶层、三层下钻与面包屑、子合集卡片与左滑三入口、新建子合集、重命名、删除上提、移动确认框**新文案**、目标选择器层级缩进与**防环**、拖拽排序、合集页内播放、离线页入口与缓存角标冒烟

---

## v2.28.0 (2026-09-15)

**合集可以整个并入另一个合集了（源合集就此消失，视频不丢），搜索页新增「直播」范围能按关键词搜到直播间并直接站内播放；顺手修掉一个快速切搜索类别时旧结果覆盖新结果的真缺陷；v2.28.0**

**A. 合集可以移动到另一个合集（新功能，语义是「并入」）**

- **语义是并入，不是嵌套**：把**源合集的全部视频移动**到目标合集，**源合集的定义被删除**。本 App **不做二级合集**——嵌套会把「合集」从一个扁平标签变成一棵树，选择器、展示、导入导出全都要跟着改，收益不抵复杂度
- **数据层新增纯函数 `moveCollectionInto(data, source, target)`**（`lib/models/whitelist_video.dart`），与既有 `renameCollection` / `deleteCollection` **并列**，同样是纯函数、不碰 IO，便于单测
- **顺序会整段重排（有意为之）**：目标原有视频**保持相对顺序排在前**，并入的按**源合集内顺序接在末尾**，然后**整段重新编号 `0..n-1`**。不重排会出事：各合集的 `order` 是**可以互相重叠**的（重命名/删除合集的历史路径会留下重复值），直接沿用会让目标合集的排序**撞车**，展示退化成「加入时间倒序」——顺序会莫名其妙变掉
- **定位口径与既有代码一致**：重编号表的 key 用 `(bvid, cid)`，跟合集页判断「同一视频」的口径相同
- **并入自己 = 未改动直接返回**（`src == dst` 提前 return），源或目标不存在则抛 `CollectionException` 并带上现有合集列表，便于定位
- **UI 入口在合集管理面板**：每行新增「移动到其他合集」→ **目标选择器只列除自己以外的合集，不提供「未分类」**（与「删除合集」的语义重复：删掉合集后视频本来就落回未分类，再给一个「移动到未分类」是同一件事的两个按钮）→ **确认框写清后果**：「把「A」里的 N 个视频移到「B」，并删除「A」这个合集（视频本身不会被删除）」，副标题「会同步到 Gist，无法在 App 内撤销」
- **没有其它合集可选时不弹空列表**：给一句「没有其它合集可以并入，请先新建一个合集」的提示就返回
- **合集卡片的左滑菜单没有加这一项（说明一下）**：左滑已经是**重命名 / 删除**两项，再加第三项在 **360dp 宽的机器上会把卡片内容挤掉大半**；「移动」是**低频操作**，放管理面板比放左滑菜单更合适

**B. 搜索页新增「直播」范围（新功能）**

- **必须用 `search_type=live_room`**：这是**实测探针结论**——`search_type=live`（不带 `_room` 后缀）返回的 `data.result` **不是数组**，代码走解析会**静默无结果**（不报错、只是空列表，属于最难查的那类失败）
- **结果卡片** = 封面 + 标题 + 「**主播名 · N 在线**」，点击**直接进站内直播播放页**（复用 v2.27.0 做的 `LivePlayerPage`）
- **只加载第 1 页、不做无限翻页**（**有意为之**：翻页等于把搜索变成一个可无限下潜的内容池，与本 App「只看事先选好的内容」的定位冲突）
- **产品边界同步更新**：代码里原先的注释写着「搜索页不加直播入口」（v2.26.0 的产品决策），现已改写。直播入口全 App **只有两处**——白名单 UP 主的「**正在直播**」标记、搜索页「**直播**」范围的关键词搜索结果；**两处都是「用户先表达意图」才拿到的**。**热门直播 / 推荐直播流仍然不做**（那才是没有边界的内容池）
- **新增** `lib/models/live_search_result.dart`

**C. 切换搜索类别直接搜索（改进）+ 修掉一个真缺陷**

- **切 Tab 自动搜**：切到「全部 B 站」/「搜索 UP 主」这两个 Tab 且**关键词非空**时**自动发起搜索**（此前必须再点一下搜索键）；「我的白名单」Tab 是**本地过滤**，**绝不发请求**——这里发请求是纯浪费并且会污染历史
- **不重复打接口**：同一个「Tab + 关键词」组合**不重复请求**，来回切 Tab 不会反复打接口；**切 Tab 触发的搜索不记入搜索历史**（用户没主动搜，不该进历史）；**换关键词后再切**会按新词补搜一次
- **修掉一个真缺陷（本轮最有价值的部分）**：三个搜索分支此前**没有代次守卫**——快速切换类别时，**先发出的请求后返回，会覆盖掉新结果**，列表显示成**上一个关键词**的内容。现加了**自增代号**，**过期响应一律丢弃**
- **新增** `test/search_live_api_test.dart`、`test/search_tab_switch_test.dart`

**D. 已知限制 / 说明（诚实披露）**

- **本批未在真机 / 模拟器验证**：改动全在 **Dart 层**，由 **widget 测试与 mock 网络**覆盖。**没有跑过真机或模拟器**
- **合集管理面板一行现在有 3 个图标按钮（共 144dp）**，**窄屏（360dp）下标题可用宽度约 170dp**，**拥挤度未在真机确认**（若实际观感过挤，后续可能要把「移动」收进溢出菜单）
- **直播搜索的 `-412`（风控）路径只用 mock 覆盖**，**未真实触发过**；**直播结果的封面防盗链未实测**
- 直播搜索**只加载第 1 页**（有意为之，见上）；**不做直播搜索历史**

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **1723 例全绿**（本批 **+35**：合集并入 **9** 例 + 管理面板端到端 **3** 例 + 直播搜索接口 **10** 例 + 切 Tab 自动搜 / 去重 / 代次守卫 **13** 例）

---

## v2.27.1 (2026-09-14)

**播放页信息块补上发布时间——就接在 UP 那一行的时长左边、同行显示、零额外高度；并修掉两处漏传 `pubdate` 的入口，从「历史记录」「每日历史」点进播放页一直看不到发布日期；v2.27.1**

**A. 播放页信息块补上发布时间**

- **位置**：信息块的 **meta 行右端**（UP 徽章那一行），文案「**发布 yyyy-MM-dd · 时长**」——**与时长写在同一个 `Text` 里**，所以**零额外高度**：横屏置顶模式屏高低，信息块每多一行都要从评论区抢地方
- **词序对齐既有惯例**：`history_tile` 用「发布 yyyy-MM-dd」，视频卡元信息行用 `·` 串联（`video_tile` 的「时长 · UP主 · 发布日期」、`upowner_page` 的「时长 · 发布日期」）——这里就是两套写法的合体，日期走**与时长同一套等宽数字语汇**（`kTypeNum` + `kInkGray50`）
- **旧数据 / 未知值不显示，且不残留分隔符**：`formatPubdate` 对 `null` / 脏值返回空串（`pubdate` 是 v2.16.16+ 导入与油猴写入才带，更早的记录是 `null`），此时**整段退化成纯时长**——界面上**连 `·` 都不会出现**，与改动前**逐字符一致**
- **无简介时照样显示**：发布日期**不依赖简介存在**（简介区 `desc` 为空时整块不构建，与本段无关）
- **横屏紧凑形态不挤坏**：信息行空间不足时，让 **UP 名（`Flexible` + 省略号）先让位**，日期与时长完整可见、整行不溢出；已用模拟器横屏置顶截图取证

**B. 修两处漏传 `pubdate` 的入口（同一个需求的尾巴）**

- `HistoryPage._openEntry` 与 `DailyHistoryPageState._openEntry` 构造 `WhitelistVideo` 时**漏传 `pubdate`** → 从**「历史记录」**和**「每日历史」**点进播放页**看不到发布日期**（`HistoryEntry` 本身存了 `pubdate`、**历史卡片也一直在显示它**，只有「进播放页」这一段把它丢了）。现补 `pubdate: e.pubdate`
- 顺带**全仓核过 12 个 `WhitelistVideo(` 构造点**：只有这两处真漏，其余都有各自来源或已带该字段

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **1688 例全绿**（本版 **+4**：新增用例覆盖「pubdate 非空 → 与时长同行显示」「空值 / 脏值 → 不显示且不残留 `·`」「无简介时照样显示」「横屏紧凑形态下日期时长都在、由 UP 名让位省略」）；Android 模拟器**截图取证三张**（竖屏 / 横屏置顶 / **从历史入口进播放页**），信息块均渲染出「**发布 yyyy-MM-dd · 时长**」的同行文案

---

## v2.27.0 (2026-09-14)

**App 内直接看直播了（白名单 UP 主页「正在直播」标记 / 信箱顶卡角标 → 站内直播播放页），并给直播接上弹幕（零新增依赖）；修掉两个只有真机实测才暴露的缺陷——认证回复被丢导致弹幕两分钟后消失、首帧闪「播放失败」；v2.27.0**

**A. App 内播放直播（新功能）**

- **入口（只有两个）**：点白名单 UP 主页的「正在直播」标记、或信箱顶卡角标，**在 App 内直接看直播**（v2.26.0 是跳站外浏览器）；UP 主页**长按**该标记**仍保留**「用 B 站打开」
- **取流**：`api.live.bilibili.com` 的 `xlive/web-room/v2/index/getRoomPlayInfo`（**匿名可用**，不需登录态）；地址拼法 `url_info.host + codec.base_url + extra`——**`base_url` 在 `codec` 层而不在 `url_info` 层，这是最容易写错的一处**；流选择优先 `http_hls` / `fmp4` / `avc`，`url_info` 长度有 host 兜底
- **地址时效**：HLS 地址约 **58 分钟**过期，故**进页现取**、**播放中自动续期**（剩余 < 10 分钟时重取，夹在 1~45 分钟区间）
- **原生**：`android/app/build.gradle.kts` 新增 `androidx.media3:media3-exoplayer-hls:1.5.1`（与既有 media3 三件套严格同版本）；`DashExoPlayer` 新增 **`isLive` 分支**走 `HlsMediaSource`，**VOD 路径未改动**
- **直播下彻底关掉 seek**：从源头**不设 seek 增量**、**不上报 seek 事件**；媒体通知与媒体会话**去掉快退/快进以及所有 seek 命令**（锁屏 / 系统媒体卡片的进度条也拖不动）
- **播放页 UI**：**没有**进度条 / 倍速 / 选集 / 前后三秒 / 下载 / 字幕 / 评论入口；保留全屏、亮度与音量**上下滑**手势，**左右滑什么都不做**（直播不可 seek，给了也没有意义）；下播显示「直播已结束」+「重新连接」；播放失败自动重取地址自愈（退避 1/2/4s，最多 3 次）
- **统计口径**：直播**不写历史、不写播放进度**（直播没有 bvid，写进去会污染历史与统计），但**观看时长计入观看统计**

**B. 直播弹幕（新功能，零新增依赖）**

- **零新增依赖**：复用 VOD 那套渲染与设置——`DanmakuOverlay` + `DanmakuSettingsStore` + `DanmakuSettingsSheet`（开关 / 透明度 / 显示区域 / 屏蔽词），**设置持久化，直播与点播共用同一份**
- **取服务器信息要 WBI 签名**：`xlive/web-room/v1/index/getDanmuInfo` **必须签名**（`wts` + `w_rid` + `web_location`），不签一律返回 `-352`；复用现有 `WbiSigner`，不引新依赖
- **WebSocket**：`wss://<host>:<wss_port>/sub`，认证 `protover=2` + token；**认证成功后立刻发第一次心跳**，之后每 **20 秒**一次（实测教训：等满 30 秒才发第一次心跳，服务端会让数据流**静默**，不报错但收不到弹幕）
- **拆包与解压**：16 字节大端头拆包；`protover=2` 用 **zlib（带头，不是 raw deflate）**解压；支持**一帧多包**与**嵌套包**（递归）；**`protover=0` 的明文 JSON 包也处理**（实测会收到 LOG_IN_NOTICE 一类）；host 兜底；断线退避重连并**重取 token**
- **渲染**：弹幕列表**原地追加**（不换 List 实例、不清屏），避免闪屏与丢帧
- **失败静默降级**：弹幕链路任何失败都**不影响播放**，最多是没弹幕

**C. 修掉两个真机实测出来的缺陷（本轮最有价值的部分）**

1. **认证回复被丢 → 弹幕两分钟后消失**：`op=8` 认证回复的包头 `protoVer` 真机实测是 **1**，而解析器要求 `== 0`，整包被**静默丢弃** → 状态永远停在「连接中…」、**心跳从未发出** → 服务端 60~130 秒后断开 → 无限退避重连，5 次后弹幕失效。现放宽为**容忍 0/1**。
   **更值得记的是根因能漏到线上**：测试替身**自己臆造了 `protoVer=0` 的报文**，恰好绕过守卫造成**假绿**——替身已改成与真机实测一致的报文，并补了「**修复前必红**」的用例。
2. **首帧闪「播放失败」**：ExoPlayer 的 `BehindLiveWindowException`（错误码 **1002**）此前走普通错误路径，会先弹一次错误卡片再自愈。现改为**不退避、立即重取地址**的静默自愈（界面显示「正在重新连接…」），但仍受「**3 次 + 退避**」预算约束——连续失败最终仍会落到错误态。

**D. 已知限制 / 说明（诚实披露）**

- **入口只有两个，且都需要该 UP 在白名单里、且此刻正在直播**：UP 主页的「正在直播」标记、信箱顶卡角标。**搜索页没有直播入口**（有意为之：本 App 定位是「只看事先选好的内容」，直播是**不可预选、无边界的信息流**，开站内入口会破坏定位）
- 直播**不做清晰度切换**、**不做直播间评论**、**不做 App 内关注/互动**
- 直播**不写入历史与断点续播**（有意为之，同上）
- 已登录态下**弹幕用户名是否不打码未验证**（匿名实测用户名会被打码成 `远***`）
- **验证环境是 Android 模拟器，没有真机**；模拟器软解 HLS 会掉帧，**实际观感需真机确认**。`code=1002` 是**概率性事件**（允许出现，只要求不再闪错误卡片）
- 直播弹幕的**高密度刷屏边界未覆盖**（实测房 5 分钟约 82 条，属低频）

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **1684 例全绿**；Android 模拟器实测——直播**能出画面与弹幕**（开/关对照证明弹幕由本 App 渲染）、**心跳实测 20.00 秒一次稳定**、**约 5 分钟窗口内弹幕 WS 零断开零重连**、媒体通知**直播下无 seek 而点播下仍有 seek**、弹幕设置**即时生效并能持久化**、直播**不写历史**；**VOD 无回归**（连播 3 个视频正常、进度条可拖、断点续播与历史正常——上一轮曾报的 `4003` 音频错误经同视频重播**零复现**，判定为**环境偶发而非回归**）

---

## v2.26.0 (2026-09-13)

**专栏也有评论区了，白名单 UP 主会亮「正在直播」标记；修掉一个让直播标记永不显示的 P0 host 缺陷（接口请求打到错误域名返回 404 后被静默吞掉）；v2.26.0**

**A. 专栏阅读页接入评论区（评论区当唯一滚动者）**

- **接口层泛化**：`fetchVideoComments` / `fetchReplyChildren` 各加 `int type = 1` 形参（形参名仍叫 `aid`，语义是 reply 的 `oid`），把原来写死的 `'type': '1'` 参数化 → 视频传 `type=1` + `aid=<avid>`，专栏传 `type=12` + `oid=<cvid>`
- **限流退避**：新增私有 `_getReplyApi` 给 reply 系列请求补上 `-509`（请求过于频繁）退避 1200ms；`_throwReplyError` 增 `-509` 分支给出可读错误
- **`lib/widgets/comment_list.dart` 通用化**：新增可选参数 `oid` / `commentType` / `identityKey` / `header` / `footerSeed` / `initialTotal` / `physics` / `api`；`video` 由 `required` 改**可空**（老调用点行为不变）
- **`lib/pages/article_page.dart` 用 header 方案**：**专栏正文整块作为评论区 `CommentListView` 的 `header`**，让评论区当**唯一滚动者**——正文与评论处在同一个滚动体内，保证「滚动到底自动加载下一页」真的有效（若做成正文在外层、评论内层两个滚动体，翻页永远触发不到）
- **实测结论**：正文与评论同一滚动体（拖动评论时作者行与「评论 270」同步位移）、楼中楼展开正常、点头像进个人页正常、下拉刷新正常、`-509` 退避在真机上真实触发并成功恢复
- **已知限制**：匿名会话下 B 站只返回热门评论且首屏即 `is_end=true`，**评论翻页无法在匿名态验证**——这是 **API 上限，不是代码缺陷**（登录态未测）

**B. 白名单 UP 主「正在直播」标记（最小形态）**

- **接口**：新增 `fetchLiveStatusByMid(mid)`（`lib/api/bilibili_api.dart`），请求 `https://api.live.bilibili.com/room/v1/Room/getRoomInfoOld`
- **节流中枢**：新增 `lib/models/live_status.dart`（`LiveStatus` + `LiveStatusHub`）——**单条串行队列**保证相邻请求间隔 **≥1.5s**，**同 mid 在途请求共享**避免并发打同一个 UP，**会话内缓存**避免来回切页反复请求，**失败静默、不写缓存**（下次仍可重试），UI 永不因网络问题报错
- **UI**：新增 `LiveNowBadge`（点缀墨系圆点 + accent 描边，**无阴影**，与 v2.18.0 起的 block 设计语言一致）——UP 主页插在「粉丝数」下方，信箱顶卡左上角叠一个
- **语义**：`liveStatus == 2`（轮播）**不算在播**——轮播没有直播流，标了会误导
- **P0 缺陷修复（本轮重点）**：`fetchLiveStatusByMid` 原先打在 `_dio` 的 baseUrl `https://api.bilibili.com` 上 → **HTTP 404**，而异常被 `catch` 静默吞掉 → **标记永不显示且不报错**。现改用**绝对 URL**，新增常量 `kLiveApi = 'https://api.live.bilibili.com'`（`lib/config.dart`）
- **防回归**：`test/live_status_test.dart` 新增一条**断言 host** 的用例（**能证伪**：改回旧 host 必红）；mock adapter 同步做了最小改动——绝对 URL 下 dio 的 `RequestOptions.path` 是**完整 URL**，只按 path 匹配会让用例**假绿**；另外订正了四处把接口写成 `x/room/v1/...` 的**过时注释**
- **已知限制（产品决策，有意为之）**：**只做「标记 + 点击跳站外浏览器」**，**不做 App 内播放直播、不在搜索页加直播入口**——本 App 的定位是「只看**事先选好**的内容」，直播是**不可预选、无边界的信息流**，给它站内入口会破坏防成瘾的定位
- **未验边界**：模拟器没装 B 站 App，站外跳转只验到「拉起浏览器」这一步；轮播分支（`liveStatus==2`）未实测；信箱顶卡角标因当时该 UP 无直播间而未目击

- 测试 / 验证：`flutter analyze` **0 issue**、全量 `flutter test` **1599 例全绿**；Android 模拟器实测——在播 UP（`白夕Seal` mid=1909499809）**UP 主页标记正常渲染**，logcat `[bili_api] fetchLiveStatusByMid mid=1909499809 → room=23336970 live=1 title="杀戮尖塔2—豹豹今天豹毙了吗？"`（**无 `失败:` 行**，即非 404 静默失败），**点击标记拉起 Chrome** 打开 `https://live.bilibili.com/23336970`；负向对照 `呼唤少女` mid=271924781 未在播 → 无标记，curl 交叉验证一致；**专栏评论区无回归**（同一滚动体、楼中楼、头像跳转、下拉刷新均正常）

---

## v2.25.1 (2026-09-13)

**安全与文档修正（无功能变化）：油猴脚本里两处明文真实 Gist ID 改为占位符语义、订正 `AndroidManifest.xml` 里关于通知权限的过时注释；全仓库已跟踪文件复核后仅剩 1 处真实 Gist ID（`app/lib/config.dart`，功能必需、有意保留）；v2.25.1**

**A. 脱敏：`amotv.user.js` 两处明文 Gist ID 改为占位符语义**

- **头部注释里的示例**（原来直接写「用户 Tukist 的 secret gist：<真实 ID>」）→ 改为**说明性语义**：「从自己的 gist 页面 URL 末尾复制，形如 `https://gist.github.com/<用户名>/<GIST_ID>`」
- **配置面板的 `placeholder`**（原来直接拿真实 ID 举例）→ 改为「Gist ID（32 位十六进制，见自己 gist 页面 URL 末尾）」
- **不写假 ID**：占位符里**不留任何看起来像真 ID 的示例值**，避免用户误把它当成默认值照抄
- **脚本功能不受影响**：脚本运行时 `gist_id` **只从 Tampermonkey 本地存储读**，**不内置任何默认值**；`placeholder` 只在输入框为空时作灰字提示，改文案**不改变任何行为**

**B. 注释订正：`AndroidManifest.xml` 里 `POST_NOTIFICATIONS` 的说明与实现对齐**

- 原注释声称「绑定 MediaSession 的媒体通知**本身豁免**该权限……即使被拒绝，媒体通知照常显示」——**与 v2.25.0-r2 的实际实现正好相反**，会误导后来人（以为该权限可有可无）→ 改为如实说明：
  - v2.25.0-r2 起本通知**故意不绑** MediaSession token（绑了会被系统媒体卡片接管，App 自己的四个按钮一个都不渲染）
  - 因此它按**普通通知**处理，**不享受**「绑定 MediaSession 的媒体通知豁免该权限」那条规则 → 这条权限是**必需**的
  - 用户在系统设置里关掉本 App 的通知后**通知就不显示**（**播放本身不受影响**）

**C. 安全排查结论（全仓库已跟踪文件）**

- `git ls-files` 全量扫真实 Gist ID 的片段 → 命中**仅 1 处**：`app/lib/config.dart` 的 `AppConfig.gistUrl` 默认值
- **这一处是有意保留的**：它是 App **免配置**读白名单的**唯一内置源**（`lanUrl` 默认为空、本地导入需手动放文件），**删掉等于功能不可用**
- **未跟踪**的 `sync_config.json`、`whitelist.json`、`cookie*.txt`、`whitelist.json.gistbak*`、`app-release.keystore` / `key.properties` 均已被 `.gitignore` 覆盖（`git status --ignored` + `git check-ignore -v` 双证）；`.gitignore` 既有规则**未改动**
- **无单文件 >50MB**

- 测试 / 验证：`flutter analyze` **0 issue**、`node --check amotv.user.js` **通过**（本轮未动 `lib/`，故未跑全量测试）

---

## v2.25.0 (2026-09-13)

**UP 主页支持左右滑动切换分区（横滑切区 / 纵滑滚列表、chips 双向同步、每区独立状态与滚动位置）；接入 Android 媒体通知 + 耳机媒体键控制（封面缩略图、视频标题、`UP名 · 状态`，快退15s / 播放暂停 / 快进15s / 关闭）；并修掉快退语义、暂停态 seek 错报播放、通知封面 cleartext 被拦、重播 tick 停滞等问题；v2.25.0**

**A. UP 主页支持左右滑动切换分区**

- **UP 主页内容区改成 `PageView`**：**横滑切分区**（全部视频 / 动态 / 专栏 / 各合集·列表），**纵滑仍然滚列表** —— 由 Flutter 按**首次移动方向**自行裁决，**没有写任何自定义手势代码**（不抢纵向滚动，无方向判定偏差）
- **顶部 chips 双向同步**：滑动切页后 chips 选中态跟着变；点 chips 也能切；**第一个 / 最后一个分区继续滑不出界**（`PageView` 默认边界行为）
- **每个分区独立状态**：新增 `_CollectionView`，**各持自己的 videos / page / loading / error / ledger** —— 切回**不重拉**、翻页进度不丢
- **滚动位置按分区保留**：先试了 `PageStorageKey` 但**实测踩坑**（页内 `ExpandableText` 里还有别的滚动容器，框架按上下文算 key/slot 会算成同一个、把外层位置覆盖成 0）→ 改成**自记账**（`_sectionOffsets` + `_SectionScrollHost`，**重建即从上次位置起步**）
- **chips 顺序未变**（「全部视频 / 动态 / 专栏 / 合集…」），因此从「全部视频」左滑到的是「动态」

**B. Android 媒体通知 + 耳机按键控制**（用户要求「和 B 站一样、能用耳机控制暂停继续」）

- **原生新增 `DashMediaNotification.kt`**：media3 `MediaSession` + 通知，展示**封面缩略图 + 视频标题 + `UP名 · 状态`**，四个动作：**快退15s / 播放暂停 / 快进15s / 关闭**
- **Dart 侧新增通道** `updateNowPlaying(...)`（同步标题 / UP / 封面 / 播放态 / 进度）与 `onMediaAction` 事件（原生 → 界面）；**通知操作与界面状态双向一致**（不会出现「通知暂停了界面还在播」）
- **耳机 / 蓝牙媒体键**走 MediaSession：`KEYCODE_MEDIA_PLAY_PAUSE` → 系统派发到活跃会话 → 播放器 play/pause（链路仅在代码级确认，**真机耳机需用户实测**）

**C. 本轮实测发现并修掉的问题**

- **快退 / 快进语义**：系统媒体卡片的「上一曲」会被映射成 `seekToPrevious`（对单集而言 = **回开头**）→ 改为**显式算目标位置并夹到 `[0, duration]`**，实测**每次恰好 ±15000ms**
- **暂停态 seek 后界面错报「正在播放」**：`_onPrepared` **无条件**置 `_playing = true`，而它**每次进 READY 都会触发** → 改为按原生 `playWhenReady` 决定（同时修正「只有 `playWhenReady` 才清 `_completed`」）
- **通知封面不显示**：部分视频 cover 是 `http://`，被 **cleartext 策略拦截**（logcat 有原文）→ **原生与 Dart 两处**都把 URL 归一化到 `https`
- **✕ 之后 tick 空转 + 两处竞态**：停止后仍轮询；且在途 tick / 原生 stop 到事件到达的 **~150ms 窗口**里 `getPosition()` 恒报 0，会把界面位置冲成 0:00 → 加**双守卫**（停止态早退、播放中位置不可能倒退到 0）
- **顺带修掉一个真 bug**：`_init()` 原来**不取消旧订阅、不 dispose 旧播放器** → 「✕ 后再播」/「评论区链接换视频」时事件被**双份处理**、且旧播放器一直出声
- **修掉一个既有 bug**（上一轮发现）：`_onCompleted` 只 `cancel` 不置 null，而 `_init` 用 `_timer ??=` → **播完再播时进度条 / 字幕不再刷新**；改为新增幂等 `_ensureTickTimer()`（用 `Timer.isActive` 判定，能**自愈重建**且**不会叠加双计时器**）

**D. 一处必须说明的平台取舍**

- **实测 + 字节码双证：Android 13+ 的系统媒体卡片不允许 App 自定义按钮** —— 它只渲染自己的固定槽位（上 / 播放暂停 / 下），media3 1.5.1 也**不把 `setCustomLayout` 导出到 `PlaybackStateCompat`**
- 因此**让用户看到的那张卡片改为 App 自己的通知**（四个动作齐全、语义精确），**MediaSession 保留**给耳机 / 蓝牙媒体键与 MediaController 用
- **代价**：该通知**不再出现在系统锁屏的媒体控制区**，也不再豁免 Android 13+ 的 `POST_NOTIFICATIONS`（会照常请求授权；**被拒时通知不显示、播放不受影响**）
- 收起态一屏最多 **3 个 action**（Android 对普通通知的硬限制），第 4 个（关闭）需展开

- 测试：`flutter analyze` **0 issue**；全量 `flutter test` **1571 例全绿**（本轮由 **1562 → 1571**，净增 **9 例**）；`:app:compileDebugKotlin` **BUILD SUCCESSFUL**；关键回归做了**红 / 绿双向验证**（去掉修复必红）
- 验证：**模拟器实测** —— UP 主页左右滑切分区 / chips 同步 / 纵滑不误切 / 切回保留数据与滚动位置；通知**展开态可见封面 + 四个按钮**、**快退 / 快进实测每次恰好 15000ms**、关闭后**通知消失 + `state=NONE(0)`** + 界面停止态且位置保留、**暂停态 seek 后仍暂停**、封面 `largeIcon=BITMAP 125x70`
- ⚠ 未验证项（如实）：**耳机按键在模拟器上无法验证**（无耳机；已用 `cmd media_session dispatch play-pause/rewind/fast-forward` 验证**等价链路**正确、`dumpsys media_session` 显示 Media button session 仍是本 App）→ **需真机确认**

---

## v2.24.0 (2026-09-12)

**修复专栏正文渲染（用户反馈「图片没法正常显示、界面直接把原始 JSON 显示出来了」）：新增 Quill Delta 格式解析并自动判别两种正文形态，图片 / 标题 / 列表 / 引用块全部正常显示，界面不再泄漏原始 JSON；v2.24.0**

**A. 根因：B 站专栏正文有 4 种形态，此前只认 HTML**

- **实测确认**：专栏正文形态由服务端 `data.type` 决定 —— **`0` / `2` = HTML**、**`3` = Quill Delta JSON**、**`4` = 纯文本**。实测 **11 篇专栏**，`type → 形态` **零反例**
- 此前解析器只认 HTML，遇到 **`type=3`** 就把整段 `{"ops":[...]}` 当**纯文本**画到屏幕上 → 图片（在 `insert.native-image` 里）**自然出不来**
- 这就是用户看到的「原始 JSON + 没有图片」

**B. 修法：新增 Delta 解析 + 自动判别，渲染链路完全复用**

- 新增 **Quill Delta 解析**，并**自动判别**两种格式：
  - 判别依据：`content` 去前导空白后**是否以 `{` 开头**且**开头 96 字符内含 `"ops"`**
  - **判别失误也安全**：`jsonDecode` 失败会**退回 HTML 分支**
- 解析产物是**同一棵节点树**，**渲染完全复用既有链路** → 图片 / 引用块 / 列表 / 链接 / 富文本样式**两种格式一致**，**不可能风格漂移**
- **Delta 支持**：`insert` 文本（保留 `\n` 为段落分隔）、`insert.native-image`（图片，带 `width/height` 走 `AspectRatio` 防跳动）、`attributes.link`（链接）、`bold` / `italic` / `strike` / `underline`
- **关键契约**：Quill 的**块级属性（`header` / `list` / `blockquote`）挂在行尾那个 `\n` 上**，解析时要把它**带到段落上**（否则标题 / 列表全丢）；**连续列表项并入同一个列表**（避免序号重数）

**C. 未知形态优雅降级（绝不泄漏 JSON、绝不崩）**

- `video-card` / `article-card` / `vote-card` / `live-card` 这类**整条跳过** —— 实测它们的 `url` 是**卡片图片、不是可点目标**，渲染出来只会误导
- 其它未知键也**跳过**；**绝不把原始 JSON 打到界面上**、绝不崩

**D. 顺手澄清一个易误判点（最终不改后缀）**

- 图片 URL 的 **`@progressive.webp` 后缀可以直连**（实测 200 `image/webp`）
- 用户截图里那张 404 是**图本身已被 B 站图床删除**（老路径 `bfs/article/<32hex>.jpg`），**不是后缀问题**
- → 最终策略：**URL 原样使用、不做后缀改写**

**E. 核查过但不需要改**

- 专栏**列表卡**的数据接口**根本不返回正文**（无 `content` / `ops`），且 Delta 型专栏的 `image_urls` **照常填充** → **封面提取无需改**
- 动态侧**只取纯文本字段**，不存在露 JSON 的风险

- 测试：`flutter analyze` **0 issue**；全量 `flutter test` **1544 例全绿**（本轮由 **1512 → 1544**，新增 **32 例**）；核心回归断言是**双向的** —— 既**抓渲染后所有 `Text`（含 `Text.rich` 的 span）拼成整屏文本**、断言 `insert` / `ops` / `native-image` / `attributes` / `progressive` / `loaded` 等字样**全部不出现**，也断言图片、`AspectRatio`、列表「·」、引用块、分割线**确实渲染出来了**（避免「整页空白」也算过）
- 验证：**模拟器实测** —— **Delta 型**（cv41358718）：**3 张图片全部显示**（原生裁剪确认真位图）、全篇逐屏目视 **JSON 零泄漏**（10 种字样 0 命中）、**4 个 h2 + 23 条 bullet + 引用块**全对、滚到底无错乱；**HTML 型**（cv17059775）：**无回归**（真实照片、列表、加粗、外链正常）；另验**纯文本型**（type=4）正常、**专栏列表卡**正常。已知环境限制：模拟器上专栏接口**频繁 `-509` 限频**（App 会退避 1.2s 重试，错误态文案正确、未把原始响应打到界面）

---

## v2.23.0 (2026-09-12)

**全面补齐视频卡片的发布日期（穷举 App 内 11 处「封面 + 标题」视频卡，把原本缺日期的 3 处补上）；修正 `arc/search` 未解析 `created` 的数据源缺口；长标题超行时可点击「展开」；v2.23.0**

**A. 全面补视频卡片的发布日期（穷举 11 处，补 3 处）**

- 穷举 App 内全部 **11 处**「封面 + 标题」视频卡片，把原本**缺日期**的 **3 处**补上：
  - ① **搜索页「我的白名单」Tab 内联 tile**（补 `· yyyy-MM-dd`）
  - ② **UP 主页「全部视频 / 合集·列表」**（同上）
  - ③ **动态里的视频投稿块**（补 `视频投稿 · 3 天前`，用动态自身的发布时间兜底）
- **历史卡（`HistoryTile`）**也补了 `发布 yyyy-MM-dd`
- **修正数据源缺口**：`arc/search`（UP 主页视频列表）此前**只把 `created` 当作 addedAt、没写 `pubdate`**，所以那处即便加日期也是空的 → 在 `lib/api/bilibili_api.dart` 补解析 `created` 为 `pubdate`
- **统一日期格式**：全部用既有的绝对日期 `WhitelistVideo.formatPubdate`（`2024-05-09`）；删掉了搜索页里一份与它输出逐字符相同的私有 `_fmtPubDate`，避免两份日期格式化漂移；**唯一例外**是动态视频投稿用**相对时间**（与同卡作者行同一套语汇）
- **番剧 / 影视季卡不补**：该接口唯一的时间信息是 `index_show`（电影 `2010-12-16上映`、番剧 `全14话`），已在副标题显示；逐条补发布日期就得为每个季再拉一次 `fetchPgcSeason`，不值（已在代码注释说明）

**B. 长标题可点击展开**

- 给上述视频卡标题 + **播放页信息行标题**接上 `ExpandableText` 的「**超行才出现『展开 / 收起』**」入口（复用既有组件，新增一个 opt-in 的 `animated` 开关，既有调用方逐帧不变）
- **方案选择**：用**显式「展开」小按钮**而不是「点标题展开」——卡片整行点击语义是「进播放页」，把标题点击改成展开会在用户想进视频时**静默地只展开标题**（且截断与否事先看不见）；现在只有**被截断时**才出现入口，点正文仍穿透给卡片，触摸目标 **≥ 48dp**
- **动效**：展开 / 收起走 `kDurBase` + `kCurveOut`；`MotionControl` 关动效时**根本不套** `AnimatedSize`（展开瞬时到位、一个 controller 都不建，与播放页信息块收起的做法一致）
- `HistoryEntry` 新增**可选**字段 `pubdate`（JSON 里**非空才落键** → 旧记录形态逐字不变、向后兼容，旧记录读回为 null 只是不显示该段）

- 测试：`flutter analyze` **0 issue**；全量 `flutter test` **1512 例全绿**（本轮由 **1495 → 1512**）；新增 **17 例**覆盖「每处补的日期可见」「长标题出现展开入口 / 短标题没有」「展开后点正文仍进播放页」「关动效瞬时到位」「history/pubdate 落盘读回」「缺失字段时副信息行与改动前逐字符一致」等
- 验证：通过**穷举清单逐处核对**（**11 处**全部有日期或已说明为何不补）；既有**硬锚点**（`'1:30 · UP主 · yyyy-MM-dd'`、`'1:30 · UP主'` 等）**一字未改**全部原样通过

---

## v2.22.0 (2026-09-11)

**等待 / 空态动画整体重做（加载态改「印刷走纸」、空态运动元素 5 → 1，并彻底去掉高斯模糊）；评论区头像可点进该评论作者的个人页；个人页新增「动态」与「专栏」两个分区（专栏正文自写轻量 HTML 渲染，零新依赖）；评论区滚动方向控制信息块显隐**

**A. 等待 / 空态动画重做（用户反馈「很丑」，上网取经后重做）**

- **加载态换主题**：从「风衣人剪影 + 3 条烟缕（带 `MaskFilter.blur`）」换成 **「印刷走纸」** —— **5 条 1px 横线**从左到右被墨**逐条**填出（**宽度参差**、前沿 **18px 渐隐**、**全程无任何模糊**），印满后**停一息**、整片淡到 **12%** 再收走重来；周期 **2.8s**；一颗 **1.2×6px 的点缀墨短竖条**（笔尖 / 刻刀）领着正在画的那条线
- **节奏**：单条 **504ms**、错峰 **420ms** → **任意时刻至多 2 条在长**（此前 3 条，观感像「条形图」）；5 条在 **t=0.78 全部收笔**、**0.80 之后才淡出**（「先印满再收走」）
- **空态换主题**：从「3 条随机游走线 + 骑线点 + 网点（**5 个运动元素**）」换成 **「一页还没写上字的纸」** —— **1 条 1px 横线** + **一片与线咬合的半调网点**（网点框**左缘 = 线渐隐起点**、**右缘 = 内容盒右缘**，两块墨交叠）+ **一颗点缀墨圆点**钉在线**收笔点**；运动元素 **5 → 1**（只剩网点的整体墨量**呼吸 ±20%**、周期 **4.4s**）
- **取经依据**：Vercel Geist（占位形状要**镜像内容轮廓** / **不要拿 skeleton 当 empty state**）、Carbon（空态插画是**可选装饰**）、NN/g（**无收益的连续动画只会抢注意力**）、Tailwind（**呼吸 2s** 才是「刻意节奏」的尺度，**14s 太慢、读不出刻意**）
- **性能**：加载态每帧绘制调用 **~30 → 21**，**去掉 3 次/帧的 `MaskFilter.blur`**，Path 对象 **3 → 0**

**B. 播放页：评论区滚动方向控制信息块显隐**

- **竖屏非全屏下**，**评论区向下滚 → 收起视频标题 / 简介块**（评论区顺势占满多出的空间）、**向上滚 → 恢复**、**回到列表顶部强制展开**
- **判定逻辑**：**累积位移 + 12px 阈值**（防抖 + **极性迟滞**）；**程序化滚动**（点控制层「评论」按钮）**不触发收起**
- **收起 / 展开为 200ms 平滑动画**；`MotionControl` 关动效时**瞬时到位且不创建 controller**（`AnimatedSize` 配 `Duration.zero` 会触发 Flutter 的 layout 断言，已避开）
- **全屏与横屏置顶模式行为不变**

**C. 评论区头像可点进个人页**

- 评论区**三处头像**（根评论 / 内嵌预览 / 楼中楼）**都能点**，**点头像进的是该评论作者**（不是视频 UP）；热区撑到 **48dp**（视觉尺寸不变）
- 评论模型补 **`mid` 解析**（接口给的是**字符串**，统一走**防御性解析**；**`mid<=0` 时不跳转**且头像**不挂手势**）
- 点进后个人页头部用**评论里的名字 / 头像预填**（秒开）

**D. 个人页新增「动态」与「专栏」分区**

- chips 行（原本只有「全部视频 + 合集 / 列表」）新增 **「动态」** 与 **「专栏」** 两个固定项；三个内容源**互斥、各自懒加载**
- **动态**：`x/polymer/web-dynamic/v1/feed/space`（**匿名可读**，需 **WBI 签名 + buvid**；**`offset` 游标分页**；对「**间歇风控软返回空**」**重试一次**）；卡片支持 **图文（1/2/3/4 图）/ 转发（含「原文已被删除」）/ 视频投稿**（**视频投稿可直接跳播放页**，会自动补 cid）
- **专栏**：`x/space/article` + `x/article/view`（**匿名可读、无需 WBI**；参数名是 **`id` 不是 `cv`**；**`-509 请求过于频繁`** 时**退避 1.2s 重试**）
- **「收藏」不做**：实测他人收藏**匿名不可读**（8 位热门 UP 的 `fav/folder/created/list-all` **全 `data=null`**）→ **不放假功能**

**E. 专栏可读（自写轻量 HTML 渲染，零新依赖）**

- **新建 `app/lib/utils/bili_html.dart`**：**手写 tokenizer + 标签栈**（**零正则**解嵌套）；支持 `p / br / img / h1-h6 / blockquote / ul / ol / li / figure / figcaption / hr / strong / em / a / u / s`
- **降级**：未知标签**剥壳留文**（内容不丢）；**畸形输入不崩**（未闭合、嵌套错乱、裸 `<`、`<p>a<p>b` 隐式收口）
- **安全**：`script / style / iframe / object / embed / noscript / head / template / svg` 在**解析阶段连内容一起丢弃**
- **渲染遵守 mono-color 语言**：段落走 `kTypeBody`、引用块用 **1px 左竖条 + 下沉底色**（复用 `AppBlock` 的机制）、图片**圆角 + 1px 描边 + 按真实宽高比占位**（防跳动）+ 点击进 `ImageViewerPage`、链接**主墨下划线**
- **新建 `app/lib/pages/article_page.dart`**：标题 + 作者行（头像 / 名字 / 相对时间）+ 统计 + 正文；专栏卡（banner / 封面 + 标题 + 摘要 2 行 + 统计）

**F. 一致性与修正**

- `AppLoadingView` / `PressDots`（印刷方点）此前是**死代码**（全 App 整页加载都用 `AppLoadingHero`）→ 本次让它在 **3 处整块等待**真正上屏（收藏夹「拉全量」进度、登录页 WebView 就绪前、收藏夹导入弹层）；并**修正**了「全 App 最后一只转圈」这类与事实不符的注释，改为如实分工（只「整页 / 整块等待」走印刷语言，按钮内联 / 确定进度 / 播放语义 / 图片占位仍保留 Material 转圈）
- 专栏正文图片的 **loading 与 error 占位区分开**（error 带 `broken_image` 图标 + 「图片加载失败」小字，此前两者共用一个灰框，用户分不清「在加载」还是「失败」）

- 测试：`flutter analyze` **0 issue**；全量 `flutter test` **1495 例全绿**（本轮由 1441 → 1495）；关键改动做了**变异验证**与**像素级断言**（如「逐条印：任意时刻至多 2 条在长」、「同 seed 逐像素一致」、「滚动三种状态互不混淆」）
- 验证：**模拟器（AVD `bili_test` / Android 15）实测**——**加载态逐条生长、构图零位移**（全屏 diff 显示变化像素**全部落在 5 个线行内**，插画盒外变化 **0px**）、无灰雾；**空态运动元素由 5 降到 1**（4 帧 diff 确认唯一变量是那颗点 / 那片网点）；**点头像进的是评论者本人**（logcat 确认 `mid` 取自被点评论）；**动态**（转发 / 纯文本 / 引用已删除）与**专栏**（列表 + 阅读页 + 图片渲染、**无 HTML 标签外泄**）均正常；**滚动收起 / 恢复 / 到顶展开三种状态像素级区分**（展开 vs 收起差异 **21.8**，到顶 vs 基线 **0.0**）。已知环境限制：该 AVD 为 **SwiftShader 软件渲染（约 4.6fps）**，**动画顺滑度需真机判断**；模拟器上 B 站接口**大面积 412**，且专栏接口频繁 **`-509`**（需退避重试）

---

## v2.21.0 (2026-09-11)

**信箱卡片做成「扑克牌比例 + 5 个设计风格」（设置里可视化预览、点选即生效并持久化）、改成多层叠放 + 划走时下一张向前推出；修掉封面被裁掉标题的真实缺陷；并修「信箱只能滑一个 / 要退出重进 / 红点与卡片数不一致」三个数据层问题，白名单里的 UP 改为轮转覆盖全部**

**A. 卡片重做：扑克牌比例 + 5 个设计风格 + 设置内预览切换**

- **卡片比例改为扑克牌（`app/lib/widgets/inbox_card_styles.dart` 的 `kInboxCardAspect = 1.39`，即宽 : 高 = 1 : 1.39）**：原先卡片高度随屏高走，矮屏会被顶出屏幕或压得很扁；现在宽度由高度反算（`cardW = maxCardH / kInboxCardAspect`），竖屏 411×914 下卡片完整可见、**不出屏**；更矮的屏**等比缩放、比例不变**。高宽比语义抽成纯函数常量，方便与测试对齐
- **新增 5 个视觉语言明显不同的版式（新 `app/lib/widgets/inbox_card_styles.dart`）**：① `classic` **经典满幅**——封面全出血铺满、底部渐变遮罩、文字压图上（最接近上一版的 Tinder 观感）；② `editorial` **编辑排版**——印刷感：封面上 / 纸面下、墨条标题、版线分隔、底部信息行；③ `polaroid` **宝丽来**——相纸内框 + 下方宽留白 + 居中题注（象刚冲印出来的照片）；④ `banner` **沉浸横幅**——封面铺满上半、底部一条纸白信息条 + 墨色时长标签；⑤ `minimal` **极简留白**——小图居中 + 大字标题，其余全部留白
- **样式持久化（新 `app/lib/services/inbox_card_style_store.dart`）**：`shared_preferences` **单 key** 存风格 id（如 `polaroid`），带 `ensureLoaded` 预载（在 `app/lib/main.dart` 接线）、未知 / 缺失值回退 `classic`；改动只碰这一个 key，与既有的配色主题、界面文案各管各的
- **设置里的「信箱卡片样式」入口（`app/lib/widgets/manage_panel.dart`）**：「个人」页设置区新增分区 → 点开**弹层**列出 5 个风格；**预览用与真机完全相同的渲染器**、只把比例调小，所以**缩略图就是卡片本身等比缩小的效果**（不会出现「预览好看、真机变形」）；每项配中文名 + 一句话说明 + **当前项打勾**；点选**立即生效**并落盘，杀进程重进保持

**B. 卡片改成多层叠放 + 划走时下一张向前推出**

- **多层叠放（新 `app/lib/widgets/inbox_card_stack.dart`）**：同时渲染**顶层 + 后 3 张**（队列再长也只渲染前 4–5 张，不随队列长度线性涨开销）；逐层**更小 / 更低 / 更淡**——缩放 **1.0 / 0.96 / 0.92 / 0.88**、底边下移 **0 / 12 / 20 / 28dp**、不透明度 **1.0 / 1.0 / 0.94 / 0.88**；保留上一版定的**底边对齐**语义（下层稳定可见，与两张卡的高度差无关）
- **「下一张向前推出」的推出动画**：一个 `AnimationController` 驱动**连续深度函数** `depth_i(u) = i - p_i(u)`，各深度带**递增延迟**（形成「后面那张追上来」的层次感），时长 **`kDurAdvance = 260ms`**、曲线 `kCurveOut`，与顶层卡片**飞出并行**播放；数学上保证「出栈那一刻两边算出的位置**逐层重合**」→ **无跳变**；栈底**补卡**（从更深更淡处浮现）
- **正确性与性能**：每张卡用 **bvid 作 key**（保证 element 正确复用、不串版式状态）；`AnimatedBuilder` 一律传 `child:`、后层 widget 实例缓存 → **拖动期间不重建版式子树**
- **撤销做全反向**：卡片**从飞出的那一侧飞回** + 后层**反向退回**（不是简单复位，是按同一深度函数的逆过程）

**C. 修「信箱只能滑一个 / 要退出重进」的三个数据层问题（`app/lib/services/inbox_service.dart`、`app/lib/pages/inbox_page.dart`）**

- **出货极少**：拉取失败（412 / 网络）的 UP 走 `keep`、**永远拿不到基线** → 永久停在「首次」分支，而原先**首次只建基线、不产未读** → 能出货的 UP 极少。修法：**首次见到的 UP 也产出「首页最新 1 条」未读**（只 1 条，兼顾「不把历史一次堆上来」），基线照旧建立
- **结果不及时进 UI**：一轮检查要**串行**遍历 100 个 UP（每个 ≥1.5s，风控时 3s）→ 2.5~5 分钟，而原先**只在整轮跑完那一刻才合并**、离开页面还完全不合并 → 表现就是「要退出重进才有货」。修法：**边查边落盘**（每查完一个 UP 立刻写盘 + 刷新红点）；页面**每 3 秒回读**（**只读盘、不触网**、无变化不发 setState）；`checkAll` 加**单飞**（并发调用共享同一轮，不会叠出两轮）
- **红点与卡片数不一致**：红点按「UP 条目 × bvid」累加（**会重复计数**），卡片按 bvid 去重 → 数字对不上。修法：红点与卡片**统一到 `_collectVisible` 一处实现**（**按 bvid 去重** + 按时间倒序），`checkAll` 开头对 `upowners` **按 mid 去重**；`markHandled` 去掉 `break`（同一 bvid 落在两个 key 时**都摘掉**）
- **红线守住**：`markHandled` 仍是**唯一消费入口**、「打开不清空」、「只追加不重排」（上一版定的语义一个没动）

**D. 修封面裁切（用户反馈「卡片不好看」的核心原因）**

- **问题定性**：竖版卡片里对 **16:9 封面**做 `cover` 裁切 → **左右各裁约 21%**；而 B 站缩略图常把标题**烧在图上且贴边缘** → 文字被切断（实测「徘徊前进的两年③」只剩「年③」）。这就是用户觉得「卡片不好看」的主因
- **修法（新增 `_CoverRegion` 统一入口）**：**完整显示 16:9 图**——落位框宽高比**恒为 16:9**，`cover` 在该盒子里**等价于 `contain`**（**左右不裁、不拉伸**）；封面区本身不是 16:9 时，用**同一张封面的模糊放大版**做底衬（`ImageFiltered` 高斯模糊）；底衬**复用 `CoverImage`** → 防盗链头 / 占位逻辑**只有一处实现**
- **`classic` 进一步修实测到的硬接缝**：清晰图**顶对齐**（顶部底衬归零）+ 下缘 **20dp 渐变过渡**（`ShaderMask` + `dstIn`，**只改 alpha 不碰颜色**，常量 **`kInboxClassicCoverFade = 20`** 可调）
- **顺带两处**：`editorial` 信息区文案**整块垂直居中**（空档 130dp → 24dp）；`minimal` 封面占比 **0.62 → 0.90**、上下留白改 **1:1**

**E. 修「UP 只查前 100 个」与「已划过的卡复活」**

- **UP 轮转（`inbox_service.dart`）**：用户白名单有 **204 个 UP**，而 `kMaxUpowners = 100` → **第 101–204 个永远不被检查**（这是「信箱经常只有 1 张卡」最可能的残留原因）。修法：改为**轮转**——游标环绕，每轮取 ≤100 个并把游标推到这段之后，**204 个 UP 在 2~3 轮内全部覆盖**；同时把「可见性过滤」的依据从「本轮检查过的 mid」改为**「当前白名单全量 mid」**（新 key `inbox:meta:known_mids`），避免没轮到的 UP 的未读**忽隐忽现**
- **「已划过的卡复活」**：拉取失败的 UP 上「已处理过滤」失效 → 已划走的卡短暂回来。修法：`_collectVisible` 收集时**排除已处理的 bvid**（顺带保证红点与卡片仍一致）

- 测试：`flutter analyze` **0 issue**；全量 `flutter test` **1375 例全绿**（本轮由 1367 例增至 1375 例）；关键改动做了**变异验证**（把「首次产未读」改回返空、把红点去重去掉、关掉边查边落盘 → 对应用例**立刻变红**），证明断言有效
- 验证：**模拟器（AVD `bili_test` / Android 15）实测**——5 个风格的封面**都不再裁切**（逐风格实测宽高比 **1.778–1.783 ≈ 16:9**，用户点名那条视频做了**老 / 新同框对比图**，文字由「年③」恢复为**完整标题**）；`classic` 的硬接缝用**改前 / 改后同封面、同机对比**确认已消失；信箱**可见 4 层叠放**、**连续滑走多张不需要重进**、**红点与卡片数四次核对全部一致**；**UP 轮转生效**（游标 **0 → 100 → 200**，第二轮覆盖到第 101–200 个 UP，其中**第 103 个成功出货**）→ 说明「首次见到的 UP 产 1 条」在真实数据上生效；5 个风格可预览 / 切换 / 持久化。已知环境限制：该 AVD 为 **SwiftShader 软件渲染（约 4.6fps）**，**推进动画的平滑性无法在模拟器判定**（260ms 动画只覆盖约 1 个渲染帧），需真机；另外模拟器上 B 站接口**大面积 412 风控**，真实出货量仍被压制（每轮 1~2 张）——轮转解决的是「该检查的都检查到」，不等于「一定有新视频」

---

## v2.20.0 (2026-09-11)

**信箱重做成 Tinder 式左右滑卡片（右滑加入 / 左滑跳过 / 撤销）；修掉用户反馈的「信箱时不时清空」真实缺陷（检测与已读确认彻底解耦、队列只增不减）；空态细线插画改动态「随机游走线」；设置页补上「界面文案」编辑入口；播放页手势横滑复用「拖动进度条」的呈现（进度条浮出 + 缩略图预览气泡）**

- **信箱改造成卡片栈（新 `app/lib/widgets/inbox_swipe_card.dart`、`app/lib/services/inbox_handled_store.dart` + 重写 `app/lib/pages/inbox_page.dart`）**：信箱页由列表改为 **Tinder 式卡片栈**——卡片含**封面大图（16:9）+ 标题（≤2 行）+ UP 头像与名字 + 时长 + 相对时间**；下层卡片以「**底边对齐**」方式露一条边（与两张卡的高度差无关，修掉「下一张更矮时被完全盖住」）；**点按卡片仍可进播放页**（与滑动不冲突）
- **滑动手势**：拖动时卡片跟手并**轻微旋转（最多 ±12°）**；**右滑 = 加入白名单**（like，显示「加入」浮层标记）、**左滑 = 跳过**（skip，显示「跳过」标记）；位移未过阈值（**屏宽 30% 或速度 700px/s**）→ **弹回**；底部另提供与滑动等价的「**跳过 / 加入**」按钮（触摸目标 **≥48dp**）；支持**撤销上一张**；`MotionControl` 关动效时**不创建 controller**（测试环境不产生无限动画）
- **修掉「信箱时不时清空」缺陷（用户反馈，已定性为真实缺陷）**：**根因**——`checkAll` 在**发现新视频的当场就把已读基线 `last_seen_bvid` 推到最新**，于是下一次检查必然「没有新视频」并顺手删掉该 UP 的未读缓存、红点归 0；而**打开信箱页本身就是一次强制检查** → 用户一打开内容就被吃掉。**修法**：把「检测」与「已读确认」彻底解耦——`checkAll` 只做「拉取 → 算候选 → **合并**进队列（不覆盖、不推进基线）」；**只有卡片操作（右滑 / 左滑 / 按钮）才消费**（`markHandled`）；基线只在「**首次见到某 UP**」或「**该 UP 队列被清空**」时推进，且**基线写盘成功后**才清「已处理」记录
- **队列语义**：**持久化**（杀进程重开仍在）、**只增不减**、**检查失败不丢**——整体失败回退上次缓存 + `failed` 标记；**单个 UP 失败则该 UP 队列一个字节不动**；「已处理」bvid 持久化到 `inbox:handled_bvids`，`checkAll` 算未读时排除
- **信箱可用性修复（设备实测发现的）**：① **检查期间不再阻塞交互**——白名单约 100 个 UP 时 `checkAll` 要跑约 2.5 分钟，原先点卡片静默无反应、按钮全灰 → 改为**不参与交互门禁**（只显示一行「检查中…」提示）；并处理并发：检查回来**只追加不重排**（卡片栈不回到第一张），服务层写盘前按「此刻」的已处理记录**重算**，避免把刚划走的条目写回未读；② **空态保留撤销入口**（原先滑掉最后一张后底部栏整体消失，撤不回来）；③ 「下一张露一角」改为**底边对齐**；④ **用户划完队列后显示空态而非加载态**（原先会先闪「正在加载…」数分钟）；⑤ 预览取帧对「接近总时长」**回退 1.5s**（避开片尾黑格）
- **手势横滑复用「拖动进度条」的呈现（`app/lib/pages/player_page.dart`）**：手势左右滑动调进度时，进度条**浮出来且手柄跟随**（控制层收起时只浮出进度条行，**不改用户的显隐偏好**）；同时显示**预览气泡（缩略图 + 时间）**，位置按位置比例映射到轨道，**松手气泡消失**；预览管线可用时**不再叠居中 seek 时间浮层**（竖屏空间不够会重叠）；**亮度 / 音量的浮层完全未变**
- **空态细线插画改成动态「随机游走线」（重写 `app/lib/widgets/dot_illustration.dart`）**：从静态图案改为**带限布朗曲线**（1/f² 频谱的随机相位正弦叠加）——3 条 1px 细线随时间缓慢漂移 + 变形，沿线**左浓右淡**，点缀色小点骑在线上；**同 seed 同路径**（谐波初相由 seed 播种，一次性生成）；空间 / 时间相位都是**整圈** → **循环无突变**；`MotionControl` 关闭时走**静态一帧、零 ticker**；**调用方零改动**
- **设置页补上「界面文案」编辑入口（`app/lib/widgets/manage_panel.dart`）**：此前 `UiCopyStore` 的数据层与各处接线都做了（47 条出厂文案），**但设置页的编辑入口一直没落地** → 现在补上：`ManagePanel` 新增「界面文案」分区 → 弹层按场景分组（**17 组**）列出全部 **47 条**，可逐条改写、单条恢复默认、全部恢复默认（二次确认）；**改一个字符即全局生效**，写入 `shared_preferences`；空 / 纯空白输入 = 清除该条覆盖、用回默认；`kDefaultCopies` 出厂值未被改动
- **文案调整**：「UP 主」页入口文案 `导入我关注的 UP` → **`导入我的 UP`**（连同导入页标题、登录门禁与登录报错共 **4 处**用户可见文案）
- 测试：`flutter analyze` **0 issue**；全量 `flutter test` **1339 例全绿**（本轮由 1176 例增至 1339 例）；其中关键用例做了**变异验证**（如把「队列合并」改回「覆盖」→ 5 例立刻变红；把「不推进基线」改回旧行为 → 回归用例变红），证明断言有效
- 验证：**模拟器（AVD `bili_test` / Android 15）实测**——右侧滑动与底部按钮均能跳过、标记浮现、弹回、撤销、空态、点卡进播放页；**反复进出信箱 + 杀进程重开，条目仍在**（「时不时清空」缺陷不复现）；且在**真实 412 风控导致绝大多数 UP 拉取失败**的场景下，**本地队列一条未丢**；空态插画确认为动态（间隔抓帧有像素差异、不同页面 seed 不同）；横滑出现进度条 + 缩略图预览气泡。已知环境限制：该 AVD 为 SwiftShader 软件渲染（约 4.6fps），**动效顺滑度无法在模拟器判断**，需真机；另：把 Android 三个动画缩放置 0 会让 App 装饰动画静止（`MotionControl` 会读 `MediaQuery.disableAnimations`），**验证动效前须确认三个 scale 为 1**

---

## v2.19.0 (2026-09-11)

**交互三补：合集卡左滑露出「重命名 / 删除」操作块（跨卡联动 + 与长按拖拽排序共存）、竖屏横滑也能调进度、拖动进度条显示「视频帧缩略图 + 时间」预览气泡**

- **合集卡左滑操作块（新 `app/lib/widgets/swipe_action_box.dart` + `app/lib/pages/playlist_page.dart`）**：首页「合集」页的真实合集卡支持**左滑**，右侧露出「**重命名**」「**删除**」两个操作块，点击复用既有的对话框 + 写 Gist 流程（不新增第二套逻辑）；组件为通用「左滑露出操作块」——**吸附**（松手过半则定格露出，未过半自动收回）、**手势结束收回**、**触摸目标 ≥48dp**（达标 Material 最小点击区）、`MotionControl` 关动效时**零 controller**（测试环境不产生无限动画）
- **跨卡联动（模块级静态引用，对宿主零侵入）**：同屏只允许一张卡露出——滑开新卡会**自动收回上一张**；按下别处 / 列表滚动同样收回。宿主页面无需持有任何状态或注册回调
- **圆角与裁切**：卡片右圆角缺口用「**只裁右外缘 + 同色补角垫片**」补齐（含**像素级测试**），露出区与卡片本体视觉连续
- **与长按拖拽排序共存（`ReorderableDelayedDragStartListener`）**：延时拖拽是**长按 500ms** 触发，横滑位移**超过 18px slop 时 Flutter 的延时识别器会自认输** → 左滑与拖拽**不冲突**（有 Flutter 源码依据 + 回归用例）；顺带修正了 `DragStartBehavior.start` 会吞掉**越过 slop 的位移**、导致单步大幅滑动失效的问题
- **禁用范围**：「未分类」卡与「收藏夹」卡**不启用**左滑（无重命名 / 删除语义）
- **竖屏也能左右滑动调进度（`app/lib/pages/player_page.dart`）**：修前非全屏水平滑动 seek 被三处硬编码 `if (_fullscreen)`（`player_page.dart:2157` / `:2173` / `:2194`）**整条掐断**（连时间 HUD 都不弹）→ 用户反馈「竖屏无法左右滑调进度」；修后三处改为统一纯函数判定 **`canGestureSeek({listenMode, durationMs})`**（听视频模式与时长未知时不 seek），竖屏 / 横屏置顶 / 全屏走**同一条路径**。非全屏**关闭底部 48px 手势豁免带**（它压在 231dp 视频区的中部，白吃 **21% 起手区**）；**全屏行为一字未动**（此前用户专门要求过的「底部豁免」修复被保住在测试里）；与亮度 / 音量的**方向判定未放宽**（避免另一个方向的破坏）
- **拖动进度条显示缩略图预览（新 `app/lib/models/video_shot.dart`、`app/lib/services/video_shot_service.dart`、`app/lib/api/bilibili_api.dart` 的 `fetchVideoShot`）**：拖动进度条时上方出现预览气泡 = **视频帧缩略图 + 时间**；数据来源是 B 站官方「进度预览图（雪碧图）」接口 `x/player/videoshot`（**免 wbi 签名、免登录**），返回 **10×10 雪碧图** + 每帧秒数（粒度约 5–8s）；`VideoShotInfo` 内含**时间 → 缩略图二分的纯函数**
- **内存红线**：雪碧图原图 4800×2700 全尺寸解码约 **52MB/张** → 实现为**按需只下当前那张 + `instantiateImageCodec(targetWidth:)` 降采样**（实测解码后 ≈**5.5MB/张**）+ **LRU 容量 2**（淘汰时 `dispose`）+ **in-flight 去重**
- **预取**：进入播放页 / 恢复进度 / seek 落点后，后台预取当前位置所在的**那张雪碧图** → 首次拖动即可见
- **失败静默降级**：接口 / 网络 / 解码任何失败 → **只显示时间气泡**（不阻塞拖动、不弹错）
- **「首次拖动看不到缩略图」修复**：原实现按「每请求递增的序号」丢弃迟到结果，而拖动全程常落在**同一张雪碧图**内、整图下载要 1.5–2.5s → 结果**全被判迟到丢弃**。改为**按雪碧图号落地**（同图迟到的结果**仍有效**，且用当前位置**重算裁剪矩形**）、**跨图才丢弃**（防显示错误区段 + 防画已释放的图）
- **气泡尺寸**：竖屏 120×67.5、全屏 180×101.25（**不拉伸变形**，均为 16:9）
- 其余：上一轮还顺带修了「合集管理」与卡片**共用同一份重命名 / 删除对话框**（抽成顶层函数，**文案逐字保留**）
- 测试：`flutter analyze` **0 issue**；全量 `flutter test` **1245 例全绿**（本轮由 1176 例增至 1245 例）；其中关键用例做了**变异验证**（如把「按雪碧图号落地」改回「按请求序号」，新用例**立刻变红**），证明断言有效
- 验证：debug APK 装机模拟器（AVD `bili_test` / Android 15）实测——① **竖屏横滑出现 seek 时间浮层**且松手后进度改变，竖屏纵滑仍是亮度 / 音量；② **拖动进度条实拍到「缩略图 + 时间气泡」**；③ **冷启动后首次拖动 3 秒内即出缩略图**（证明预取命中）；④ 合集卡**左滑露出两块**、点「重命名」弹出对话框（未真改）、**跨卡联动收回**、「未分类」卡**左滑无反应**、**已滑开的卡长按拖拽仍可用**（拖起有浮层与投影，拖回原位**零像素差异**）。已知环境限制：该 AVD 是 SwiftShader 软件渲染（约 4.6fps），**动效顺滑度在模拟器上无法判断**，需真机

---

## v2.18.0 (2026-09-10)

**视觉体系级重构 + 动效系统建设：token 化设计地基（克莱因蓝 + 暖白底材、全面去阴影改描边）、块化设计语言（AppBlock 6 变体）、交错入场 + 封面飞入、风衣男剪影加载动画；首页改 Material 3 底部导航四页签，App 与项目更名 amoTV**

- **视觉地基与配色系统（新 `app/lib/theme/app_tokens.dart`、`app/lib/theme/app_theme.dart`）**：新建 token 体系（颜色 / 字阶 / 间距 / 圆角 / 动效）与主题工厂 `buildAppTheme`，**全库 197 处硬编码颜色统一收敛到 token**（不再散落魔法色值）；主色由 B 站蓝 `#00A1D6` 换成**克莱因蓝 `#002FA7`**，底材改**暖白 `#FAFAF7`**，**取消一切阴影**——层级改由 **1px 描边 + 底材差**表达；新增 `app/lib/theme/ink_recipes.dart`（**10 套双墨配色**，取自 mono-color-skill 的双色表）+ `app/lib/theme/app_palette.dart`（`AppPalette extends ThemeExtension`，内置 **WCAG 对比度护栏**：`inkFill` / `inkText` / `inkDeco` 分档，浅墨配方自动压深到 4.5:1 / 3:1）+ `app/lib/services/theme_store.dart`（本地持久化）；**设置面板新增「配色主题」选择器**（10 套，点击即生效、杀 App 重进保持）
- **品牌与导航结构（`app/lib/pages/playlist_page.dart` 等）**：App 与项目更名为 **amoTV**（Android 显示名、AppBar 标题、README、油猴脚本 `@name`；`applicationId` 与 Dart 包名按工程判断**未改**，以免用户数据丢失）；首页导航从「AppBar 图标 + PageView 横滑」改为 **Material 3 底部导航 4 项**——合集 / UP 主 / 历史 / **个人**（原「统计」+「设置」合并为「个人」，统计在上、设置在下）；「新建合集」从设置区移到合集页（顶部常驻按钮 + 空态行动按钮）；修复导航栏 1px 顶线被 `NavigationBar` 不透明背景盖掉，AppBar 与卡片补描边
- **块化设计语言（新 `app/lib/widgets/app_block.dart`）**：`AppBlock` 提供 6 个变体（`videoInfo` / `comment` / `reply` / `videoCard` / `collectionCard` / `setting`），靠「底材差 + 描边重量 + 左侧竖条 + 圆角」四轴表达层级；评论块 / 回复块（缩进 + 左侧竖条 + 下沉底色）/ 视频卡 / 历史卡 / 播放页信息块 / 合集卡全部块化；播放页信息块带左侧短竖条（主墨图形档）
- **动效系统（新 `app/lib/theme/app_motion.dart`、`app/lib/theme/motion_control.dart`、`app/lib/widgets/staggered_entrance.dart`）**：入场节奏 token 化；全局动效开关 **在测试环境自动关闭**（避免无限动画卡住 `pumpAndSettle`）；**交错入场**——首屏 36ms/条、240ms 单条、最长 528ms 封顶，翻页追加 20ms / 180ms / 300ms，配 `EntranceLedger` 记账（滚出滚回不重播），应用于评论、历史、收藏夹、夹内视频、搜索结果（视频 / 番剧 / UP 主）、信箱、UP 主页、关注导入；**页面转场升级为三段式**（淡入 + 3% 上滑 + 下层压暗），**播放页走快速淡入无位移**（配合封面飞入）；新增 `app/lib/widgets/cover_hero.dart` + 播放页封面占位层——**点卡片时封面飞入播放页**（原生纹理无法 Hero，故为「封面 → 占位 → 淡出露画面」的假转场，含 1200ms 兜底超时）；播放页信息块**延迟补场**（120ms 后 320ms 内淡入 + 96%→100% 微放大），与封面飞行时间重叠
- **加载动画与加载文案（新 `app/lib/widgets/smoke_silhouette.dart`、`app/lib/widgets/animated_copy_line.dart`）**：**通用风衣男抽烟剪影**（不指向任何具体角色，规避版权），剪影用主墨图形档、烟用点缀墨，3 条非等分相位烟缕为确定性动画（3200ms 周期）；`AnimatedCopyLine` 逐字淡入 + 上浮 4px；新增 `AppLoadingHero`（剪影 + 主文案 + 逐字副文案），各页整页加载与分页 footer 换用；新增 **15 条可编辑加载文案**（`loading.line.*` / `footer.loading.*` / `loading.empty.*`），走既有 `UiCopyStore` 机制，用户可在设置里改写
- **其它视觉与交互收口**：空态 / 错误态统一到 `AppStateView` / `AppErrorView` + `DotIllustration` 细线插画（每页不同 seed），删掉 5 个重复的私有状态组件；合集卡成为视觉主角——**「N 天前更新 / 加入」角标**（点缀墨）+ **底部 3px 观看进度条** + 「已看 X/Y」；新增 `app/lib/widgets/add_success_button.dart`（「加入白名单」三态按钮 + **波纹扩散 + 描边生长对勾**确认动效）；播放页进度条从系统 `Slider` 改为自绘细轨（2px + 暗垫，亮 / 暗画面双向可见），底部按钮开启态加「纸白短线」（颜色不是唯一信息载体）；`HistoryStore.maxEntries` **200 → 1000**
- 修复 6 处模拟器实测发现的缺陷：导航描边不渲染、播放页标签截断、进度条亮底不可见、未读角标不随配色、返回键与横幅重叠、浅墨下热力图对比度不足；修复 2 个集成测试既有失败（comment_flow 日志白名单漏 `[comment_list]` 前缀；landscape_pin 依赖宿主机手工旋转 → 改为测试自触发）
- 测试：`flutter analyze` **0 issue**；全量 `flutter test` **1149 例全绿**（本轮由 866 例增至 1149 例，新增覆盖 token / 双墨配色与 WCAG 护栏、块组件 6 变体、交错入场与 `EntranceLedger` 记账、动效开关的测试环境自动关闭、加载文案可编辑等）；集成测试 **11 例全绿**（含横屏置顶模式流程）
- 验证：debug APK 装机模拟器（AVD `bili_test` / Android 15）实测取证——底部导航四页签切换与描边渲染、10 套配色切换即时生效且杀进程重进保持、块化观感（底材差 / 描边 / 左侧竖条层级可辨）、列表交错入场逐条浮现、剪影加载动画 + 逐字文案、点卡片封面飞入播放页（占位淡出露画面）、「加入白名单」确认动效（波纹扩散 + 对勾生长）

---

## v2.17.17 (2026-09-09)

**横屏置顶模式：退出全屏不再强制转竖屏——设备横放停在横屏「顶部置顶视频 + 下方评论区」；布局横竖屏自适应**

- **方向策略（`app/lib/pages/player_page.dart` `_toggleFullscreen` / PopScope）**：全屏（横屏）播放中点「退出全屏」或返回（顶栏箭头 / 系统返回键）→ **不再强制转竖屏**——方向放开为「竖屏 + 双向横屏」三向（`kPlayerPageFreeOrientations`：portraitUp + landscapeLeft/Right，不含 portraitDown 防倒持误入）：设备当前横放即**停在横屏「置顶+评论」模式**，设备竖放仍竖屏置顶+评论（兼容 v2.17.0 行为），之后旋转设备可在横/竖两态自由切换。**全屏中返回 = 先退出全屏**（回当前方向置顶+评论，页面不离开，再返回才离开播放页，`_handleBack` + `PopScope(canPop: !_fullscreen)`）；离开播放页 dispose 仍恢复系统竖屏 + edgeToEdge（`_restoreSystemUi` 现状保留）
- **非全屏布局横竖屏自适应（同一 Column 两分支）**：竖屏封顶比例不变（屏高 60%）；**横屏**（宽>高，屏高低）→ 视频区高度封顶改为屏高 **55%**（`kLandscapeVideoHeightRatio`，16:9 视频按屏宽换算理想高度≈整屏高，必须封顶留出下方内容；盒内画面仍按 AspectRatio 居中 + 黑边补齐），视频信息行**横屏自动紧凑**（标题 1 行 / 简介折叠少行，`_buildVideoInfoBar` 按方向取 maxLines/foldLines），简介「展开」封顶高度横屏收紧（48–80，`_descMaxExpandedHeight`），剩余高度全部留给**内嵌评论区**（Expanded 哪怕矮也可滚）；手势层/字幕/听视频占位/控制层仍只绑定视频区矩形（横屏非全屏同样生效，`_gestureAreaSize`/字幕 bottom/中央簇紧凑判定改为复用同一方向感知的视频区高度 `_embeddedVideoHeight`）
- 测试：新增 `test/player_landscape_pin_test.dart` widget 测试（platform 通道捕获 `SystemChrome.setPreferredOrientations` 实参：进全屏=[landscapeL/R] 锁横屏、退出/返回退全屏=放开三向、dispose=[portraitUp] 恢复——**方向策略核心证据**；另断言竖屏 400x800 与横屏 800x400 两态视频区封顶高度/信息行紧贴/评论区可见不越屏/全屏无下方内容区）；新增 `integration_test/landscape_pin_flow_test.dart` 真机验收（真实播放器 + 宿主机 adb 旋转打点：竖屏置顶→进全屏→横屏退出**停留横屏置顶+评论**（几何/评论区可滚/开弹幕不崩）→转竖屏回归→返回先退全屏）；`flutter analyze` 0 issue、全量 `flutter test` 通过（866 例）
- 验证：debug APK 装机模拟器（Android 15 API35，真实白名单视频匿名播放「Yazi 文件管理器」BV1yRkCYVEUT，onPrepared 852x480 + 进度保存/观看计时日志确认真实播放）——**集成测试 `integration_test/landscape_pin_flow_test.dart` 全流程取证（[集成] 几何数值）**：① 竖屏进入播放 屏=411x914，视频区 **411x231@顶 0**（宽高比封顶）、信息行顶=231、评论区 411x480 底 890≤914（竖屏置顶+评论）；② 进全屏 → 屏 914x411、视频区 **914x411 占满整屏**、信息行/评论区=0（全屏无下方内容区）；③ **横屏退出全屏 → 停留横屏置顶+评论（核心验收）**：屏仍 914x411（未强制转竖屏），视频区 **914x226@顶0 = 屏高 55% 封顶**、信息行顶=226、评论区 862x22 可见不越屏且 **上拉滚动 300px 生效（评论区可滚）**、全程 0 RenderFlex 溢出异常；④ 转回竖屏 → 回归 视频区 411x231@顶0 + 评论区 480（竖放兼容 v2.17.0）；⑤ 全屏点返回箭头 = 先退出全屏（页面不离开），再返回才离开播放页（dispose 恢复方向）。弹幕冒烟：横屏/竖屏置顶下开弹幕真实拉取 **121 条**、游标对齐/布局正常、开关关闭不崩；观看计时 +10s 累计正常。方向策略 API 侧证据在 widget 测试（platform 通道捕获 setPreferredOrientations 实参）

---

## v2.17.16 (2026-09-09)

**搜索页「搜索历史记录」+ 观看热力图配色改「相对制」（不再固定时间分档）**

- **搜索历史（本地，`app/lib/services/search_history_store.dart` + `search_page.dart`）**：三个搜索 Tab 共用一份本地关键词历史（shared_preferences 单 key JSON 数组，不入 Gist 不跨设备）——**去重置顶**（新搜置顶、重复关键词剔除旧位置移到顶部）、**上限 20 条**自动裁剪最旧、支持单删与一键清空；存储损坏（解析失败 / 非列表 / 脏元素）一律容错为干净数据，不崩溃不影响搜索（含损坏后 add 自愈，见单测）。**搜索页 UI**：进入搜索页输入框为空时（「全部 B 站 / 搜索 UP 主」Tab）显示「搜索历史」面板——标题行（历史图标 + 清空入口）+ 历史词列表（每行点 = **直接填入输入框并立即搜索**、行尾 X 或长按 = 删除单条）；切到「我的白名单」Tab 面板自动隐藏不挡本地列表（空历史也不显示）。**记录时机**：键盘搜索键 / 点搜索按钮 / 点历史词 → 记入历史（防抖自动搜索、切范围/排序自动重查**不记录**，避免把打字联想中间词刷进历史）；切 Tab / 切类型不影响历史。测试：store 单测（去重置顶 / 重复上移 / 上限裁剪 / 空词忽略 / removeAt / clear / 持久化模拟重启 / 损坏容错自愈 / dedupeFront 纯函数）+ 搜索页 widget 冒烟（空历史隐藏 / 历史词渲染 / 输入时隐藏清空恢复 / 点历史填入并搜索 / 键盘提交记录 / 单删 / 清空 / 白名单 Tab 不挡）
- **观看热力图配色改「相对制」（`watch_stats.dart` + `watch_stats_page.dart`）**：删除固定分钟分档（原 `watchLevel` 0..5 / `kHeatLevelColors` / `heatColorForLevel` 及其单测），改为窗口（近 53 周）内**最长单日观看秒为基准**的连续渐变——纯函数 `WatchStats.relativeIntensity(daySec, maxSec)` = `clamp(daySec/maxSec, 0, 1)`（max<=0 或无观看防除零返回 0；超长截断 1）+ `heatColorForIntensity(intensity)`（≤0 = 无观看浅近白底 #EBEEF5；≥1 = 最长那天**克莱因蓝 #002FA7**；0<强度<1 = 浅蓝起点 #D5E5FF → 克莱因蓝连续插值）；`HeatCell` 不再固化 level（渲染时按 `maxDaySecondsOfGrid(grid)` 实时算强度）——**数据只集中一天也能最深**（不再像固定分档那样日均 5 分钟永远浅色），任一天观看都能在窗口内找到相对深浅；图例改「无观看灰格 + 浅蓝→克莱因蓝渐变条」，说明文案点明「相对色阶：最深=近53周内单日最长观看，其余按当天/最长比例变浅」（原「档位：<5 分/…」固定文案移除）。测试：`relativeIntensity`（0/半量/1/超长截断/max≤0 防御/单日即最长=1）+ `heatColorForIntensity`（两端 + 0.5 连续插值 + 单调）+ `maxDaySecondsOfGrid`（取窗口最长 / 全 0 防御）
- 测试：新增 `search_history_store_test.dart`、`search_history_page_test.dart`（假同步服务 + 假 B 站 API 注入，不发真实网络），更新 `watch_stats_test.dart` / `watch_stats_page_test.dart` 配色与网格用例；`flutter analyze` 0 issue、全量 `flutter test` 通过（865 例）
- 验证：debug APK 装机模拟器——**① 搜索历史**：搜索页输入搜一次 → 清空输入框 → 「搜索历史」面板出现该词（uiautomator dump 见「搜索历史 / 关键词」文本）→ 点历史词触发搜索（输入框自动填入、日志出现搜索请求）→ 单删（行尾 X）/ 清空可用；**② 热力相对配色**：颜色为逐格渲染语义无法截屏取证——由「相对强度/配色纯函数单测 + 网格布局 dump（最长日格子 Semantics 文案）+ 最长日=最深语义」覆盖验证边界

---

## v2.17.15 (2026-09-09)

**根治播放中「间歇 2001/Source error」的必现路径——流 URL deadline 实测 2h + 主动预取到期前平滑换源**

- **deadline 实测（v2.17.15 前置侦察，`deadline_probe.py` 临时脚本，测完即删不入库）**：真实白名单视频匿名取流（WBI playurl，普通 + 番剧共 4 条视频 × DASH video/audio/mp4 durl），解析流 URL query 的 `deadline` 参数——**全部稳定 7200s = 整整 2 小时**（单位是 Unix **秒** ~1.7e9，2026-09 实测），且带防盗链头实测 HTTP 206 可取流。**结论：普通视频（<2h）播放中不会因 URL 到期中断，v2.17.14 观察到的「间歇 2001」主因是网络/CDN 瞬时错误（已由 v2.17.14 自动续播覆盖）**；但「超长视频（>2h）一次看完 / 长时间暂停后续播」仍会**用尽** URL 剩余有效期 → 到期后读流必失败——这是被动的 onUrlExpired「失败才恢复」无法消除的必然中断，本次根治
- **主动预取 + 平滑换源（Dart `app/lib/pages/player_page.dart`，v2.17.15）**：播放中每 10s（20 × 500ms tick）解析当前**网络流** URL 的 `deadline`（`streamDeadlineMs`/`streamDeadlineRemainMs`/`shouldPrefetchSource` 纯函数，单位秒/毫秒按量级自动归一）→ 判定「URL 剩余有效期不足以播完剩余内容 + 60s 提前量」→ **到期前主动重取 playurl（同集同清晰度）→ setDataSource(新 URL, 当前位置) 平滑换源**（复用续播路径，但 URL 尚有效时主动做，播放几乎无感）；换源成功记录新 deadline 继续周期判定。普通视频差量恒定 ≫ 提前量 → 判定不通过即零开销返回，**不发任何多余请求**
- **防冲突（与 v2.17.14 机制协同）**：预取进行中（`_prefetching`）与被动自动续播（`_autoRecovering`）**互斥**（入口互查对方）；`_initSession` 播放器重建代次校验（重进/手动重试/切集/换源时安全退出，防向新播放器重复 setDataSource）；用户 seek 拖动中不触发；换源节流 ≥90s（成功/失败都占位，防网络异常时请求风暴）；**换源失败静默不弹错**（URL 尚有效则下轮再试；预取期间原生若因 URL 真过期发 onUrlExpired 被让路 → 预取失败路径自动回退 `_onAutoRecover` 被动续播兜底）
- **deadline 缺失回退**：本地缓存播放 / URL 无 deadline 参数 → 不预取（本地文件不取网络流；无签名 URL 无过期概念），保持 v2.17.14 被动恢复
- 测试：新增 `player_prefetch_test.dart` 17 例（deadline 秒/毫秒/缺失/非法/相对串解析、剩余毫秒计算含过期、换源决策含边界/过期/缺失/时长未知/自定义提前量、常量自洽）；`flutter analyze` 0 issue、全量 `flutter test` 通过
- 验证：debug APK 装机模拟器——**① 换源执行路径**：临时把记录 deadline 压到「剩余 120s」（纯验证手法，验证后还原源码）→ 播放 ~10s 后日志出现「主动预取换源：URL 剩余≈…s」→ 重取 playurl → setDataSource → 再次 onPrepared 续播，播放未中断未弹错；**② 正常路径回归**：还原后普通视频（真实白名单视频）播放无任何预取日志（差量 ≫ 提前量，零请求零干扰）；真 deadline（2h）到期现场无法短时复现，由决策单测 + 换源执行路径日志 + 代码审查覆盖，边界见正文

---

## v2.17.14 (2026-09-09)

**播放中断自愈：网络抖动/读流超时（「播放失败（2001）：Source error」）不再打断观看——自动重取流续播 + 数据源超时调大**

- **根因**：原生 `DashExoPlayer` 只把 HTTP 403/404/410（流 URL 过期）归为 `onUrlExpired` 自动恢复；**读流超时（Media3 errorCode 2001 = `ERROR_CODE_TIMEOUT`）/ 瞬时网络 IO 错误（断连/解析失败）全走 `onError` → Dart 弹「播放失败（2001）：Source error」打断观看，需手动重试**。叠加 `DefaultHttpDataSource` 默认 connect/read 超时都只有 **8s**——弱网/抖动（慢速读流、秒级断流）很容易在 8s 处被误判超时
- **错误分类扩展（原生 `app/android/.../DashExoPlayer.kt`，v2.17.14）**：`onPlayerError` 的分类改为 `isRecoverableSourceError`——**URL 过期**（HTTP 403/404/410，另把 429/5xx 这类 CDN/网关瞬时故障一并纳入——重取流换新签名地址即可自愈）∪ **瞬时网络错误**（读/建连超时 `SocketTimeoutException`、域名解析失败 `UnknownHostException`、连接被重置/拒绝/断开 `SocketException` 等）→ 统一走 `onUrlExpired` 让 Dart **重取 playurl + setDataSource 续播（保留位置）**，不再弹错误；只有真失败（格式损坏/解码失败/本地文件缺失等）才走 `onError`。判定按 java.net 原生异常沿 cause 链查找，不依赖 media3 包装类型（1.5.x 已移除 `HttpDataSource.TimeoutException`，超时由 `HttpDataSourceException` 包 `SocketTimeoutException` 呈现，链式可达）
- **数据源超时调大（原生）**：connect **8s → 15s**（弱网建连/首字节慢不再误判）、read **8s → 20s**（单次 socket 读超时：正常传输数据连续，>20s 收不到任何字节 ≈ 连接已死；宁可多等配合自动续播兜底，不在慢网上误弹错）
- **Dart 自动续播（`app/lib/pages/player_page.dart`）**：`onUrlExpired` 事件语义扩展为「可自动恢复的数据源错误」，续播失败按退避 **1s→2s→4s 自动重试最多 3 次**（`kAutoRecoverBackoffMs`/`autoRecoverDelayMs` 纯函数）——**每段播放独立预算**：新流成功 READY / 手动重试 / 换源 / 重进清零，播放中多次零星网络抖动各自都有完整重试次数；防重入（`_autoRecovering`）+ 播放器重建代次校验（`_initSession`，防 await 间隙向新播放器重复 setDataSource）。**仍失败才显示「播放中断（网络或视频流异常），请重试」+ 重试按钮（手动兜底保留）**——2001 弹错打断观看成为极少情况
- 测试：新增 `player_auto_recover_test.dart` 7 例（退避表 1s→2s→4s 且递增、放弃文案非空、`autoRecoverDelayMs` 第 0/1/2 次分别 1000/2000/4000、第 3 次起超限返回 null、负数防御、预算与上限自洽）；原生错误分类无可复用 Kotlin 单测设施（`android/app/src` 无 test 源集），由 Dart 决策纯函数 + 代码审查 + 构建覆盖；`flutter analyze` 0 issue、全量 `flutter test` 通过（820 例）
- 验证：debug APK 装机模拟器（Pixel 9 API 35，软件渲染）实测——**① 正常播放回归**：真实白名单视频（火柴人 VS 我的世界，BV163426vE3s）起流 → `onPrepared 852x480 duration=740s`、进度持续前进并 10s 定时落盘（`保存进度 …9682 ms`），无回归；**② 自动恢复现场复现**：播放中一次真实 connect 失败（DefaultHttpDataSource 建连异常）→ 原生按新分类发 `onUrlExpired`（**未走 onError**）→ Dart 日志「自动续播第 1 次，退避 1000ms」→ 重取 playurl（dashV=4/dashA=3）→ setDataSource → `onVideoSizeChanged`/继续出帧——全程无「播放失败」弹错，旧代码此路径会直接弹错打断；**③ 手动重试兜底**：wifi+蜂窝数据全断后进入播放 → 取流失败显示错误视图（重试/返回按钮）→ 恢复网络点「重试」→ 再次 `onPrepared` 正常播放。**2001 读超时主动复现边界**：模拟器网络快，Progressive 源近整段 m4s 提前缓冲 + 蜂窝兜底链路，长时间断网不触发读超时——超时分类由真实 connect 失败现场（同一条 isRecoverableSourceError 链）+ 单测 + 代码审查覆盖；弱网真机上的实测反馈待后续版本收集

---

## v2.17.13 (2026-09-09)

**启动自动同步关注 → 白名单（增量静默）：每次进 App 自动把新关注的 UP 加进白名单；关注导入链路加固（"导入后白名单没几个"的解析容错修复）**

- **启动自动同步服务（新 `app/lib/services/followings_auto_sync.dart`，v2.17.13）**：首页 initState 后延迟 ~4s **静默执行一次**（登录成功返回后也顺手触发一次）——三关前置门禁（**未登录 / 未配 GitHub token+gist / 距上次成功同步 < 10 分钟** → 直接跳过，不发任何请求）→ 翻页拉 B 站关注（`fetchFollowingsOfMine`，20 条/页、**最多 10 页 = 前 200**，与手动导入页上限一致）→ 对不在白名单的关注批量 `UpownerWriter.addBatch` 增量加入（一次拉 Gist → 查重合并 → 一次写盘，复用导入链路）→ 有新加入自动刷新白名单列表（UP 管理页/信箱立即可见）
- **节流与提前停止（取舍）**：成功完成一次同步（含"没有新关注"）才记录时间戳，10 分钟内重复启动不重复拉取（防频繁重启骚扰 B 站接口）；翻页遇到**连续一整页（20 条）关注都已在白名单** → 提前停止（新关注总排在关注列表前面，后面的老关注基本已同步过——首次同步白名单接近空不命中该条件，自动拉满前 200 建库）；中途某页失败（网络/风控）用已拉到部分先增量同步；**失败一律不记时间戳 → 下次启动自动重试**
- **只增不删 + 手动移除跳过名单**：自动同步**只加不删**——B 站取关的 UP 不移出白名单（白名单是用户自管的集合，取关 ≠ 想删白名单）。反向保护：用户**手动从白名单移除**的 UP（UP 管理页移除 / UP 详情页「取消关注」两处）记入本地跳过名单（`SharedPreferences`），即便该 UP 在 B 站仍被关注，自动同步**不会把它悄悄加回**（手动优先于自动跟随）；重新手动加入后正常，之后再次移除会重新记录
- **静默原则**：成功/失败/跳过都**不弹窗不打扰**，只打 `[followings-sync]` debug 日志；任何异常兜底收敛为结果返回，不向上抛
- **「导入后白名单只几个/为空」排查修复（v2.17.13）**：代码审查确认 v2.17.12 链路（分页 `pn*ps<total` + 上限 200、addBatch 查重合并、Gist/缓存双写、UP 管理页全量 ListView 显示）逻辑自洽、单测覆盖；发现一个真实解析容错缺口——`fetchFollowingsOfMine` 单项解析的 `mid` **只接受 `num`**，若 B 站某时期把 mid 返回为**数字串**（B 站多接口已字符串化 id），整批关注会被当"缺 mid 脏条目"丢弃 → 表现为导入后白名单几乎没 UP。**修复：mid 解析 num/数字串兼容**（非法串按 0 由上层过滤），补单测。其余可疑点（登录账号与预期不符、真实关注确实少、Gist 读取回退）无法在无真实登录态下复现，需真机确认（见下）
- 测试：新增 `followings_auto_sync_test.dart` 18 例——纯决策 plan（未登录/未配置/10 分钟内节流带剩余时间/超窗与从未同步就绪）、纯查重 filterNewFollowings（白名单/跳过名单/无效/内部重复）、服务 syncOnce（未登录不发请求 / 未配置不发请求 / 首次 45 关注 3 页全量加入且只写一次 Gist+缓存并记时间戳 / 增量只加新 5 且整页已知提前停（2 页）/ 无新增不写盘仍记时间戳 / 二次启动节流不发请求 / 跳过名单不加回 / 中途页失败用已拉部分 / 拉白名单失败跳过）+ rememberManualRemoval 幂等；followings 接口补 mid 数字串解析用例；`flutter analyze` 0 issue、全量 `flutter test` 通过（813 例）
- 验证：debug APK 装机模拟器——未登录/未配置启动**静默跳过**（logcat 仅 `[followings-sync]` 跳过日志、无关注/白名单网络请求）；已配置 + 注入登录态后的真实同步链路（真实关注拉取/增量入白名单）需**真机登录**验证（模拟器无法收短信）

---

## v2.17.12 (2026-09-09)

**关注 UP 体系：App 内统一「关注 = 加入白名单 UP 主」+「导入我关注的 UP」批量加入（B 站关注列表 → 白名单，一个方向）**

- **B 站「我关注的 UP」接口（`app/lib/api/bilibili_api.dart` 新增 `fetchFollowingsOfMine`，v2.17.12）**：`x/relation/followings?vmid=<自己的 mid>`（需登录：无 SESSDATA 抛 -101「请先登录」；有 SESSDATA 但失效 → nav 拿 mid 抛 -101「登录已失效」）——mid 走 `_ensureMyMid`（nav 会话缓存，同收藏夹导入）；`pn/ps` 分页，解析 `data.list[]{mid/uname/face}` 转 [Upowner]（face `//` 补 https:；缺 mid 脏条目过滤；total 数字串兼容，负数/缺失按 0）；错误分类 -101/-412/-352/其他业务码 → [BiliApiException]、网络 [DioException] 上抛。**字段验证说明**：2026-09 实测本机匿名请求该接口一律返回 -101「账号未登录」（关注列表现在属个人账号数据，无法匿名看结构）——字段形态按 bilibili-API-collect 文档口径 + 防御解析实现；登录态真实列表需真机验证（模拟器无法收短信登录码）
- **「导入我关注的 UP」入口（新 `app/lib/pages/followings_import_page.dart`，UP 主管理页顶部第二按钮）**：首页 UP 主管理页（主页左滑第 2 页）标题下方新增「导入我关注的 UP」按钮（与「搜索 UP 主」并排）——点击走 [runFollowingsImportFlow]：**配置门禁**（未配 GitHub token/gist_id 先提示，不发请求）→ **登录门禁**（无 SESSDATA 提示「关注列表属于个人账号数据」并引导登录，登录成功继续/保持匿名中止）→ **勾选页**：页头「B 站关注共 N 位 · 已加载 M」（关注很多只拉**前 200 位**，超出提示「其余请分批搜索加入」——取舍：避免为全量关注反复请求触发风控）、列表行 = 勾选框 + 名字 + mid（已在白名单的关注项直接标「已关注」灰色不可选）、底部「全选/取消全选」+「加入白名单（已选 K）」、「加载更多」翻页（20 条/页，翻完提示已加载全部/已达上限）
- **批量加入只写一次盘（`UpownerWriter.addBatch` 新增）**：一次拉 Gist → 入参去重（内部重复/无效 mid 剔除）→ mid 查重（已在白名单跳过，计入 skipped）→ 合并 **一次 saveToGist → 一次写本地缓存**（非逐条 add 的 N 次拉/写）；全部已存在不发写请求；结果 [UpownerBatchResult]（ok/data/added/skipped），UI 汇总「已添加 X，跳过 Y（已在白名单）」；写入成功返回上一页自动刷新管理列表（pop(true) → onDone → 首页 `_load`）
- **App 内关注/取关（`app/lib/pages/upowner_page.dart` 顶部按钮统一文案）**：UP 详情页 AppBar 从「仅白名单时移除图标」改为**常驻「关注 / 已关注」文字按钮**（未关注：FilledButton.tonal「关注」→ `UpownerWriter.add` 加入白名单，成功变「已关注」+ snack「已关注：xx」；已关注：点按弹确认「取消关注 = 从白名单移除该 UP 主，不影响已加入视频，可随时重新关注」→ `removeByMid` 移除变回「关注」，留在本页可再关注）——**播放页 UP 入口 / 评论 UP 链接 / 搜索 UP 结果 / 白名单管理页**等一切进 UP 详情页的途径都可用；页面内改过关注状态后返回 pop(true)（PopScope 带返回值），上层刷新白名单快照（搜索页回跳刷新「关注」按钮与白名单 Tab）。搜索 UP 结果按钮文案由「加入/已加入」统一为**「关注 / 已关注」**（UpownerTile）；管理页副标题同步说明「关注 UP 主 = 加入白名单」
- **真实写入边界**：关注/取关/批量导入都要写 Gist——模拟器上未配置真实 GitHub token/gist_id，点击均走「请先到右上角管理入口配置…」门禁提示不落盘（配置后真机即可用）；**upowners 落库（Gist 内容 + 本地缓存）由 widget 测试用内存 GistApi + 记录型假同步服务验证**
- **反向同步取舍**：App 内关注/取关**只写白名单 Gist，不回写 B 站关注关系**（`x/relation/modify` 是官方账号操作、需 csrf 且属敏感动作，第三方客户端不代写）——「导入我关注的 UP」也只做 B 站 → 白名单单方向；若日后要双端一致需另行评估官方 OAuth/接口策略
- 测试：`fetchFollowingsOfMine` 单测 9 例（登录门禁不发请求 / nav mid 缓存与 vmid/pn/ps 参数 / face 补 https + total 字符串 + 脏条目过滤 / hasMore total 在场与兜底两分支 / list 缺失空页 / -101/-412/-352/其他业务码 / nav -101 / 网络 DioException）+ `UpownerWriter.add/addBatch/removeByMid` 单测 9 例（关注与取关文案、批量一次写盘、内部去重、已在跳过计数、全跳过不写盘、未配置门禁）+ UP 详情页关注按钮 widget 测试 5 例（关注写 Gist+缓存变已关注 / 取消关注确认移除回未关注 / 弹窗取消不变 / 未配置提示 / 返回 pop 结果）+ 导入流程 widget 测试 4 例（配置门禁 / 登录门禁中止 / 全选批量加入汇总与 Gist+缓存 / 翻页加载更多）；`flutter analyze` 0 issue、全量 `flutter test` 通过（797 例）
- 验证：debug APK 装机模拟器 uiautomator dump——UP 管理页出现「导入我关注的 UP」按钮、点击提示配置门禁；搜索 UP 结果行「关注」按钮、非白名单 UP 详情页顶部「关注」按钮可见可点（无配置不落盘、状态不变）；真实关注列表导入（登录态）需真机验证

---

## v2.17.11 (2026-09-09)

**收藏夹内搜索：夹内视频页加搜索框，输入自动拉全夹后本地按标题 / UP 主过滤（收藏夹直看补齐「找得到」短板）**

- **夹内搜索框（`app/lib/pages/favorite_videos_page.dart`，v2.17.7 收藏夹浏览第三层内）**：AppBar 下新增搜索框（放大镜图标 + hint「在收藏夹中搜索」+ 右侧清空按钮，输入防抖 400ms）——夹内条目来自 `fetchFavoriteVideos` 分页，B 站侧接口**没有夹内关键词搜索**，故采用 **「输入 → 自动翻页拉全夹（fetchFavoriteVideos 翻到 hasMore=false，按 bvid 去重）→ 本地过滤」** 路线：拉取期间列表区显示进度（转圈 + 「正在读取收藏夹全部视频（第 N 页）…」）；拉完按关键词本地过滤（新纯函数 `filterFavoriteVideosByKeyword`：**标题或 UP 主名**包含关键词、忽略大小写，对齐白名单 Tab 搜索语义）即时展示匹配列表，**跨页条目也能搜到**；点结果照常补 cid 播放（同浏览）
- **清空恢复分页浏览（取舍）**：点清空按钮 / 删光关键词 → 退出搜索恢复原分页浏览——**若已拉过全量，把整夹一次并入列表直显**（数据已在内存，不再需要上拉翻页，见底部「没有更多了」）；未拉过全量则保留原分页状态（滚到底继续从服务端拉）。搜索缓存留在内存：再次输入同一收藏夹关键词**直接本地过滤、不重复拉取**
- **交互细节**：输入防抖只管「何时开始拉全量」——本地过滤零成本、已缓存时逐字即时过滤，不会因边输入边拉触发重复翻页；搜索中**下拉刷新 = 重拉全量再过滤**（分页浏览时下拉刷新仍为清空重拉第一页，同时作废搜索缓存）；防抖窗口内 / 拉取中重复输入只更新过滤词，不重复发起；翻页循环带代际号，清空 / 刷新 / 页面销毁即放弃在途请求（不把过期结果串入新状态）；**无匹配提示「未找到匹配的视频」**；**夹内 >500 条（服务端 totalCount）搜索前 snack 提示「拉全量可能稍慢」仍继续**（取舍：夹内搜索必须见过全部条目才能保证匹配不漏，一般收藏夹数量可控；超大夹可接受进度等待或用清空中断）
- **失败兜底**：拉全量中途失败（风控 -412 / 网络 / 登录失效 -101）→ snack 区分提示并**退出搜索回到分页浏览**（已加载的浏览列表保留，不弹整页错误）；空夹 / 未登录 / 首屏错误等整页状态沿用原视图（无内容可搜，不显示搜索框）
- 测试：过滤纯函数 5 例（空白原样 / 标题子串 / 大小写不敏感 / UP 主名匹配 / 无匹配空列表）+ 夹内搜索流程 widget 测试 4 例（防抖窗口内不拉全量、到期自动拉全夹跨页匹配、点结果补 cid 播放回调；清空后整夹直显且无新增请求；无匹配提示；拉全量中途 -412 snack + 退出回浏览）；`flutter analyze` 0 issue、全量 `flutter test` 通过（770 例）
- 验证：debug APK 装机模拟器 uiautomator dump——夹内页搜索框出现、输入关键词自动拉全夹过滤（仅匹配项在列表）、清空恢复整夹浏览、点结果进入播放页

---

## v2.17.10 (2026-09-09)

**观看统计页重做：GitHub 官方样式克莱因蓝热力单张大图 + 总览移到热力下方 + 点日期格进当天观看历史 + 设置区内联页底（与首页齿轮共用组件）**

- **热力图重做（仿 GitHub 官方 contribution 常见样式）**：近 53 周**单张大图**（今天在最右列）、**圆角小方块**格子（圆角 3px、格间距 2px）、主色 **克莱因蓝 #002FA7 + 白**——无观看 = 浅近白底 `#EBEEF5`，有观看按分钟数 5 档蓝阶（<5 分 `#C7D8FF` → 5-15 `#7FA6FF` → 15-30 `#3D6EFF` → 30-60 `#1546C8` → ≥60 分克莱因蓝 `#002FA7`）；配色抽成常量 + 纯函数 `heatColorForLevel`（供渲染与单测共用）；保留月份标签 + 少→多图例；手机窄屏横向滑动看更早的周并**默认锚定到最近端（今天可见）**，平板等宽屏整图直接放下不滚动；今天的格子加克莱因蓝描边
- **总览统计移到热力下方**（原 2×2 总览卡在热力上）：观看热力 → 观看总览（今日 / 本周 / 累计 / 最长连续天 + 副信息「N 天有观看记录 · 平均每天 X 分钟」）→ 设置区，同一滚动页
- **点日期格进入当天观看历史（新 `app/lib/pages/daily_history_page.dart`）**：点任意日期格 → push 该日历史页，列出**那一天**的观看记录（按本地日期过滤 [HistoryStore]，复用抽取出的公共条目组件 [HistoryTile]），点击条目**续播**（同历史记录页模式：构造视频推播放页、进度自动恢复）；当天无记录显示空态「该日无观看记录」；格子带无障碍语义标签（日期 + 观看时长，读屏/自动化可用）
- **设置区内联统计页底部**：首页齿轮的管理面板内容抽取为公共组件 **`lib/widgets/manage_panel.dart` 的 [ManagePanel]**（B 站账号 / GitHub 配置 / 新建合集 / 合集管理 / 离线缓存 / 翻译服务 / 版本更新分区原样保留）——**首页齿轮弹层与统计页底部内联共用同一组件**（弹层模式点「登录 / 检查更新」先关面板再动作，`closeBeforeNavigate` 区分；统计页内联以「设置」为题嵌入滚动页底，登录入口在统计页也可直达）
- 历史记录条目组件抽为公共 [HistoryTile]（`lib/widgets/history_tile.dart`）：历史页与该日历史页共用，样式与交互不变（封面/标题/进度/相对时间、点击续播；删除入口仅历史页传 [onRemove] 时显示）
- 测试：统计页配色/顺序/点日进历史/设置区内联 widget 测试、该日历史页过滤纯函数 + 页面（只列当天/空态/点击跳播放页）、ManagePanel 抽取后渲染/保存配置/内联不 pop 测试（16 新增）；`flutter analyze` 0 issue、全量 `flutter test` 通过（761 例）
- 验证：debug APK 装机模拟器 uiautomator dump——统计页单张大图（月份标签/图例/今天格语义）、总览在热力下方（y 坐标序）、点有观看的日格 → 该日历史页列出当天条目、滚动到底设置区（「设置」+ GitHub 配置等分区）、首页齿轮弹层回归（「管理」面板各分区）

---

## v2.17.9 (2026-09-09)

**观看统计页：真实观看时长按天本地记录 + GitHub 风格蓝色热力图 + 总览（防沉迷数据可视化）**

- **真实观看时长记录（新 `app/lib/services/watch_stats.dart`）**：播放页按 **真实播放秒数** 累计——每 500ms tick 取一次位置，仅当 **playing 且位置增量 `0 < Δ ≤ 5s`**（连续前进）才累计：暂停 / 缓冲停住（Δ=0）、快退（负）、快进 3 秒 / 拖动 seek / 断点恢复 / 评论 ?t 定位（Δ 超阈值，且 seek 后重置累计基线防小跳误计）都不算观看；**听视频（纯音频）模式照常计入**（播放器位置照常前进）；屏幕熄灭后 Dart tick 被系统挂起 → 期间不计（取舍：以「位置真实前进」为口径，不做壁钟累计）。累计走内存，攒够 ~10s 或退出播放页时批量写 shared_preferences（不每 tick 写盘）；按**本地日期** `yyyy-MM-dd` 一天一值（跨日看自动落新一天），**保留近 400 天自动裁剪**，数据损坏 / 脏键容错视为空不崩溃。数据**仅本地、不入 Gist、不跨设备**（观看统计属个人隐私，与白名单云端同步体系隔离）
- **播放页接入**：`_tick` 增量累计 + seek 出口（快进/快退/进度条/横屏拖动/链接定位）与 `onPrepared`（新流）统一重置基线；dispose 兜底落盘
- **观看统计页（新 `app/lib/pages/watch_stats_page.dart`，主页 PageView 第 4 页 / index3）**：主页左滑两页到（主页 → UP 主管理 → 统计），顶栏新增 **「观看统计」图标直达**（animateToPage，tooltip），底部提示语更新为「左右滑动：历史 / 合集 / UP 主 / 统计」。页面布局：
  - **总览卡 2×2**：今日观看 / 本周观看（周一起算）/ 累计观看 / **最长连续观看天数**（streak），下方副信息「N 天有观看记录 · 平均每天 X 分钟」
  - **GitHub 风格热力图（蓝色主色）**：近 **53 周**网格（行 = 周一..周日、列 = 周，**今天在最右列**，未来格子留空）；格子按当天观看分钟分 5 档**蓝阶**（0=浅灰无观看，<5 分浅蓝 `#D5E5FF` → ≥60 分深蓝 `#0B5FFF`）；**点/长按格子**在底部详情条显示「yyyy-MM-dd · 观看 xx 分钟」；**月份标签**（列上方 x 月）+ **少→多图例**（档位说明）；热力可**横向滑动**看更早的周（左侧周几栏固定）；无数据显示空态「开始观看后这里会生成你的观看热力」
  - 统计页与播放页共用 [WatchStats] 单例；切到统计页时重读（同历史页约定）
- 测试：WatchStats 单测 21 例（累计纯函数排除跳变 / watchLevel 蓝阶分级 / 跨日 clock 注入 / streak / 400 天裁剪 / 损坏容错 / 持久化），统计页纯函数单测（53 周网格行列与今天落位、level 换算、月份标签、时长文案）+ 页面冒烟（空态 / 有数据渲染热力卡）；`flutter analyze` 0 error、全量 `flutter test` 通过（744 例）
- 验证：debug APK 装机模拟器真实播放约 60s（非 seek）→ 退出后统计页今日时长 ≈60s、热力今天格上色（日志 + UI dump 取证）

---

## v2.17.8 (2026-09-08)

**UP 主页接口修复：粉丝数读不到 + 进 UP 主页频繁「网络请求失败」（B 站 space wbi 接口风控与字段变化的兼容修复）**

- **粉丝数读取修复（根因）**：UP 主详情接口 `x/space/wbi/acc/info` 的 `data` **不含 `fans` 字段**（2026-09 匿名/登录态双实测确认：成功响应仅 name/face/sign/level 等，无 fans、无 card）——旧解析 `data['fans']` 恒为 null，粉丝数永远显示「—」。粉丝数改用 **`x/relation/stat?vmid=`（`data.follower`）**（B 站网页 UP 主页同源）：新增 `fetchUpownerFollower`（匿名实测稳定、错误分类与既有接口一致），页面层粉丝数独立于资料拉取——资料接口被风控时粉丝数照常显示
- **自动重试（吸收「重试几次后正常」）**：`acc/info`、`arc/search` 等 space wbi 接口对匿名/高频请求有**间歇风控**（-352/-412，实测等待 1~3s 后重试即恢复）——UP 主页进入时并发 3 个请求无任何重试，接口偶发被拦即整页「网络请求失败」。页面层新增**失败自动重试（退避 1s → 2s，最多 3 次尝试）**：`_loadInfo`（资料 + 粉丝数各带重试）、视频列表（指数退避，重试期间保持转圈不落错误态）→ 仍失败才显示既有整页错误 + 「重试」按钮兜底；重试不并发重复（`_loadingInfo`/`_loadingMore` 守卫 + 列表代际号 `_listGen` 防过期结果串入）、页面销毁即放弃、不无限循环
- **信息会话级缓存**：UP 主资料 + 粉丝数成功结果按 mid 会话级缓存（静态 `_upInfoCache`），**重进同一位 UP 主主页直接显示、不再请求** space wbi 接口（少请求即少触发风控）；缓存恰好缺粉丝数（上次 stat 失败）时补拉一次 stat
- **降级不阻塞**：资料接口（acc/info）匿名被 -352 拦截 → 自动重试后静默降级——粉丝数（stat 成功）照常显示、名字/头像回退 initial（搜索结果预填）或**视频列表作者名兜底**（标题/头像不全空），不弹整页错误
- **列表总数解析修正**：`arc/search` 的总数实测在 `data.page.count`（`list.count` 不存在）——旧解析恒 null、hasMore 只能「装满 20 就下一页」，**末尾必多打一次空页**（多余的风控触发面）；改为 page.count 优先、list.count 兜底，UP 主视频恰 20 条时不再多发空请求
- 错误分类保留：-412（风控）/ -352（限流）提示「请稍后再试」，网络失败提示检查网络

## v2.17.7 (2026-09-08)

**收藏夹浏览入口：首页「收藏夹」卡 → 我的收藏夹列表 → 夹内视频直接点播（白名单外可播，无需先导入）**

v2.17.5 打通了「收藏夹 → 白名单」的**导入**方向；本版补上**浏览**方向——不导入、不改白名单，登录后把收藏夹当「一个虚拟合集」直接看：首页合集区顶部新增固定的「收藏夹」卡（第一层）→ 我的 B 站收藏夹列表（第二层）→ 点某收藏夹看它的视频列表并可**直接播放**（第三层）。与白名单/UP 主页/搜索同一套「白名单外可播」模式：不写 Gist、不自动入库。

- **首页固定「收藏夹」卡（`app/lib/pages/playlist_page.dart`）**：合集区顶部（缓存栏下方）常驻一张入口卡，样式同合集卡但视觉可区分（folder_special 图标 + tertiary 渐变 + 副标「我的 B 站收藏」）；**不随合集列表滚动、空名单/未同步时也显示**。点击 → 登录门禁：收藏夹属个人账号数据——无 SESSDATA 先 snack 提示并引导登录（复用首页登录入口，登录成功才进总览）；会话已失效则由总览页内 -101 引导重登兜底
- **收藏夹总览页（新 `app/lib/pages/favorites_page.dart`，第二层）**：AppBar「收藏夹」；列表 = `fetchMyFavorites`（封面 / 名称 / 「N 个视频」，样式同导入弹层）；下拉刷新；错误可重试（风控 -412 / 其他 / 网络区分提示）；空态「还没有收藏夹」；未登录 / 登录已失效（-101）→ 整页「去登录」引导（登录成功自动重载）
- **夹内视频页（新 `app/lib/pages/favorite_videos_page.dart`，第三层）**：`fetchFavoriteVideos` 分页列表（上拉加载更多，hasMore 权威；行复用 [VideoTile]：封面 / 标题 / 时长 / UP 主 / 发布时间）；**点视频直接播放**——夹内条目无 cid → 先 `fetchVideoMeta` 补全（cid/pages/desc/owner，多 P 视频选集能力随之生效）→ 构造完整 WhitelistVideo → push 播放页；进度/历史等播放页常规能力正常，**不入白名单**；失效条目（view 62002）snack 提示并跳过不打断浏览；下拉刷新；空态/错误/登录失效（-101）均有整页状态与重试/去登录
- **取舍说明**：
  - **「收藏的合集」暂不展示**：收藏夹 `resource/list` 里的非视频条目（type≠2：音频/专栏/合集/剧集等）沿 API 层过滤被跳过，夹内视频照常显示；若某收藏夹全是这类收藏，视频页会显示空态并注明「非视频内容本版暂不展示」。接入「B 站合集收藏」浏览需另一套资源语义（resource/list 的 id_type 区分或独立合集收藏接口），留后续
  - **夹内视频仅观看、无「加入白名单」长按**：批量加入已有 v2.17.5 收藏夹导入、单条加入可用导入/搜索入口，本页保持纯浏览（防沉迷原则不变）
- 验证：新页面/首页入口 widget 测试覆盖（收藏夹卡存在与点击、未登录引导、总览与视频页数据渲染、点视频→补 meta→播放回调、62002 跳过）；`flutter analyze` 0 error、全量 `flutter test` 通过；debug APK 装机模拟器实测首页卡与未登录引导（模拟器无法收短信验证码，登录态真实链路需真机验证）

---

## v2.17.6 (2026-09-08)

**评论视频链接带进度跳转：正文贴 `?p=` / `?t=` 分享链接可跳到指定分 P 与进度**

B 站评论**没有专用的「跳转进度」标签**（web 评论仅纯文本链接），评论里想指到「某视频某分 P 的某个时刻」时，惯例是贴一条带 `?p=`/`?t=` 的完整分享链接（web 播放器直接支持这两种参数）。本版让 App 端评论链接同样认这两种参数：点击后**先跳对应分 P、再定位到指定进度**（覆盖该集的记忆进度——「链接定位」语义优先于「历史续播」）。

- **链接解析（`app/lib/utils/comment_links.dart`）**：新增纯函数 `parseVideoLinkPosition(url)`——解析完整视频 URL 的 `?p=<分P号 1起>` 与 `?t=<秒>`，输出 `pageIndex`（0 起）/ `positionMs`（毫秒）；`CommentLink` 新增这两个字段（video/b23 分类携带，裸 BV / 无参数链接 = null 维持旧行为）。**`t` 支持格式**：纯数字秒（整数 / 小数，如 `129`、`129.0`）与带单位 `Xs` / `XmYs`（如 `30s`、`2m5s`=125s，分/秒均可小数）——`mm:ss` 冒号与 `XhYmZs` 等其余格式**不支持**（保守按无 t 处理，避免错误定位）。`p`/`t` 越界不在解析层钳制（解析时不知道实际 pages），由播放端按实际集数/时长兜底。b23 短链：点击 resolveShortLink 后按**最终落点 URL** 再分类，天然拿到落点的 `p`/`t`。单测覆盖全部格式与非法/越界边界
- **播放页初始定位（`app/lib/pages/player_page.dart`）**：`PlayerPage` 新增可选 `initialPositionMs`——>0 时首次 onPrepared **直接 seek 到该位置并覆盖该集记忆进度**（不弹「已从上次…继续」）；null/<=0 保持原记忆进度恢复。内部引入一次性 `_pendingSeekMs`（用掉即清）：同视频跳分 P（`_switchToPage` 增可选 `seekMs`）、播放器未就绪时的入队定位都走它，切集/内部换源会显式清除防过期定位串台
- **评论点击分发（`app/lib/widgets/comment_list.dart` / `app/lib/pages/comment_page.dart`）**：`onOpenVideo`/`onNavigateToVideo` 回调新增 `pageIndex`/`positionMs` 命名参数（typedef `OpenCommentVideo`）；`_previewVideo` 把链接携带的 p/t 一路透传（b23 落点同）。分发语义（`openVideoInNewPlayer`）：
  - **带 p/t** = 明确的「跳到该视频该分P该时间」：**同 bvid → 本页内跳**（目标集=当前集 → 直接 seek t；其他分 P 且本页 pages 覆盖 → `_switchToPage` 切集 + 定位；pages 覆盖不了 → 兜底叠新页）；**异 bvid → 叠新播放页**并传 `initialPageIndex`+`initialPositionMs`（新页首次定位覆盖其记忆进度）
  - **无参数**：完全维持 v2.17.1 行为（同 bvid 正播第 1 集跳过、其余 push 新页从开头播）
  - **取舍**：同 bvid 带参用「本页内跳」而非叠新页——返回键语义仍是「回上一个视频」、省一个播放器实例（防双音轨机制只对叠页需要），且与选集 UI 的原地切集体验一致；无回调兜底 push 同样透传定位参数
- 验证：解析与分发逻辑由单测/行为测试覆盖（定位 seek 目标在 widget 测试中通过
  播放器通道日志断言：initialPositionMs → `seekTo 120000`、同 bvid 切分P →
  `seekTo 30000`、同集带 t → `seekTo 45000`，均覆盖对应集记忆进度）；debug
  APK 装机模拟器冷启动回归：进程正常、无崩溃/无 Dart 异常（真实评论里带
  `?p/?t` 链接样本难找，见 README 已知限制说明，点击链路由同一套分发代码承担）

---

## v2.17.5 (2026-09-08)

**B 站收藏夹 → 白名单互通（导入方向 MVP）：登录后把自己的收藏夹一键批量导入白名单**

「收藏夹与白名单互通」的导入方向打通——在 App 里登录 B 站账号后，可读取自己创建的收藏夹列表（默认夹/自建夹），选一个收藏夹把里面的视频**一键批量加入白名单**（查重跳过已在白名单的、失效稿件自动跳过）。反向（白名单新增自动收藏回 B 站）与双向自动同步超出本批次，见「取舍说明」。

- **API 层（`app/lib/api/bilibili_api.dart`）**：新增两个**需登录**的封装（登录态 Cookie 注入；接口均不需要 WBI 签名）：
  - `fetchMyFavorites()`：登录门禁（无 SESSDATA → 抛 -101「请先登录」）→ nav 接口拿自己的 mid（`data.mid`，会话内缓存）→ `x/v3/fav/folder/created/list-all?up_mid=<mid>&pn=1&ps=20` → 收藏夹列表（mediaId/title/mediaCount/cover，`media_id` 缺失时兜底 `id`、封面补 https、脏条目过滤）。**attr 位义未实测，不做过滤**（私密/默认夹等都返回，权限由 B 站接口控制）
  - `fetchFavoriteVideos(mediaId, {pn, ps})`：`x/v3/fav/resource/list?media_id=&pn=&ps=&platform=web` → 单页视频列表（bvid/title/cover/duration/pubdate/upName 雏形）+ totalCount + hasMore；type≠2（音频/专栏/剧集）与无 bvid 脏条目过滤
  - 错误分类与既有接口一致：-101（区分「请先登录」/「登录已失效」）/ -412 风控「请稍后再试」/ 其他业务码带接口 message / 网络 DioException 原样上抛
- **导入逻辑（`app/lib/services/whitelist_writer.dart`）**：`importFavoriteFolder(mediaId, folderTitle, {onProgress})` → 结果汇总 {total/added/skipped/failed/interrupted}：
  - 开头**一次**拉当前白名单 bvid 集合（失败不阻塞，addVideo 内部查重兜底）→ 翻页拉收藏夹全部视频（has_more 服务端给，防御上限 200 页）
  - 逐条：**bvid 查重跳过**（不发 view 请求）→ `fetchVideoMeta` view 复检补全 meta（cid/pages/desc/pubdate 等）→ 构造完整 WhitelistVideo → [addVideo] 写 Gist（再查重兜底 + 写本地缓存）
  - **失效条目**：view 复检失败 code 62002「稿件已失效」/ -404 已删除 → 计 failed 跳过、**不中断**其余导入；拉列表阶段失败（BiliApiException/DioException）在写任何东西前上抛
  - 逐条写盘失败不抛：中断并汇总（interrupted + interruptReason），已写条数见 added
- **UI（`app/lib/widgets/favorites_import_dialog.dart` + 首页）**：首页右上角「导入」对话框内新增「**从 B 站收藏夹批量导入**」入口（放导入入口而非管理面板——新增白名单语义与粘贴导入一致，管理面板保持「管理只允许合集/配置」的防沉迷边界）：
  - 流程：配置门禁（GitHub token/gist）→ **登录门禁**（无 SESSDATA → 提示「收藏夹导入需要登录 B 站账号」+ 引导进登录页；登录成功继续、保持匿名中止）→ 收藏夹列表弹层（loading / 失败重试 / 空态 / 列表展示封面+名称+数量）→ 选夹确认（提示夹内 N 个视频，说明自动跳过已在白名单/失效）→ 进度对话框「导入中 i/N」逐条写入 → 结果汇总 snack「已导入 X，跳过 Y（已在白名单），失败 Z」→ 刷新列表
- **取舍说明**：
  - **仅导入方向**：本批次做「收藏夹 → 白名单」批量导入（用户主动、确认式，符合防沉迷「新增须主动决策」原则）；反向（白名单新增自动收藏回 B 站）与双向自动同步是另一个方向（涉及 B 站写接口 + 双向状态一致性），不在本批次，后续可按需做
  - **单夹选择**：一次导入一个收藏夹（列表弹层选择后确认）；多收藏夹批量勾选导入留待后续（导入循环与进度已支持任意条数，扩展成本低）
  - **attr 不做过滤**：收藏夹 attr 位义未实测，全部原样列出由用户自己选（私密夹 B 站侧会校验登录态）
  - **失效条目不重试**：view 复检 62002/-404 计失败跳过；已失效内容 B 站收藏夹接口本身也不再返回
- **测试**：`test/favorites_api_test.dart`（登录门禁 / nav mid 解析与缓存 / list-all 参数与解析（media_id 兜底 id）/ resource/list 分页参数与解析 / -101 两种文案 / -412 / 其他业务码 / 网络）+ `test/favorites_import_test.dart`（导入核心逻辑：查重跳过省 view 请求、62002 失败不中断、拉列表失败未写 Gist、空夹；UI：未登录提示 + 引导、空夹弹层空态、首页入口存在与未登录引导）

## v2.17.4 (2026-09-08)

**UP 主主页「合集/列表」区（仿 B 站 UP 主页：展示该 UP 主的合集与列表，点合集看其视频，可播放/加入白名单）**

承接 v2.17.2（播放页 UP 主入口）。进入 UP 主详情页后，顶部新增「合集」区——列出该 UP 主的合集（season）与列表（series），点合集在其内浏览视频（点击播放 / 长按加入白名单），补充了 UP 主页的合集维度（此前只能看「全部视频」投稿流）：

- **API 层（`app/lib/api/bilibili_api.dart`）**：新增三个**匿名可用、无需 WBI 签名**的封装（带完整浏览器头 + buvid 指纹，与评论区接口同策略；登录态存在时照常注入 SESSDATA）：
  - `fetchUpownerCollections(mid)`（`x/polymer/web-space/seasons_series_list`）→ `UpownerCollectionsResult{seasons[], series[]}`，每项 `UpownerCollection{kind(season/series), id(season_id/series_id), name, cover, description, total, creator}`；`items_lists` 缺失 → 空结果（该 UP 主无合集非错误）；id/total 数字串（String）容错；脏条目（id≤0/空名）过滤；`creator='auto'` 的系列以 `isAuto` 标记（由页面层决定取舍，API 原样返回两类）
  - `fetchSeasonArchives(seasonId, {page})`（`x/polymer/web-space/seasons_archives_list`）→ `UpownerVideosPage`：`data.archives[]` **无 cid / 无 upper 名**（有 aid/bvid/title/pic/duration(秒)/pubdate），解析为 cid=0 的 WhitelistVideo（播放时 view 补齐）+ pubdate 记录，`page.total` 分页判断
  - `fetchSeriesArchives(mid, seriesId, {page})`（`x/series/archives`，参数 pn/ps）→ 同结构（`page{num,size,total}` 与 season 的 `page{page_num,page_size,total}` 字段不同，统一只取 total）
  - 错误分类与既有接口一致：-412 风控 / -352 限流 / 其他业务码带接口 message / 网络 DioException 原样上抛；code=0 无 data → 合集/系列视频接口按空页处理、合集清单接口抛「未返回数据」
- **页面（`app/lib/pages/upowner_page.dart`）**：顶部「合集」区（**只在拿到 ≥1 个合集/列表时显示**，无合集整区隐藏不占位、加载失败静默隐藏不影响主列表）——区头「合集」+ 横向 chips 行：第一个「全部视频」（默认，即原主列表），其后各合集/列表（season 名带「合集·」前缀、series 名带「 · 列表」后缀，均与 B 站展示一致）；点合集 chip → 下方视频列表**切换为该合集视频视图**（fetchSeasonArchives / fetchSeriesArchives 按类型自动选接口、独立分页滚动到底加载 20 条/页），此时**搜索框与排序 chips 隐藏**（搜索/排序仍只作用于「全部视频」原列表——合集视频按合集自身顺序展示）；点「全部视频」chip 切回原列表（搜索/排序恢复）。两个视图共用行组件与交互（点击 → 缺 cid 走现有 `fetchVideoMeta` view 补齐再进播放页；长按 → 「加入白名单视频 / 取消」）；切换视图自动滚回顶部；请求进行中切走 → 过期结果丢弃不污染新视图；按 bvid 去重防接口重复条目；滚动监听按当前视图分发翻页
- **取舍说明**：
  - **series `creator='auto'`（直播回放等系统自动生成列表）过滤不显示**：页面层把 `isAuto` 系列从「合集」区剔除（`fetchUpownerCollections` 原样返回、不丢数据）——与 B 站网页端 UP 主页一致：这类列表是系统按直播回放/视频自动归集的、非 UP 主动整理内容，放进来会污染该区（老番茄等 UP 的 auto 系列动辄十几个）；想看直播回放可回 B 站看
  - **合集/列表视频项无 cid / 无 upper 名** → 列表项 cid=0、upName 空串：点击播放复用现有「view 接口实时补 cid」流程（与 UP 主全部视频、信箱同款，实机验证取流日志带真实 cid）；UP 主页列表行本就不展示 upName，加入白名单走 view 补齐真实元数据
  - 合集/列表清单一次取一页（page_size=20，UP 主页一般 < 20 个；极端超 20 只显示第一页，注释已说明）
- **测试**：`UpownerPage` 增可选 `api` 注入参数（widget 测试注入 mock BiliApi）；新增 `app/test/upowner_collection_api_test.dart`（**17 个单测**：三个接口的 URL/参数构造、seasons_series_list `items_lists` 结构解析（seasons/series 分开）、meta 字段解析与 total/id 数字串容错、creator=auto → isAuto、脏条目过滤、空 items_lists、-412/-352/无 data 错误分类、seasons/series 视频 archives 解析（cid=0/duration 秒/pubdate/封面补全）与 page.total 分页、空页/脏 bvid 丢弃）；新增 `app/test/upowner_page_collections_test.dart`（**5 个 widget 测试**：有合集显示「合集」区 + chips（auto 系列被过滤）/ 无合集整区隐藏 / 点合集 chip 切换列表（搜索排序隐藏）+ 点「全部视频」切回恢复 / 点自建列表走 x/series/archives / 合集视频长按弹「加入白名单视频」菜单可取消关闭）
- **验证**：`flutter analyze` 0 issue；全量单测 **656 通过**（634 → 新增 22：API 17 + widget 5）；模拟器实测（真实网络 + 真实 App，匿名 720P）：首页合集卡「コデ」→ 串流教程视频（摄影师云飞）→ 播放页点 UP 主 → 进 UP 主页——**合集区出现**（uiautomator dump 文本证据：区头「合集」+「全部视频」chip +「合集·摄影师云飞的手机、平板测评」+「合集·智能手表」chips）；点合集 chip → logcat `fetchSeasonArchives season_id=754991`、下方列表切换为该合集视频（dump：搜索框/排序 chips 隐藏、9 个视频行）；点合集视频 → 进播放页 logcat `取流 bvid=BV11tbT6MEWv cid=41673623143`（合集接口视频无 cid，**真实 cid 由 view 补齐**）+ 720P DASH 取流成功 + 播放进度正常保存；返回后合集视图状态保留；集成测试 `integration_test/upowner_collection_flow_test.dart`（`flutter test integration_test/... -d emulator-5554`）真实网络通过：合集区出现 → 点「合集·摄影师云飞的手机、平板测评」→ 9 个视频行 → 点「全部视频」切回主列表（搜索/排序恢复）

---

## v2.17.3 (2026-09-08)

**视频简介（desc）：导入存储 + 播放页信息行显示 + 简介/评论过长折叠展开**

承接 v2.17.2（UP 主入口）。本版把「视频简介」接入全链路——白名单数据模型加 `desc` 字段并随导入写入（普通视频导入 + 油猴脚本），播放页竖屏信息行在标题/UP 主下方显示简介（过长折叠），评论区正文过长也折叠（评论/简介长文都支持「展开 / 收起」）：

- **数据模型 `WhitelistVideo.desc`（`app/lib/models/whitelist_video.dart`）**：新字段 `desc`（String，默认空串），视频级（多 P 视频简介不分 P、各分 P 共享）；`fromJson` 缺失/脏类型 → 空串不崩，`toJson` 非空才输出（无简介/旧数据不回写多余字段，与 epId/pubdate 约定一致），`copyWith` 沿用原值（合集移动/重排不丢简介）。兼容旧数据：无 desc 条目照常解析
- **导入路径写入 desc**：
  - **普通视频**（`app/lib/services/whitelist_writer.dart` `videoFromMeta`）：写 view 接口 `data.desc`（含 `\n` 换行原样；非 String 脏类型 toString 容错、缺失 → 空串）
  - **番剧/电影**（`videoFromPgcEpisode`）：**简介留空**（取舍说明见代码注释）——pgc 简介是**季级**字段（整季一段简介，不是每集一段），而番剧导入是**逐集**写 WhitelistVideo（一集一条），按季复制会污染每集且季简介更新要批量改；播放页简介区在 desc 为空时不显示也不占位，观感无缺口。「季级简介」留待后续（季导入入口把 season 简介存合集级/单独字段再按 seasonId 取）
  - **油猴脚本**（`bili-whitelist.user.js` v2.3.2 → **v2.3.3**）：`fetchVideoInfo` 两路（页面 `__INITIAL_STATE__.videoData.desc` / view API `data.desc`）都写 desc；parse/build 依旧整条透传/整对象序列化——新条目含 desc 随合并写回不丢、旧条目原样保留（遵循历史「upowners/collections 丢失」防丢字段教训，视频条目不做字段级重建）。node 逻辑自测：videoData 分支（零请求）/view API 分支写 desc、旧条目透传均通过
- **播放页信息行简介区（`app/lib/pages/player_page.dart` `_buildVideoInfoBar`）**：标题/UP 主行下方显示简介（小字灰色、紧凑）。数据优先 `WhitelistVideo.desc`；为空（旧数据/评论链接现构视频）→ **运行时补拉**：搭 UP 主元数据那次 `fetchVideoMeta` 的顺风车取 view `data.desc`（零额外请求；纯函数 `viewDescOf` 解析；结果按 bvid 会话内缓存 `_viewDescCache`，换源复位、按 bvid 对账防串台）。desc 为空（无简介/番剧季级取舍/拉取失败）→ **不显示简介区、不占位**。长简介超 3 行折叠省略 + 「展开」点击看全文、「收起」复原；展开态封顶高度内可滚动（防超长简介把固定信息行撑爆布局，`_descMaxExpandedHeight` 按屏高/视频区高动态取值）
- **折叠组件 `ExpandableText`（新 `app/lib/widgets/expandable_text.dart`，播放页简介与评论正文共用一份折叠逻辑）**：按**行数**折叠（LayoutBuilder 拿可用宽 → TextPainter 按 foldLines 布局 → didExceedMaxLines 超行即折叠，换行/宽字符/字号按真实排版算，比字符数阈值准）。纯文本形态（完整态 SelectableText 保选择复制；折叠态 Text+ellipsis）与富文本形态（链接混排，折叠/展开 Text.rich，识别器复用安全）都支持；`copyTip` 长按整段复制兜底、`maxExpandedHeight` 展开封顶内部滚动可配
- **评论区正文折叠（`app/lib/widgets/comment_list.dart`）**：`_LinkifiedBody` 改走 `ExpandableText`——评论正文超 5 行折叠 + 「展开」，点击展开全文 + 「收起」复原；纯文本短评保持原 SelectableText 选择/复制交互；**与链接渲染共存**：正文含链接时折叠态 Text.rich 截断、展开恢复完整链接混排（链接可点、URL 尾随标点裁剪语义不变），折叠态/富文本整段长按复制兜底保留。展开状态按条存在组件 State 内：同屏父级重建不丢；滚出 ListView 视口销毁后重折叠（简单方案，注释说明）
- **验证**：`flutter analyze` 0 issue；全量单测 **634 通过**（620 → 新增 14：模型 desc 序列化/脏类型/往返/copyWith 5、`videoFromMeta` 写 desc/脏类型容错 + `videoFromPgcEpisode` desc 留空 3、`viewDescOf` 2、`ExpandableText` 折叠展开 widget 测试 4——纯文本短文不折叠/长文展开收起/富文本链接混排折叠/封顶滚动）；模拟器实测（匿名 720P）：旧数据（无 desc）播放正常、信息行无简介区不占位（dump 证据）；真实 view 接口拉取正常（logcat `UP 主信息 mid=…`）；简介折叠交互由 `flutter test integration_test/desc_fold_flow_test.dart -d emulator-5554` 在模拟器真实运行验证（条目标注 desc → 折叠「展开」→ 全文「收起」→ 复原；旧数据无 desc → 运行时补拉显示简介区；无简介视频不占位）
- 说明：本机模拟器匿名登录态下 Gist 写入/大会员取流受环境限制，导入写 Gist 链路未在模拟器端到端复测（由单测 + 油猴 node 自测覆盖写入 JSON 含 desc 与透传），播放页简介显示链路已用「运行时补拉」等价路径实机验证（同一 `ExpandableText` 与简介区代码路径）

---

## v2.17.2 (2026-09-07)

**播放页 UP 主入口（阶段 C 收尾：竖屏信息行 UP 主区仿 B 站——头像 + 名字进 UP 主页）**

播放页竖屏大重构三阶段收官（A 竖屏布局 → B 跳转导航 → C UP 主入口）。本版把 v2.17.0 信息行里 UP 主名文本占位升级为可点的 UP 主入口，**不扩大 WhitelistVideo 模型**（白名单数据 / 导入 / 油猴链路零改动），mid/头像运行时补齐：

- **UP 主区（`app/lib/pages/player_page.dart` + 新组件 `app/lib/widgets/upowner_badge.dart`）**：普通视频信息行显示**圆形头像（32px，UA/Referer 防盗链头 + 失败/空 → 首字圆形占位）+ UP 主名**（可点、紧凑不喧宾夺主）。mid/face/真名由运行时 `fetchVideoMeta(bvid)` 取 view 接口 `data.owner{mid,name,face}` 补齐（纯函数 `parseViewOwner` 解析，防御脏数据；结果按 bvid **会话内缓存** `_upMetaCache`，多播放页/多 P 切集不重复请求；换源 `playVideo` 后复位重拉、按 bvid 对账防串台）；真名拉取成功后覆盖 up_name 展示
- **点击分发**：有 mid → 进 `UpownerPage(mid, initial: Upowner(预填头像名))`——**push 前自动暂停本页并保存进度**（复用阶段 B 的让路机制），从 UP 主页返回时 `didPopNext` 恢复续播，且从 UP 主页再点开视频不会双音轨；mid 未取到/失败 → 名字照常显示、点击 SnackBar「无法获取 UP 主信息」，不进页
- **番剧 / 电影（带 epId）取舍（弱化）**：pgc 内容挂靠官方/搬运号，无 UP 主页点播价值且易误导——**不拉 owner、信息行显示剧集标签**（导入的 up_name，空则「剧集」）、**不可点**；旧版导入的无 epId 番剧数据无法区分，走普通视频路径（行为按 view owner 实测，属已知边界，注释已说明）
- **验证**：`flutter analyze` 0 issue；全量单测 **620 通过**（609 + 新增 11：`UpownerBadge` 渲染/占位/点击分发/纯展示 5 个 widget 测试 + `parseViewOwner` 解析 6 个单测）；模拟器实测（匿名 720P）：播普通视频 → 信息行 UP 主区拉取成功（logcat `UP 主信息 mid=… name=摄影师云飞`）、点击进 UP 主页 mid/名字一致（头部预填即时显示，acc/info 匿名被 -412 限流属环境条件）、返回续播恢复；番剧集（epId）→ 不拉取 owner、信息行剧集标签、点击无跳转（按设计）

---

## v2.17.1 (2026-09-07)

**评论视频链接跳转可返回（阶段 B：push 新播放页 + 暂停旧页 + 返回续播）**

承接 v2.17.0（阶段 A：竖屏视频置顶 + 内嵌评论）。本版把「评论区点视频链接」从 **v2.16.23+ 的当前播放页换源（playVideo）** 改为 **push 新播放页**——跳转后可返回，返回时回到上一个视频继续播放（无双音轨）：

- **路由可见性**（`app/lib/main.dart` + `app/lib/pages/player_page.dart`）：`main.dart` 挂全局 `RouteObserver<ModalRoute<void>>`（`routeObserver`，导出）；`PlayerPage` 实现 `RouteAware`——`didChangeDependencies` 订阅 / `dispose` 退订，`didPopNext`（本页重新成为顶层）恢复续播；push 新 `PlayerPage` 的路由统一带 `RouteSettings(name: 'player')`（`kPlayerRouteName`），播放页内评论跳转 / comment_list 兜底 / 历史、收件箱、UP 主页、搜索、合集各入口全部统一
  - **机制取舍**（实现说明）：本 Flutter 版本 `RouteAware.didPushNext()` **无参**（RouteObserver 只通知被盖住的页、不传上方新路由），无法按「路由名 == player」在 didPushNext 里过滤——若无差别暂停，打开全屏独立评论页（边看边评）会被误停。因此**暂停改在 push 新播放页的调用点显式执行**（`_pauseBeforePushingNewPlayer`：pause + 保存进度 + 置标记），**恢复续播走 didPushNext 的对称事件 didPopNext**；didPushNext 不覆盖（忽略）
- **评论链接跳转语义**（player_page / comment_page / comment_list）：内嵌评论 onOpenVideo 与独立评论页 C 内链接统一 → **push 新播放页**。全屏独立评论页 C 打开时播放页**不暂停**（边看边评）；C 内点链接 → **C 先 pop 自己**（让旧播放页重新成为顶层，否则 P2 叠在 C 上旧页收不到任何通知）→ 播放页 push 前显式暂停旧页 → 叠 P2。C 均带回调；无回调兜底仍 push 新播放页（路由名 player，双音轨取舍见 comment_page 注释——当前 C 唯一入口是播放页、必传回调）。同 bvid 且在播第 0 集点本视频自身链接 → 不暂停不叠页
- **playVideo 换源保留**：`playVideo` 方法语义不变（当前页停旧播新），保留给多 P 切集 / 内部换源场景，不再由评论触发；同 bvid 判定等行为不回退
- **验证**：`flutter analyze` 0 issue；全量单测 **609 通过**（606 + 新增 3：P 叠 P2 暂停/P2 返回恢复、打开独立评论页不暂停（边看边评）、同 bvid 不叠页；既有 playVideo 换源 / CommentPage 回调 / 兜底测试适配新语义）；模拟器实测：播 A（取流成功/内嵌评论加载）、全屏打开独立评论页期间 A **持续播放不暂停**（保存进度持续推进、无暂停日志）、返回后仍续播（无 didPopNext 误触发）；「评论链接 → P2 → 返回续播」完整链路由 widget 测试覆盖（本机白名单视频评论均不含视频链接，无法在真机有机触发评论链接跳转——如实说明）
- 阶段 C（竖屏信息行 UP 主头像/主页入口等）按计划留待后续版本，本版未做

---

## v2.17.0 (2026-09-07)

**新功能：播放页竖屏布局重构（视频置顶 + 内嵌评论区）**

此前播放页**竖屏也是全屏黑底**：视频按宽高比垂直居中、上下大黑边，评论区需点「评论」进独立页。本次按 B 站观看页习惯重构竖屏（非全屏）形态：

- **竖屏布局（`app/lib/pages/player_page.dart`）**：
  - 页面改为：**上部视频区**（按视频宽高比铺满可用宽、顶部置顶；超高视频如 9:16 封顶屏高 60%，盒内画面居中 + 黑边补齐，给下方内容留位）+ **视频信息行**（标题/分 P、UP 主名文本占位——阶段 C 再加 UP 头像与主页入口、时长）+ **内嵌评论区**（滚动）
  - 画面/听视频占位/弹幕/字幕/手势/控制层**全部绑定在视频区矩形内**（不再整屏黑底、不再覆盖下方评论区）；字幕改为相对视频区底部定位（控制层显隐时 92/16），音量/亮度滑动换算与豁免带判定改用视频区尺寸
  - 听视频占位界面压缩适配矮视频盒（可滚动防溢出）；竖屏视频区过矮（超宽视频 <230px）时收起中央播放簇，双击播放/暂停仍可用
  - **横屏全屏保持原布局**（整屏播放、无评论区）；切换全屏布局联动正确
- **评论按钮**（控制层）：竖屏点击 → 平滑滚动定位到下方内嵌评论区（锚定列表顶「评论 N」区头）；横屏全屏点击 → 打开原独立评论页（返回后仍全屏续播）
- **内嵌评论区**（`app/lib/widgets/comment_list.dart` 新增，从 comment_page 抽取）：置顶/主评论分页上拉加载/楼中楼展开翻页/图片点击全屏查看保存/正文链接（视频/UP/番剧提示/外链）全部与独立评论页对齐；按「bvid + 当前分 P」换 key，**换集/换源自动重拉评论**；评论内点视频链接 → 本页实例换源（防双音轨，评论区随 key 刷新）
- `app/lib/pages/comment_page.dart` 改为薄壳（仅 Scaffold + AppBar 标题），列表逻辑统一走公共 `CommentListView`，两处不再双份维护
- **验证**：`flutter analyze` 0 error；全量单测 606 通过（含既有评论链接换源/倍速/历史/首页用例适配）；模拟器实测竖屏播放（视频顶部、评论区下方滚动）、全屏切换、评论按钮定位、换集评论刷新、字幕/弹幕在视频区内显示
- 阶段 B（评论链接跳转导航/返回续播）、阶段 C（UP 主入口）按计划留待后续版本，本版未做

---

## v2.16.24 (2026-09-07)

**修复**

- **修复"播放中关闭弹幕再打开 → 大量补发视频开头弹幕、时间轴错乱"**（用户报告：播到中段（如 3 分钟处）关弹幕再开，弹幕从视频开头密集涌出）：
  - **根因**（`app/lib/widgets/danmaku_overlay.dart` `_DanmakuOverlayState`）：弹幕层只在「开关开 && 有数据」时构建——**关弹幕 → overlay 卸载销毁 State；再开 → 全新 State**，`initState` 把发射游标无条件置 0；而发射循环对 `timeSec <= 当前播放位置` 的弹幕逐条发射 → 重开时游标从 0 起，把 **0 ~ 当前播放位置**整段弹幕按信用速率持续补发（开头弹幕成串涌出、时间轴全乱）。同理，「切集恢复进度 / 弹幕拉取完成晚于播放推进」时 overlay 直接挂载在中段也会补发
  - **修复**：发射游标的初始 / 每次复位统一改为**对齐当前播放位置**（新增纯函数 `danmakuCursorAt(list, posSec)`：在按 timeSec 升序的列表里二分找「首个 timeSec ≥ 当前播放秒」的下标）——弹幕层挂载 / 开关重开（initState）、切集换数据、设置变更清屏重载、seek / 恢复进度 >3s 跳变（didUpdateWidget）四条路径统一走该对齐；**严格已播过的弹幕（timeSec < 当前时刻）一律不再发射**，位置 0 时等价于从头开始（0 秒弹幕不误跳过，与发射口 `timeSec <= posSec` 自洽）
  - **顺带修复（同游标逻辑）**：seek **后退**时旧代码游标只前进（停留原前沿）→ 后退后到原前沿之间**长时间无弹幕**；现按新位置重算 → 后退越过的时间窗**重新发射**（与 B 站一致）
  - **单测**（`app/test/danmaku_features_test.dart`）：纯函数对齐语义（位置 0 / 中段 / 恰等于当前时刻保留 / 越末尾 / 空表 / seek 后退重算）；widget 测试「中段（3 分钟处）挂载：30 条开头弹幕一条不补发，播放位置越过 181s 只发后续那一条」（复现用户场景）；既有「positionMs 静态 2000 挂载即全量发射」类用例改「0 挂载 → 小步推到 2000」（对齐后语义一致）
  - **模拟器实测（logcat 文本取证，不读媒体）**：装 debug 包匿名播放白名单视频——开启弹幕时若 overlay 挂载在中段（播到 ~17s 才开/弹幕拉取晚于播放推进）见 `[danmaku] 游标对齐 pos=17123ms → cursor=1/17（跳过已播弹幕，不补发）`；完整复现「播到中段关弹幕再开」：45.4s 关（cursor=2/12）→ 播放推进 ~10s → 56s 重开 → logcat 见缓存命中 + `[danmaku] 游标对齐 pos=55897ms → cursor=2/12（跳过已播弹幕，不补发）`——游标保持推进后的位置不归 0，重开后按自然节奏发射后续弹幕，全程无开头补发、无二次清屏（对照修复前：重开即游标归 0，0~当前位置整段弹幕按 40 条/s 持续涌出）
- **「相同弹幕为什么乘几（×N）」结论：不是功能也不是 bug，App 无任何合并/计数显示代码**：
  - **代码证据**：全库 grep（× / merge / duplicate / mergeSimilar / count 等）确认弹幕渲染层（`danmaku_overlay.dart` `_spawn` 原样绘制 `d.text`）、数据模型（`danmaku.dart` 解析单条）、开关逻辑均无「合并相似弹幕显示 ×N」功能；git 历史五版弹幕迭代（v2.16.3/6/11/13/19）也从未加过
  - **数据证据**（2026-09-07 实测真实弹幕 XML）：B 站 `x/v1/dm/list.so` 对重复文案**逐条下发、不加计数、文本中不含 × 字符**（抽样 3600 条/视频：含 × 文本 0 条；同文案最多 2273 / 1554 / 814 条，且集中在同秒/邻近秒）——App 把 B 站下发的逐条原样渲染 → 同一文案短时间内**成串飞过**，观感上像「×N」
  - **处置**：保持现状不改代码（重复文案是 B 站数据本来的样子，逐条渲染符合「还原弹幕时间轴」语义）；「像 B 站官方播放器那样把同屏相似弹幕合并成一条 + ×N 角标」属于**新功能**（需要去重折叠 + 角标绘制），不在本次 bug 修复范围——是否要做请用户拍板（见 README 弹幕段说明）

---

## v2.16.23 (2026-09-07)

**修复**

- **修复"播放页打开评论区 → 点评论里的视频链接 → 跳转预览播放新视频时，旧视频音频仍继续播放"（双音轨）**：
  - **现象**：播放页 P 播视频 A → 点底部「评论」push 评论区 C（P 正常继续播，边看边评的预期行为）→ C 里点视频链接 → 旧实现 push 第二个播放页 P2（叠在 C 上）→ 栈 [P, C, P2]——**P 的播放器未释放**，P2 播视频 B 时 P 的 A 音频仍在响 = 双音轨
  - **根因**（`app/lib/pages/comment_page.dart` `_previewVideo`）：评论正文视频链接（v2.16.19+ 可点）走「fetchVideoMeta → push 新 PlayerPage」——新播放页叠加在旧播放页之上，旧页的播放器/定时器/事件订阅从未 dispose → 两个 Media3 实例同时出声
  - **修复（点评论视频链接 = 切到该视频看：停旧播新，不叠页）**：
    - `app/lib/pages/player_page.dart`：新增可复用换源方法 `playVideo(WhitelistVideo)`——**在当前播放页实例停旧播新**：先保存旧视频进度/写历史（`_saveExitProgress`，自 `dispose` 提取共用）→ dispose 旧播放器并完整清理关联状态（tick/浮层定时器、事件订阅、实时转写、字幕轨道与文本、弹幕渲染数据、进度/错误/缓冲/手势 hud 状态等）→ 更新当前视频状态 `_video`（页面内全部 `widget.video` 引用改走 `_video`，保证标题/取流/弹幕/下载/历史/评论入口一致切到新视频）→ **复用首次加载流程 `_init` 重新取流**（本地缓存优先、bvid/epId 取流分支、记忆进度恢复）→ 弹幕开关仍开则自动拉新视频弹幕。不重建页面、不新增 Navigator 页
    - `app/lib/pages/comment_page.dart`：构造新增可选参数 `onOpenVideoPreview`（播放页打开评论区时传入）——点视频链接 → **回调播放页换源 + pop 评论页**回播放页即见新视频；无回调（评论页独立打开）→ 保持 push 新 PlayerPage 兜底
    - **取舍（不回归）**：UP 主页链接仍 push UpownerPage、外链仍走 url_launcher 系统浏览器——此时旧视频继续播是「边浏览边听」的可接受行为（不产生双音轨），维持现状
  - **单测**（`app/test/comment_video_link_test.dart` 新增，mock 原生播放器通道/HTTP 路由，全离线）：
    - `PlayerPage.playVideo` 换源：旧播放器 dispose、只新建 1 个播放器（无双播放器）、页面不叠加（标题切到新视频）；同 bvid 点击跳过不重载
    - `CommentPage` 有回调：点评论视频链接 → 回调拿目标视频 + pop 评论页；无回调 → 兜底 push 新 PlayerPage
  - **模拟器实测（logcat / uiautomator dump 文本取证，不读媒体）**：播 A → 开评论 → 点评论里视频链接 → 回播放页播 B：logcat 见新 bvid 取流 + `onPrepared`、旧播放器 `dispose` 日志（单播放器生命周期），全程无双音轨；UP/外链入口不回归

---

## v2.16.22 (2026-09-07)

**修复**

- **修复 vivo 真机"播放有声音没画面（一直黑屏）"**（用户 vivo V2364A 真机反馈；模拟器 AOSP 正常、无法复现）：
  - **现象**：点视频播放**有声音、无任何提示、一直黑屏**；同一 App 模拟器播放正常，用户真机历史"手动登录后有画面"，最新版本仍黑
  - **根因**（`app/android/.../BiliDashPlayerPlugin.kt` `createPlayer`）：原生插件把**自增序号**（`nextTextureId++`，从 1 起）当作纹理 id 返回给 Dart；而 Dart 侧 `Texture(textureId:)` 必须使用 Flutter 引擎在 `TextureRegistry.createSurfaceTexture()` 时**分配的纹理 id**（`SurfaceTextureEntry.id()`）。两者错位时，Flutter 引擎纹理注册表里查不到 Dart 传入的 id → `Texture` widget 无对应纹理可渲染 → 视频帧解码后只输出到 SurfaceTexture、**不上屏**；音频轨独立播放不受影响 → **黑屏但有声音**。模拟器恰好 id 巧合对齐未触发；vivo 真机引擎纹理分配起点/时序与自增序号错开 → 必现
  - **修复**：`createPlayer` 改为取 `registry.createSurfaceTexture()` 返回的 `entry.id()` 作为纹理 id（与官方 `video_player_android` 同源实现一致），并加 `Log.i` 便于真机取证
  - **真机验证（系统层文本证据，logcat 全量抓取）**：真机已装 v2.16.22（含修复）播放白名单视频——audio_flinger **持续 1 active track**（uid 10406 = App，音频真实输出）、播放页时间轴持续推进（30s 内 57:58→58:55）、**全程无播放错误**（Media3 无 error → 音视频轨解码正常，排除编码不兼容假说）；修复链路与官方实现一致（纹理 id 对齐 → 帧上屏）。⚠ 像素画面最终目视确认由用户在真机复核（本机约束禁读媒体/截图，无法自动化判色）
  - ⚠ 排查记录：vivo 设备 logcat 中 App 进程日志（含原生 `Log.i`）与媒体解码器日志（ACodec/Codec2）均不可见（平台裁剪），改用 audio_flinger / 播放页 UI 时间轴等系统层证据定位与验证

---

## v2.16.21 (2026-09-05)

**新增 / 改进（"进去即登录 + 1080P"链路做扎实——自动续期 + 失效自动重登 + 匿名明确提示）**

- **自动续期分档提前（`app/lib/api/bilibili_api.dart` `planSessionStart` +
  `app/lib/pages/playlist_page.dart` `_handleSessionOnStart`）**：续期触发阈值按
  refresh_token 有无分档——
  - **有 refresh_token → 距过期 < 15 天即静默续期**（SESSDATA 约 30 天有效，续期成功
    即把新 SESSDATA/新 refresh_token 入库 → 会话始终活跃在新窗口内，**登录一次长期
    不掉线**；约半月一次续期调用，频率远低于 B 站续期接口风控阈值——权衡后不做
    「每次启动都续期」）
  - 无 refresh_token（登录时未抓到刷新口令）→ 续期必失败，阈值保持 7 天（避免白跑
    失败请求）；已过期一律先试续期、失败再引导重登
- **续期结果分类处理（`refreshSession` 返回 `SessionRenewResult`）**：
  - `networkError`（网络/超时）→ 保留现会话、**下次启动再试**，彻底不打扰
  - `missingCredentials` / `tokenInvalid`（refresh_token 失效，-101 等）→ 会话**已过期**
    → 清失效凭据 + 自动引导重新登录；**未过期** → 保留现会话（SESSDATA 仍有效、
    1080P 继续——不打断可用会话强弹登录页；播放遇 -101 / 管理面板与播放页临近过期
    横幅会在会话真正失效前引导）
  - 响应解析兜底：新会话**同时兼容 Set-Cookie 与响应体 `data.sessdata`/`data.bili_jct`
    两种下发**（不同时期接口实现都覆盖），拿不到新会话绝不覆盖旧凭据
- **关闭登录页 = 明确匿名（不默认静默降级）**：
  - **首页**（`playlist_page.dart`）：无有效 SESSDATA 时 AppBar 下方显示提示条
    「未登录仅 720P，去登录解锁 1080P（登录一次，之后每次进入自动恢复）」，点按直达
    登录页；登录成功/关闭后即时刷新（无需重启）
  - **播放页**（`player_page.dart`）：匿名观看时顶部横幅「未登录仅 720P，去登录解锁
    1080P（登录一次长期保持）」，点按去登录；**登录成功才重取流解锁 1080P，未登录
    返回不打断当前播放**（`_goLogin` 按登录结果决策，失败态保留「去登录」按钮）
  - 登录成功保存后首页/播放提示即消失；登录态下播放取流自动 `qn=80`（1080P）
- **单测**（`app/test/auto_login_test.dart` 更新 + 新增 `app/test/session_renew_test.dart`
  / `app/test/playurl_auth_test.dart`）：
  - `planSessionStart` 双档阈值映射（有 token 15 天 / 无 token 7 天，含边界与已过期）
  - `refreshSession` 分类：缺凭据（不发网络请求）/ Set-Cookie 续期成功入库 /
    响应体兜底续期成功 / refresh_token 失效（-101）不动 storage / code=0 拿不到新会话
    不覆盖旧凭据 / 网络异常不动 storage
  - `fetchPlayUrl` **登录态注入断言**：有效 SESSDATA → 取流请求 Cookie 带 SESSDATA +
    buvid 指纹 + `qn=80`（1080P 链路）；匿名 → 不带 SESSDATA 仍可播；残留过期
    SESSDATA → 不注入 + 清除（v2.16.20 修复路径回归）
  - Widget：关闭登录页后首页显示「未登录仅 720P」提示条、点按再次请求登录；有
    refresh_token 提前续期窗口内续期网络失败 → 保留、不请求登录
- **模拟器实测（logcat + uiautomator dump 文本取证，不读媒体）**：
  - 无会话冷启动 → `[session] 启动检查：无 SESSDATA → 自动进入登录页` +
    `[session] 打开登录页（自动引导）`，dump 见登录页（banner 全文 + 短信登录
    手机号/验证码）；back 返回 → 首页 dump 含「未登录仅 720P…」提示条
  - 匿名播放：点合集视频 → `[bili_api] fetchPlayUrl qn=80 fnval=16 ok: quality=64
    (720P) dashV=4` + `[player] onPrepared 852x480` + 进度推进（无 BiliDashError）；
    播放页 dump 含匿名横幅文案
  - 注入合成有效会话（+30 天）冷启动 → `[session] 启动检查：会话有效（≥续期阈值），
    静默恢复`，无登录页弹出
  - 注入将过期会话（+10 天 + refresh_token）冷启动 → 进入提前续期窗口、真实发出
    续期请求 → 接口失败记录（模拟器网络层 badResponse；业务码 -101 判定留给真机）→
    `[session] 续期失败（networkError，未过期）→ 保留现会话`，不弹登录页、流程不崩
- ⚠ 验证局限：**真实登录（短信收码）→ 服务端真给 1080P 的端到端无法在模拟器完成**
  （无法收码、合成 SESSDATA 服务端不认）——App 可控链路（有效 SESSDATA 注入 +
  qn=80 请求）由单测 + 逻辑保证，请真机登录后复核 quality=80 下发

---

## v2.16.20 (2026-09-05)

**修复**

- **修复"未登录状态下点视频播放没画面（登录后正常）"回归**（用户 vivo 真机反馈）：
  - **现象**：v2.16.18 自动登录（启动自动处理登录态 + 移除首页登录按钮）后，未登录/会话过期的用户在首页点视频播放没画面；登录后（新会话）播放正常
  - **根因**（诊断）：v2.16.18 的启动自动登录在**会话已过期且续期失败**时会引导进入登录页，但用户跳过登录页（不登录）后，**secure storage 里残留的过期 SESSDATA 并未被清除**——旧实现（含 v2.16.18 前手动登录时代）认为"有会话就注入"。残留过期 SESSDATA 会在播放取流时经 `_injectAuth` 注入请求，B 站对**真实过期 cookie** 的 playurl 返回 **-101（登录已失效）**（注：伪造/无效 cookie 会被服务端当匿名处理给 720P，与真实过期会话行为不同）→ 播放页报"登录已失效"、无画面；重新登录（新有效 SESSDATA）后正常。模拟器（storage 空=真纯匿名）无法复现——匿名播放本就正常（`fetchPlayUrl quality=64` 720P → onPrepared），与真机"残留过期会话"状态差异即在此
  - **修复**（`app/lib/api/bilibili_api.dart` `_injectAuth` + `app/lib/pages/playlist_page.dart` `_handleSessionOnStart`）：
    - `_injectAuth`：读到的 SESSDATA 若本地解析**已过期**（`sessdataExpireAt`）→ **不注入**该失效会话（回退纯匿名请求，匿名 720P 实测正常），并顺带 `clearSession()` 清除失效凭据——播放取流不再被残留过期会话拖垮，未登录也能正常匿名播放
    - `_handleSessionOnStart`：会话已过期且续期失败 → 引导登录页前**先 `clearSession()` 清掉失效会话**（用户跳过登录页后处于真未登录态，后续播放走匿名正常，不再残留死 cookie）
  - **单测**：`pgc_playurl_api_test.dart` 新增"残留已过期 SESSDATA → 不注入（回退匿名）+ 清除失效凭据"（与 `fetchPlayUrl` 共用同一 `_injectAuth`，覆盖取流共用路径）；`auto_login_test.dart` 过期续期失败用例补断言"失效会话被清除"
  - **模拟器回归（logcat 文本取证，不读媒体）**：无 SESSDATA 冷启动 → `[session] 启动检查：无 SESSDATA → 自动进入登录页`（自动登录仍按预期触发一次）→ 返回首页正常 → 点视频 → `[bili_api] fetchPlayUrl ... ok: quality=64 dashV=4` + `[player] onPrepared` + 进度持续推进（无 BiliDashError）；评论/弹幕入口匿名抽查正常（fetchVideoComments ok / fetchDanmaku 206 条渲染无异常）
  - ⚠ 验证局限：真实"签名有效但已过期"的 SESSDATA 需真机真实账号复现（无法离线构造、不回显真实凭据），修复使残留过期会话不再被注入（本地即可判定过期并清除），真机请升级后复核

---

## v2.16.19 (2026-09-05)

**新增 / 改进**

- **评论区图片全屏查看 + 保存到相册**（`app/lib/pages/image_viewer_page.dart` 新增 + `app/lib/pages/comment_page.dart` 缩略图接入）：
  - 点评论图片（根评论 / 楼中楼子回复都有）→ 黑底全屏查看页：**PageView 多图左右滑动 + 顶部页码（`x/N`）/ 关闭**、每图 **InteractiveViewer 双指缩放 1~4x + 双击 1x↔2.5x**（以屏幕中心为锚点）；**1x 时禁平移**（横向滑动交给 PageView 翻页）、放大后优先平移看图——缩放与翻页手势不打架；图片加载失败灰底占位不白屏
  - **保存到相册**：底部「保存到相册」按钮 → Dart 带 UA/Referer 下载当前图字节（`lib/services/gallery_saver.dart`，dio，gif 存原始字节相册里可动）→ 原生 `GalleryController.kt`（MethodChannel `bili_whitelist/gallery`，与 MediaController/ApkInstaller 同风格手动注册）：
    - **通道选择：原生小通道（免第三方依赖）**——评估两条路（image_gallery_saver/gal pub 包 vs 原生 MediaStore），原生 ~30 行内搞定且不留弃维护包 → 选原生
    - **API 29+（主流，本项目 Android 10+ 目标）**：`MediaStore.Images` 插 `Pictures/BiliWhiteList`（RELATIVE_PATH + IS_PENDING 两步写，失败清理半成品条目）→ **零存储权限**
    - **API 26~28**（minSdk=26 需覆盖）：老式 `getExternalStoragePublicDirectory(Pictures)/BiliWhiteList` + MediaScanner 广播；需要 `WRITE_EXTERNAL_STORAGE`（Manifest 已加 `maxSdkVersion=28` 限定，Android 10+ 永不出现该权限）——未授权时原生 `requestPermissions` 自动弹系统授权框，结果经 `MainActivity.onRequestPermissionsResult` 转发给 GalleryController **补完挂起的保存**（Dart 一次调用即可，授权框期间 await 挂起）；拒绝 → SnackBar「未授予存储权限…」
  - 保存成功/失败中文 SnackBar（下载失败 / 权限拒绝 / 相册写入失败分文案）
- **评论正文链接识别与站内跳转**（`app/lib/utils/comment_links.dart` 新增纯函数 + `comment_page.dart` 渲染/分发接入）：
  - **拆分**：正文按链接拆「纯文本段/链接段」交替（`splitCommentLinks`）——覆盖完整视频链接（`www./m.`、带 `?p=` 查询）、**裸 BV**（`BV`+10 位，正文独立出现）、b23 短链（带协议头/裸 `b23.tv/xx`）、UP 空间（`space.bilibili.com/<uid>` 与 `/space/<uid>`，含无协议头）、番剧（`bangumi/play/ep|ss`，容忍大写）、通用 http(s)；**URL 在中文/全角标点处截断 + 尾随标点裁剪**（`链接。` / `链接，谢谢` 不吞标点）、换行正文不丢字、多个链接顺序拆分
  - **渲染**：有链接 → RichText 链接段**主色 + 下划线**可点（`TapGestureRecognizer`），纯文本段原样；**无链接评论保持 SelectableText**（选择/复制能力不变）；有链接的评论长按整段复制兜底（RichText 不可选）
  - **点击分发**：`video`（完整链接/裸 BV）→ `fetchVideoMeta` → `WhitelistWriter.videoFromMeta` → `PlayerPage` **站内预览播放（只播放不加入白名单）**；`b23` → `resolveShortLink`（复用 `utils/import_parser.dart`）重定向解析落点后按落点再分发（视频→预览 / 番剧→提示 / UP→主页 / 其他→浏览器；落点仍为短链防死循环直接浏览器）；`up` → `UpownerPage(mid)`；`other` → **url_launcher**（`^6.3.0`，新依赖）系统浏览器 `LaunchMode.externalApplication`（校验 scheme 只放 http/https，失败 SnackBar）
  - **番剧链接取舍（bangumi）**：App 无通用番剧/电影播放入口（番剧需导入白名单走选集 + 会员集 pgc 回退），评论内点击**不硬塞播放**，SnackBar 提示「番剧/电影链接：请在搜索页切换『番剧/电影』搜索后导入观看」——先做视频/UP/通用三类站内动作
- **弹幕顶/底与滚动分区重叠调整（上一任务遗留改动随本版一并发布）**（`app/lib/widgets/danmaku_overlay.dart` + `app/test/danmaku_features_test.dart`，顺带更新 `app/lib/models/danmaku_lanes.dart` 注释）：
  - **问题**：v2.16.13 显示区域把弹幕带等比压缩后，轨道数换算仍扣除顶部 3 行 / 底部 2 行固定行——区域 30% 时 8 行只剩 **3 条滚动轨**，小显示区域弹幕量被顶/底固定行**过度挤压**（滚动轨堵、弹幕密时丢弃多）
  - **调整**：顶锚以下**不再为顶/底弹幕分区避让**——**滚动轨道占满整条可用带**（`danmakuScrollLaneCount` 改为 `floor(带高/行高)` 钳 `1..60`，不再减顶/底固定行：100% 屏 756px → 28 条（旧 23）、30% 226.8px → 8 条（旧 3））；顶部弹幕从带顶向下堆叠（行 i y = 顶锚 + i×行高，新纯函数 `danmakuTopTextY`）、底部弹幕从带底向上堆叠（行 i y = 底缘 − i×行高 − 文本高，`danmakuBottomTextY`），与滚动轨道**纵坐标可重叠**；各自**内部**仍互不叠（滚动按轨道追尾碰撞、顶/底行分别堆叠、行满丢弃），所有弹幕 y 不越显示区域（底缘额外钳制在区域下界内，极小区域自动上提）
  - **单测**（`danmaku_features_test.dart` 弹幕组重构）：轨道换算 100%→28 / 30%→8 / 带高不足行高保底 1 / 超大屏封顶 60；顶/底行 y 换算（行 0 贴带顶 / 贴底缘、与滚动轨道同 y = 可重叠基础）；区域 30% 三类弹幕共存 widget 测试（y 不越区域）；`danmaku_lanes.dart` 仅注释对齐（底部行 0 = 贴底缘最底行，取行先取最底行向上堆叠）
- **发布元信息**：`pubspec.yaml` version 2.16.18+48 → **2.16.19+49**；依赖新增 `url_launcher ^6.3.0`

---

## v2.16.18 (2026-09-05)

**新增 / 改进**

- **自动登录（启动自动处理登录态 + 移除首页登录按钮）**——解决用户反馈「每次进 App 都要点首页"登录（解锁 1080P）"按钮」：
  - **根因**（诊断）：① 首页 AppBar 登录按钮是**无条件常驻渲染**（tooltip/图标固定为"登录（解锁 1080P）"，无任何登录态分支）——已登录用户每次进入仍看到"要登录"，反复误点/困惑；② **无 SESSDATA（首次/彻底过期）时没有任何自动登录路径**——旧 `_maybeRefreshSession` 只覆盖"已有 SESSDATA 且将过期→续期"，无会话直接静默跳过，用户只能手动点按钮；续期逻辑本身已挂 initState 且不依赖按钮（此项非问题）
  - **启动自动登录**（`app/lib/pages/playlist_page.dart` `_handleSessionOnStart`，首页 initState 触发，不依赖任何按钮）：读 secure storage——**SESSDATA 有效（距过期 ≥ 7 天）→ 静默恢复**，不弹任何界面；**距过期 < 7 天（含已过期）→ refresh_token 静默续期**（成功即静默；续期失败且**已过期**→ 自动引导重新登录；未过期 → 保留现会话不打扰）；**无 SESSDATA（首次使用/登出）→ 自动进入登录页引导一次**（防循环：本次进程只引导一次）。存储读取异常（插件缺失/Keystore 故障）静默跳过，不误导登录
  - **决策映射抽为纯函数**（`app/lib/api/bilibili_api.dart` `planSessionStart` + `SessionStartAction` 枚举）：`null → autoLogin / <7天 → refresh / ≥7天 → silent`，可单测
  - **移除首页 AppBar 登录按钮**（原 tooltip"登录（解锁 1080P）"IconButton 删除）；**次级"账号/重新登录"入口**移到**管理面板**（右上角齿轮 → 新增「B 站账号」区块：状态读实时——已连接/会话过期/未登录文案 + 「登录 / 重新登录」按钮，关闭面板后推登录页）
  - **自动引导登录页带提示条**（`app/lib/pages/login_page.dart` 新增可选 `banner`，自动路径传 `kAutoLoginBanner`：说明"登录后自动保存：下次进入自动恢复，无需再次登录；不登录也能看（最高 720P）"）；手动入口（管理面板）不显示提示条
  - 测试注入接缝：`PlaylistPage.openLogin`（测试替身记录"请求登录"而不推含 WebView 的真实 LoginPage——后者构造在测试环境 assert 平台未注册）
  - **单测**（新 `app/test/auto_login_test.dart`，11 用例）：`planSessionStart` 映射（null→autoLogin / ≥7天与恰好 7 天→silent / <7天与已过期→refresh）；widget（mock secure storage 通道 + 注入登录导航替身）——无会话冷启动**自动请求登录一次且带 banner**、多次 pump 不重复触发、有会话（≥7 天）**静默零请求**、<7 天缺 refresh 凭据续期失败未过期**保留不请求**、已过期缺凭据→**自动请求重登**、管理面板已连接「重新登录」/ 未连接「登录」入口与状态文案、首页无"登录（解锁 1080P）"tooltip。旧 `collection_page_test.dart` 拖动用例注入合成有效会话（测拖动与登录无关，无会话会触发自动登录页而 WebView 测试环境不可构建）
  - **模拟器实测（logcat + uiautomator 文本取证，不读媒体）**：无会话冷启动 → `[session] 启动检查：无 SESSDATA → 自动进入登录页` + `[session] 打开登录页（自动引导）`，uiautomator dump 见登录页（content-desc「登录」+ Back + banner 全文 + WebView 加载）；back 回首页无循环；注入合成会话（+30 天，仅结构合法的假凭据，非真实账号）冷启动 → `[session] 启动检查：会话有效（≥7天），静默恢复`、**无**登录页弹出，首页 dump 正常合集卡片；打开管理面板 → dump 含「B 站账号 / 已登录：B 站账号已连接（1080P 已解锁）/ 重新登录」
  - ⚠ **播放取流 1080P 档位说明**：App 取流注入 SESSDATA 逻辑未改动（既有实现 + 既有单测覆盖），合成会话服务端不认、无法在自动化中复现真实 1080P 档位；真实账号 1080P 请在真机登录后复核

## v2.16.17 (2026-09-05)

**修复**

- **横屏全屏从物理屏幕底部滑动仍误触发 seek（v2.16.14 豁免带未覆盖横屏物理底部）**：
  - **问题（用户实测反馈）**：v2.16.14 豁免带只查 Pan 起点的 y0 上下（底部/顶部带），但**横屏全屏旋转后物理屏幕底边（系统导航区）对应 Flutter 逻辑坐标的左/右边缘**（landscapeLeft / landscapeRight 分别是逻辑左或右边缘）——用户从物理底部上滑在逻辑上呈「起点贴逻辑左/右边缘、y0 在屏中部」的**横向滑动**（被判定 horizontal seek），不落任何 y 豁免带 → 豁免失效、仍被当 seek
  - **修复（`app/lib/pages/player_page.dart`）**：豁免带扩展为**四边**——`isExcludedGestureStart` 改为 `{x0, y0, width, height, topPx, bottomFactor, bottomMinPx, leftPx, rightPx}`（纯函数可单测）：**横屏左右边缘豁免带取与底部同宽**（`max(短边 ×8%, 48px)`，覆盖 3-button / 手势导航条物理高度换算 + 手指容差；**两侧都留**——物理底边随旋转方向映到左或右任一边，见类顶坐标说明注释）；竖屏左右 16px 窄带防误触；上下豁免参数与 v2.16.14 一致
  - **按下点判定（实测发现，v2.16.17 关键）**：豁免判定从 `onPanStart` 坐标改为 **`onPanDown` 按下点坐标**——`onPanStart.localPosition` 是手势**赢得竞技场那一刻**的位置（已滑过 touch slop、且边缘滑动常被系统手势区延迟释放，模拟器实测右缘起点内移可达 ~100px：胜出点 x0=811、距右缘 103px），按它判边缘带会漏判；`onPanDown` 在手指落屏第一时间回调、即真实触摸起点（实测贴边起点 x0=4 / 910 精确命中）
  - **实测（模拟器 logcat，横屏全屏 ROTATION_90、逻辑屏 914x411）**：左缘滑动按下点 x0=4、右缘 x0=910（y0=206 屏中部）→ 豁免日志命中、**无 seek / 无 hud**；显示空间底边条带按下点 y0=408 → y 底部带豁免命中、无 seek（系统手势区吞掉的场合 App 无触摸，也不会误 seek）；屏幕中部横滑 → 正常 `seekTo`（比例 -0.34 实测）。**竖屏回归**：底部带按下点 y0=869 → 豁免；中部纵向亮度（左半 50%→52%）/ 音量（右半 12/15→13/15）正常；中部横向滑动忽略（无 seek）；双击播放/暂停、单击显隐控制层、长按 2x 均正常
  - **单测**：`test/player_gesture_test.dart` 豁免组扩展为 12 用例——横屏 y0 屏中部 + x0 贴左/右缘 → true（v2.16.17 核心回归）/ 左右带外（x0 > 48 距缘）→ false（中部 seek 不被豁免）/ 竖屏默认左右窄带边界 / 左右带可调窄关闭 / 底部顶部带内边界 / 尺寸异常防御

---

## v2.16.16 (2026-09-05)

**新增**

- **视频列表项显示发布时间（`pubdate` 字段 + 各导入路径写入 + 列表展示）**：
  - **数据模型**（`app/lib/models/whitelist_video.dart`）：`WhitelistVideo` 新增可选 `pubdate`（发布时间，Unix 秒；视频级附加字段，向后兼容不升顶层 version——旧读者忽略该键、新读者缺省 null = 未知）；fromJson `is num` 防御（脏类型 → null 不崩）、toJson 非空才输出（旧数据不回写多余字段）、copyWith 沿用；新增 `formatPubdate()`（Unix 秒 → `yyyy-MM-dd`；null / 0 → 空串，供列表项拼接）
  - **普通视频导入**（`app/lib/services/whitelist_writer.dart` `videoFromMeta`）：写入 view 接口 `data.pubdate`（**秒**，实测单位与语义同搜索结果的 pubdate）；0 / 缺失 / 脏类型 → null（不写脏值）。`addByBvid`（首页粘贴链接导入 / 搜索页「加入」）自动带上
  - **番剧 / 电影导入**（`PgcEpisode` + `videoFromPgcEpisode`）：`app/lib/api/bilibili_api.dart` 的 `PgcEpisode` 新增 `pubTimeSec`——**2026-09 实测 `pgc/view/web/season` 的 `episodes[].pub_time` 是 Unix 秒整数**（与普通 view `data.pubdate` 同单位、逐集各不相同：Hand Shakers 每集差一周；**不需要毫秒换算**，区别于同条目里的 `duration` 毫秒），0 = 接口未给；`videoFromPgcEpisode` 透传为 `pubdate`（>0 才写）→ `importPgcSeason` 整季导入的每集自动带发布时间
  - **列表展示**（`app/lib/widgets/video_tile.dart`）：副信息行 `时长 · UP主` → 有 `pubdate` 时拼 ` · 2023-05-01`；**null / 0 不拼该段**（旧数据展示与旧版逐字符一致、不破坏布局）。合集页 / 未分类白名单列表等 `VideoTile` 使用方自动生效；历史页 / 搜索结果等数据源本无 `pubdate`，自然不显示（无异常）
  - **油猴脚本 v2.3.2**（`bili-whitelist.user.js`）：`fetchVideoInfo` 两路（页面 `__INITIAL_STATE__.videoData.pubdate` 优先 / view API `data.pubdate` 回退）为**新条目**带 `pubdate`（`toPubdate` 只收正数，0 / 缺失不写脏值）；`parseWhitelist` 条目整体透传 + `buildWhitelistJson` 整对象序列化 → 读到的**旧条目含 `pubdate` 原样保留**、合并写回不丢（遵循历史「upowners / collections 丢失」教训：新增字段不做字段级重建）
  - **单测**：`whitelist_video_v3_test.dart` 新增 pubdate 组（解析 / 序列化 / null 缺省 / 脏类型 / 往返 / copyWith / `formatPubdate`）；`whitelist_writer_test.dart`（videoFromMeta 带 pubdate / 缺失与 0 与脏类型 → null、videoFromPgcEpisode 透传 pub_time 秒 + 无 pub_time → null、toJson 往返）；`pgc_api_test.dart`（episodes[].pub_time 秒级解析断言）；新 `video_tile_test.dart` widget 测试（有 pubdate → `1:30 · UP主 · 2023-05-01`；null / 0 → 旧文案无日期段、不崩）
  - **实测（模拟器）**：以含 `pubdate` / 旧数据 / 真实番剧（`fetchPgcSeason` 真接口）三组数据驱动真实列表渲染，dump / logcat 文本取证显示日期、旧数据不显示且无异常

---

## v2.16.15 (2026-09-05)

**新增**

- **播放页评论区查看（只读，视频 / 番剧通用）**：
  - **入口**（`app/lib/pages/player_page.dart`）：播放页底部功能行新增「评论」按钮（弹幕与下载之间）→ 打开评论区
  - **接口**（`app/lib/api/bilibili_api.dart` + 新模型 `app/lib/models/comment.dart`）：主评论 `x/v2/reply/main`（**匿名可用**，带完整头 + buvid 指纹 / 登录态 Cookie，**无需 WBI**）——`fetchVideoComments({aid, mode=3, next})` 翻页**回传上一响应 `cursor.next` 原样**（不手写 +1），`data.replies[]` 解析（每条内嵌至多 3 条楼中楼预览）+ `top_replies` 置顶 + `cursor{next,is_end,all_count}`；楼中楼 `x/v2/reply/reply`——`fetchReplyChildren({aid, root, pn, ps})`，`hasMore = pn×ps < page.count`（空页不再多拉）。**oid 防御**：`replies[]` 中 `oid != aid` 的脏条目丢弃；错误分类：12002=评论区已关闭、-412 风控、-352 限流、网络 DioException（UI 各自提示 / 重试）
  - **aid 解析**：普通视频与番剧集统一按 aid 取评论——`resolveAidForVideo`（纯函数，meta 带 aid 直接用）+ `BiliApi.fetchVideoAid`（无 meta 时调 view 接口取 `data.aid`，番剧 ep 的 aid 与 `PgcEpisode.aid` 一致；失败提示重试）
  - **正文清洗**：`content.message` 的 `<br />` → 换行、HTML 实体 / 数字实体解码、残留标签剥除；图片 `content.pictures[]`（`img_src` http→https，`i*.hdslb.com` 无需 Referer，加载仍带浏览器头兜底；按原图宽高比占位；`play_gif_thumbnail` 标「动图」角标）
  - **评论页**（新 `app/lib/pages/comment_page.dart`）：AppBar「评论 N」（N=评论总数）或「评论」；列表项 = 圆头像 + 用户名 + 等级角标（Lv 色块）+ 正文（可复制 SelectableText）+ 图片（多图按宽高比、失败灰底占位）+ 点赞 + 相对时间 + 「N 条回复」；**楼中楼预览**（收起态缩进小字显示内嵌预览）→ 点「N 条回复」展开拉完整楼中楼（pn 递增翻页、hasMore「加载更多回复」、可收起）；**上拉加载下一页主评论**（滚动近底部触发，cursor.next 回传；到底显示「没有更多了」）；置顶评论顶部展示 + 「置顶」角标；空态「暂无评论」/ 12002「评论区已关闭」/ 错误态带重试；只读不写操作，长内容不截断（整页可滚动）
  - **单测**：`app/test/comment_model_test.dart`（消息清洗 / 图片 / 预览浅解析防嵌套 / resolveAidForVideo）+ `app/test/comment_api_test.dart`（请求参数 aid/mode/next 与 root/pn、解析、空态 null/[]、oid 不一致丢弃、cursor.next 透传、12002/-412/-352 / 网络、hasMore 换算、fetchVideoAid 三态）
  - **设备实测**（模拟器，`app/integration_test/comment_flow_test.dart` 真网络集成测试 + uiautomator/logcat 文本取证）：播放页「评论」→ 评论区加载（「评论 89」+ 置顶 + 用户名/Lv/点赞/时间节点齐全）；点「2 条回复」→ 楼中楼 `fetchReplyChildren` 展开成功；多页视频上拉翻页（第 2 页请求带上游 cursor.next）；带图评论图片真实加载（无「图片加载失败」）；番剧集（epId）aid 解析正确打开评论；无评论 / 关闭视频显示空态或关闭提示

---

## v2.16.14 (2026-09-05)

**修复**

- **横屏全屏滑动手势与系统手势冲突（从屏幕下方滑动唤醒导航误触发 seek）**：
  - **问题（用户实测反馈）**：横屏全屏（immersiveSticky 沉浸模式）时想从屏幕**底部**向上滑动唤醒 Android 三键导航 / 手势导航，App 手势层与系统手势区同时收到触摸——旧版单一 Pan 覆盖整屏，从屏幕下方发起的滑动被当成左右滑 **seek**（进度被拖走、时间浮层乱跳）；系统导航条在几何屏幕底部，这一带本就不该属于播放手势
  - **修复（`app/lib/pages/player_page.dart`）**：新增**手势起点豁免带**（v2.16.14+）——Pan **起点 y0** 落在屏幕**底部豁免带**（`max(屏高 × 8%, 48px)`，横屏屏高 ~400px 时取 48px 兜底宽于 8% 的 32px；竖屏 ~8% 屏高更宽）或**顶部豁免带**（固定 24px，横屏刘海 / 状态栏下拉区）内 → 本次 Pan **整体忽略**（不 seek、不调亮度/音量、不出 hud），把这段屏幕让给系统手势；屏幕中部滑动（seek / 亮度音量）完全不受影响。竖屏/横屏统一适用——竖屏从底部启动的纵向调节本就会被系统「上滑唤导航」抢占，豁免更安全
  - **豁免只作用于 Pan 滑动**：tap / 双击 / 长按不走 Pan 竞技场（tap 无位移不触发 Pan），底部带内单击显隐控制层照常；控制层按钮 / 进度条在 Stack 上层本就独立
  - **纯函数 + 单测**：`isExcludedGestureStart({y0, height, bottomFactor, bottomMinPx, topPx})`（纯函数，便于脱离 Widget 测判定）+ `test/player_gesture_test.dart` 新增 8 用例（底部带内含边界 / 带外 / 顶部带内含边界 / 中部净区 / 矮屏 minPx 兜底 / 纯比例模式 / 顶部可关 / 高度异常防御）
  - **实测（模拟器 logcat）**：横屏全屏底部区域横向滑动 → 无 seek / 无 hud 日志（豁免生效）；屏幕中部横向滑动 → 正常 `seekTo`；竖屏底部区域滑动 → 无亮度/音量调节日志、中部正常

---

## v2.16.13 (2026-09-05)

**新增**

- **弹幕显示区域设置 + 弹幕设置记忆（开关/区域/透明度全持久化，重启保持）**：
  - **问题（实测）**：v2.16.12 及更早弹幕在全屏弹幕带（屏高约 12%~82%）内自由滚动，观感上会飘到画面中下部**妨碍观看内容**；且弹幕开关状态只在内存——切出播放页/杀进程重启后回到默认关，用户每次看弹幕都要重新点开，区域/透明度等设置在面板内已自动保存但开关这一高频设置反而没记忆
  - **显示区域（`app/lib/widgets/danmaku_overlay.dart` + `danmaku_settings_sheet.dart`）**：设置面板新增「显示区域」滑杆（**10%~100%，10 步进**，说明"弹幕只在屏幕上方 N% 高度内滚动"）——弹幕带顶/底锚点随百分比等比压缩到屏幕上方该比例内，滚动轨道数按缩放后带高换算（新增纯函数 `danmakuScrollLaneCount(带高px, 行高)`：区域 100% 与旧版逐像素一致；30% 时滚动轨道 23 → 3 条，发射 y 上限 ≈ 屏高×30%）；底部锚点额外钳制在区域下界内（极小区域底部弹幕不越界）；区域/屏蔽/透明度任一设置变化仍走既有「新实例 → 清屏按当前位置重载」，区域调小即时清屏重排
  - **设置记忆（`app/lib/models/danmaku_settings.dart` + `player_page.dart`）**：设置模型新增 `enabled`（开关，默认关）与 `displayAreaPercent`（区域，默认 100）并随既有单 key JSON 一并持久化（旧数据缺字段回默认，兼容）；播放页「弹幕」按钮**点按取反即写入持久化**；进播放页异步读持久化 `enabled` 初始化开关——上次开着 → 本次进页**自动开并自动拉弹幕**（切集沿用既有「开关保持 + 自动换集弹幕」逻辑），重启 App 后开关/区域/透明度全部保持
  - **单测**（`app/test/danmaku_features_test.dart`）：settings JSON roundtrip / 旧数据缺字段回默认 / 区域脏值归一化（越界与非 10 步进收敛）；`danmakuScrollLaneCount` 换算（100% 与旧版一致、30% 显著减少、极小区域保底 1 轨、超大屏封顶）；store roundtrip 含新字段

---

## v2.16.12 (2026-09-05)

**修复**

- **播放页亮度/音量滑动手感（灵敏度过高 + 基准不可靠）**：
  - **问题（实测）**：v2.16.7~11 纵向调节换算「滑满一屏 = ±100%」，且手势过程中**把每次的目标值写回基准、下一帧再叠加上去**（`_adjustBase` 被逐帧改写）——换算双重叠加 + 满行程 100% 映射，手指动一点点亮度/音量就冲到 0 或 100；亮度基准依赖原生 `getBrightness` 读窗口亮度，原生把「窗口未覆盖（-1 = `BRIGHTNESS_OVERRIDE_NONE`）」换算成系统亮度兜底前若把 -1 原样透传，Dart 侧 -1×100 → clamp 到 0 → 基准错误导致跳变
  - **修复（`app/lib/pages/player_page.dart` + `app/lib/services/device_media.dart`）**：
    - **灵敏度 0.3**：`adjustPercent` / `volumeTargetLevel` 统一乘灵敏度 `kAdjustSensitivity = 0.3`——**完整上下滑一屏 ≈ 基准 ±30%**（旧版 ±100%），小幅滑动平滑小幅变化（滑 1/10 屏 ≈ ±3%；音量档位离散，小幅滑动按档取整静默），不再一碰就跳 0/100；clamp 不变（亮度 5..100、音量 0..100）
    - **基准在锁定后固定**：手势开始时异步读取一次原生基准（取到前不调节），之后目标始终 =「固定基准 + 累计比例 × 灵敏度」——消除逐帧改写基准的双重叠加，滑动可平滑回退、不越界；读取失败亮度兜底 50%（`getBrightnessPercent` 内置），音量（无有效档位）放弃本次手势、不硬设 0
    - **基准读取兜底（Dart 侧，可单测）**：`normalizeBrightnessPercent` 纯函数——原生亮度原始值 null / NaN / Infinity / 越界（含 -1 未设置被透传）一律回退有效默认（50%），有效 0..1 → 0..100 并钳制；`DeviceMedia.getBrightnessPercent` 保证**永远返回有效 0..100 值**（不抛异常、不返回 -1/异常基准）
  - **单测**：`app/test/device_media_test.dart` 新增 8 用例（-1/未设兜底、null/NaN/越界兜底、有效值换算）；`app/test/player_gesture_test.dart` 换算断言按灵敏度 0.3 更新（满屏 ±30%、1/10 屏 ±3%、音量满屏 +0.3×max 档、clamp 边界、负方向、亮度下限 5%）

---

## v2.16.11 (2026-09-04)

**修复**

- **弹幕滚动不连续（卡顿/跳跃/时快时慢）**：
  - **根因（实证）**：v2.16.6 弹幕位置推进与画面重绘解耦——Ticker 每帧在数据层面推进弹幕 x，但 **CustomPaint 只在"发射帧"（`changed → setState`）才真正重绘**。两批弹幕之间（可达父层 500ms 播放位置轮询间隔）弹幕位置在变、画面却不刷新，等下一次 setState 才一次性画出来 → 弹幕以 ~500ms 步长"瞬移"，表现为滚动不连续 / 跳跃 / 时快时慢（推进 60fps × 绘制 ~2-8fps）
  - **修复（`lib/widgets/danmaku_overlay.dart`）**：
    - **每帧落屏**：CustomPainter 挂到 `ValueNotifier` repaint 信号上，Ticker 每帧推进后 `value++` → 只 markNeedsPaint 本层（外层 `RepaintBoundary` 隔离，不 rebuild、不波及播放页整树）——推进的每一帧都真正画出来，滚动回到 60fps 连续
    - **时间基准（暂停/恢复/切后台不跳）**：帧间隙 > 100ms（切后台/引擎停摆/大卡顿恢复）→ 重置时间基准、本帧不推进（弹幕原地继续，不做大步补偿）；播放暂停期间基准每帧照常更新 → 恢复瞬间 dt 不跳变
    - **发射时间预算**：发射从"每帧条数上限"改为按**真实流逝时间累计信用**（40 条/s 封顶、单帧余额 4），seek/卡顿积压的密集弹幕不再一次性扎堆补发，按信用摊到后续帧逐个进入
    - 纯函数 `danmakuSmoothDt` / `danmakuCreditAfter` 可单测；TextPainter 缓存维持（发射时 layout 一次、每帧只 paint，无每帧重排）
  - **顺带健壮性修复（debug 构建下弹幕层 ticker 死亡）**：Ticker 帧回调发生在每帧 layout 之前，mount 后**首帧**回调时 render object 尚未 layout——原实现直接读 `context.size` 会触发 Flutter debug 断言（"has not been through layout"）并抛异常终止 Ticker 调度（此后弹幕完全不推进）。改为 `RenderBox.hasSize` 安全读取（`_currentSize`），layout 完成前该帧跳过布局/发射、下一帧补上，不抛异常。release 下原代码因断言关闭未暴露，但 debug（flutter run/调试）下弹幕层会静默失效
  - **单测**：`test/danmaku_features_test.dart` 新增 4 用例（dt 平滑/信用纯逻辑、逐帧推进 + 大间隙重置不跳变、暂停冻结 + 恢复平滑）

---

## v2.16.10 (2026-09-04)

**修复**

- **白名单 UP 主隔段时间重进 App 后被自动清空（油猴脚本丢 v4 upowners 字段）**：
  - **问题**：v2.13.0 App 新增顶层 `upowners`（UP 主白名单，数据模型升 v4），但**油猴脚本 v2.3.0 的 `parseWhitelist` / `buildWhitelistJson` 仍只认识 `version/updated_at/collections/videos`**——在电脑上用油猴加视频时 GET→PATCH 覆盖写回，把 Gist 里的 `upowners` 整个字段丢掉（与历史「合集消失」同款 bug：当时油猴旧版只认识 videos、不保留 collections）。之后手机重进 App 触发同步，拉到的是已被清空的 Gist，表现为「UP 主被自动清空」
  - **修复（油猴 v2.3.1）**：`parseWhitelist` 透传保留顶层 `upowners` 数组（读入即保留、内容不丢）；`buildWhitelistJson` 对缺失的 `upowners` 补空数组兜底（序列化不再丢键）；空结构初始化同步带 `upowners: []`
  - **App 端核对结论**：`WhitelistData` 模型 `fromJson/toJson/copyWith/normalizedForSave` 与全部写 Gist 路径（`GithubApi.saveToGist` 载荷、`WhitelistWriter.addVideo/importPgcSeason/合集移动/删除/重排`、`UpownerWriter.add/removeByMid/updateLastSeenBatch`、信箱、`SyncService.saveToCache`）均保留 `upowners`，无丢失路径——本版本补回归单测：**所有管理变换（改名/删除合集、合集重排、视频移合集/移除/拖拽重排、UP 主增删）与模型往返、保存序列化均保留 `upowners`**（`test/whitelist_order_test.dart` 新增 7 用例）
  - **数据现状**：Gist 上 `upowners` 已被上述 bug 清空且无可恢复备份（本地各备份 / git 历史均不含），**已丢失的 UP 主需在 App 搜索页重新添加一次**；修复后油猴/App 任意写入不再丢

---

## v2.16.9 (2026-09-04)

**修复**

- **播放页滑动手势主导方向判定（横屏 seek 与亮度/音量共存）**：
  - **问题**：v2.16.7 手势按"横屏只注册横向、竖屏只注册纵向"分方向注册——人无法完全垂直 / 水平滑动，**全屏横屏稍斜的上下滑会被误判成横向 seek**（调节亮度 / 音量失效），较正的上下滑又完全无响应
  - **修复（参考 B 站）**：滑动统一走单一 Pan 手势，位移累计超 12px 阈值后按**主导分量**锁定方向——水平位移主导 → 全屏横屏 seek（位移比例 = 时长比例，松手 seekTo）；垂直位移主导 → 按起点左半屏调亮度 / 右半屏调音量（**横屏竖屏都可用**）；斜向滑动按主方向归类，**锁定后本次手势不再切换**（防中途抖动）。竖屏仍只保留亮度 / 音量（水平主导忽略，无 seek 防误触）
  - **纯函数**：新增 `decideMode(dx, dy, threshold)`（|dx| ≥ |dy| → 水平、|dy| > |dx| → 垂直、均未超阈值 → 未定）+ `nextPanMode`（方向锁定），单测覆盖斜向归类 / 45° 平分 / 负方向 / 锁定后不受后续位移影响（`test/player_gesture_test.dart`，14 → 21 用例）

---

## v2.16.8 (2026-09-03)

**修复**

- **应用内更新下载修复（断点续传 + 网络异常友好提示）**：
  - **网络中断不再「整体重下」**：之前下载到一半断网，dio 抛 `unknown`（底层 IOException，message 常为 null/英文）→ 用户看到「网络异常：未知错误」，且半成品被直接删除、下次必须整体重新下载
  - **断点续传**：下载目标改为半成品 `.part` 文件（`app-update-<code>.apk.part`）——下载前若 `.part` 已存在，按其字节数带 `Range: bytes=N-` 续传（服务器回 206 追加续传、200 不支持 Range 则全量重写），下载完成才改名正式 APK；**失败 / 取消都保留 `.part`**，下次点「重试 / 立即更新」自动从断点继续
  - **错误分类友好化**：断网 / 连接被重置 / 接收中断 → 「网络中断，下载未完成，请检查网络后重试（将自动从断点继续）」；超时 / 连接失败 / HTTP 403 / 404 各有明确中文提示；未知错误兜底给「网络异常，请检查网络后重试」——**任何路径不再出现「未知错误」裸文案**
  - **自动重试**：瞬时网络错误自动重试 2 次（1s→2s 指数退避），每次重试从断点继续；HTTP 业务错误（403/404）与用户取消不重试
  - **完整性校验**：`.part` 下载完成仍对整个文件做 SHA-256 校验（`info.sha256` 存在时），失败删除文件抛「完整性校验失败」
  - **进度基数修正**：续传时进度分母优先取 APK 完整大小（`asset.size` / `Content-Range`），下载进度从上次断点累计，不跳变

**其他**

- 下载文件改名带 versionCode（`app-update-<code>.apk`），换新版本自动清理旧版本号残留半成品，避免跨版本续接拼出损坏文件

---

## v2.16.7 (2026-09-03)

**新增**

- **播放页 B 站式快捷手势**（参照 B 站手机端播放器）：
  - **双击播放 / 暂停**：画面任意处快速双击切换播放 / 暂停（与单击显隐共存——单击因等待双击窗口判定延迟 ~300ms 触发显隐；双击赢得手势时延迟的单击自动取消，不误触显隐）
  - **横屏左右滑 seek（全屏）**：全屏横屏下画面左右滑动按**位移比例**拖动进度（滑满一屏 ≈ 100% 时长），滑动中浮层实时显示「当前进度 / 总时长」（如 `12:34 / 56:78`），**松手 seekTo** 并保存进度；方向与进度条一致（右滑前进、左滑后退）
  - **竖屏半屏上下滑调亮度 / 音量**：竖屏（非全屏）左半屏纵向滑动调**应用内亮度**、右半屏调**媒体音量**——滑满一屏 ≈ 0↔100%，**调节即时生效、松手不恢复**，滑动中浮层显示图标 + 百分比
  - **手势冲突处理**：单击显隐 / 双击暂停 / 长按 2x / 横屏 seek / 竖屏亮度·音量 / 进度条拖动 / 控制层按钮——横向与纵向手势**按屏幕方向分道注册**（全屏只注册横向、竖屏只注册纵向，另一方向不存在即不会抢判定）；控制层按钮与进度条位于 Stack 上层，其区域内点击 / 拖动天然优先（按钮优先）

**原生通道（新增）**

- `bili_whitelist/media` MethodChannel（android/app/src/main/kotlin/.../MediaController.kt）：`getVolume` / `setVolume`（AudioManager STREAM_MUSIC，按档精确设置且不弹系统音量 UI）+ `getBrightness` / `setBrightness`（应用内亮度——WindowManager LayoutParams.screenBrightness 0~1，仅当前 Activity、退出播放恢复系统亮度；下限钳 5% 防全黑时浮层不可见）；Dart 侧封装 `lib/services/device_media.dart`，通道不可用 / 异常静默放弃本次手势（不打断播放）

**重构**

- 播放页手势纯逻辑抽为可单测顶层函数：`verticalSlideKind`（半屏判定）/ `slideFraction`（位移→比例，拖满一屏=±100%）/ `seekTargetMs`（seek 目标，钳制 0..时长）/ `volumeTargetLevel` / `adjustPercent` / `brightnessPercent`（亮度下限 5%），单测 `test/player_gesture_test.dart`（14 用例）

---

## v2.16.6 (2026-09-03)

**新增**

- **弹幕增强**（播放页「弹幕」，长按弹幕按钮弹出设置面板集中管理）：
  - **移动平滑**：滚动弹幕位置按帧间时间差（dt）浮点精确推进，dt 钳制上限防掉帧跳变（卡顿恢复平滑不"生硬"）；单帧发射预算限制，密集弹幕分散到后续帧逐个进入（帧率稳定）；滚动穿越时长 5s → 4s（更接近 B 站节奏）；顶部 / 底部弹幕带 0.15s 淡入 + 0.4s 淡出
  - **真碰撞分层（消除叠字）**：不再 round-robin——滚动弹幕按水平轨道分配，每条轨道记录最后一条弹幕的尾缘 / 速率，新弹幕按**追尾时间精确判定**能否同轨（O(1)，无每帧重排；同速跟车保持间距、快车只在追上时慢车已出屏才同轨），轨道打满丢弃并计数；顶部 / 底部各自纵向行堆叠（停留占行、取最上空闲行，全忙丢弃）
  - **弹幕屏蔽**：屏蔽关键词列表（substring 匹配、去空白去重、增删管理）+ 屏蔽类型开关（滚动 / 顶部 / 底部分别控制）；发射前过滤，设置变更即时清屏按当前位置重载生效
  - **透明度调节**：滑杆 20%~100% 实时预览，绘制 alpha 全局乘系数（含阴影）；与屏蔽设置一并本地持久化（shared_preferences），下次进入自动恢复
  - 弹幕活动周期日志（debugPrint：发射 / 屏蔽 / 丢弃计数 + 屏上活跃 vs 轨道数）供 logcat 实测

**重构**

- 播放页弹幕逻辑拆分：纯逻辑 `models/danmaku_lanes.dart`（滚动追尾判定 + 顶 / 底行堆叠分配器）、`models/danmaku_settings.dart`（屏蔽词 / 类型 / 透明度 + 序列化容错）、`services/danmaku_settings_store.dart`（持久化，失败静默）；渲染层 `widgets/danmaku_overlay.dart` 按设置实例变化自动重载

---

## v2.16.5 (2026-09-03)

**新增**

- **搜索支持番剧 / 电影并可导入**：「全部 B 站」搜索页新增搜索范围切换（视频 / 番剧 / 电影 / 电视剧）：
  - 番剧 / 电影走 media 搜索接口（`x/web-interface/wbi/search/type`，`search_type=media_bangumi / media_ft`，匿名 + WBI 签名即可，2026-09 curl 实测确认字段：`season_id`（整季导入钥匙，无顶层 `ep_id`）/ `title`（含高亮标签需清洗）/ `cover`（偶有 `http://` 前缀需补 https）/ `badges[]` 角标（独家、大会员）/ `styles` 风格串 / `index_show`（「全14话」或上映日期）/ `eps[0].id` 首集 ep_id）
  - 结果列表展示封面 + 类型角标 + 标题 + 角标/集数/风格副标题；右侧「导入」**整季逐集加入白名单**（与首页粘贴链接导入共用 `WhitelistWriter.importPgcSeason` + `runPgcSeasonImport`，进度逐集提示、bvid 查重自动跳过已存在集），导入后按钮变「已导入」（会话级记忆 + 白名单首集 ep_id 匹配双重判断）
  - media 结果同样支持上拉翻页（`data.numResults` 判断，与视频搜索同一套 `hasMore` 逻辑）；排序 chip 仅视频范围显示（media 接口不支持 order）
  - 电视剧（`media_tv`）/ 纪录片（`media_doc`）入口同样可切：**匿名请求实测被 B 站降级过滤（code=-1200「被降级过滤的请求」）**，错误分类提示「可能需登录」，番剧/电影不受影响

**重构**

- 番剧/电影整季导入逻辑从首页私有方法抽为共用：`WhitelistWriter.importPgcSeason`（纯逻辑：拉整季 + 逐集 addVideo，进度回调，异常汇总）+ `widgets/pgc_import_dialog.dart` 的 `runPgcSeasonImport`（UI 编排：配置门禁 + 进度对话框 + 结果反馈），首页粘贴链接导入与搜索页 media 结果导入共用同一实现与文案

---

## v2.16.4 (2026-09-03)

**新增**

- **会员番剧完整播放**：番剧/电影整季导入时每集写入 `epId`（番剧集标识，向后兼容：旧数据无该字段不受影响）。播放带 `epId` 的番剧集时，普通接口取流失败（会员集实测返回 -404）自动回退 pgc 番剧取流接口（`pgc/player/web/playurl`，登录态 SESSDATA 随请求注入）：
  - 免费集仍优先走普通接口（720P 比 pgc 端点的更清晰），行为不变
  - 会员/付费集**登录大会员账号后可完整播放**；未登录/非大会员时匿名接口只给试看流（`is_preview=1`，仅前几分钟），App **不播试看**、明确提示「该集为大会员内容，当前为试看（仅前几分钟），请登录大会员账号完整观看」并引导去登录——避免"能播但只有几分钟"的误导
  - 旧版导入的番剧数据（无 epId）保持原「该集可能为大会员/付费内容或已下架」提示

---

## v2.16.1 (2026-09-02)

**修复**

- **应用内更新在私有仓库下完全不可用**：仓库是私有的且从未创建过 GitHub Release，`releases/latest` 匿名访问永远 404。
  - `UpdateService` 新增 `tokenProvider`（主页接管理页已配置的 GitHub token）：`fetchLatest` 带 `Authorization` 头访问 Releases API。
  - 私有仓库 APK 资产下载改为两段式：先带 token + `Accept: application/octet-stream` 请求资产 API 地址，手动跟随 302 到签名 CDN 地址后再下载——鉴权头不随跳转转发（否则 S3 双重鉴权报 400）。
  - `UpdateInfo` 新增 `apkApiUrl` 字段（资产 `url`）；资产选择按设备 ABI 匹配（arm64-v8a / armeabi-v7a / x86_64 / x86），无匹配回退 arm64-v8a。
  - GitHub 401 单独提示「token 无效或已过期」。

---

## v2.16.0 (2026-09-02)

**新增**

- **观看历史**：主页左滑进入历史记录界面，AppBar 也有入口图标；播放页自动写入观看进度，历史列表可按进度续播。

---

## v2.15.1 (2026-09-01)

**新增**

- 首页支持左右滑动：第一页管理视频合集，第二页管理白名单 UP 主；可查看、进入、移除已加入的 UP 主，并可直接跳到「搜索 UP 主」。
- UP 主详情页新增「在该 UP 主的视频中搜索」输入框，只在当前白名单 UP 主投稿内过滤，保留排序与分页加载。
- `BiliApi.fetchUpownerVideos` 新增 `keyword` 参数，对接 B 站 `x/space/wbi/arc/search` 的 UP 主内稿件搜索。
- 「检查更新」遇到 GitHub Release 404 时改为提示“暂时没有可用更新 / Release 未创建 / 仓库不可访问”，不再误报“版本仓库不存在”。

---

## v2.15.0 (2026-09-01，T2+T3 合并发布)

**新增**

- **白名单 UP 主**：搜索页「搜索 UP 主」Tab（B 站全网用户搜索），结果可一键加入白名单；UP 主详情页展示头像 + 名字 + 粉丝 + 简介 + 视频列表（最新发布 / 最多播放 / 最多收藏 三种排序，分页 20 条/页）
- **信箱**：首页 AppBar 最左侧图标（带未读红点），启动 5s 后自动检查所有白名单 UP 主的新视频；下拉刷新强制重检；顶部「全部标记已读」一键清空
- **应用内版本更新**：启动 5s 后静默检查（24h 节流），有新版本且非强制时弹窗；管理面板 →「检查更新」手动触发（force=true 跳过节流）；下载完成自动触发系统安装（FileProvider 暴露 ApplicationSupport/updates）
- **强制更新字段 `min_supported_code` 预留**，首版不启用

**数据模型 / 接口**

- `whitelist.json` 顶层新增 `upowners[]` 数组（兼容 v3：缺字段视为 `[]`），写回 Gist 时统一规范化为 `version=4`
- 新接口：
  - `BiliApi.searchUpowner(keyword, page)`（`search_type=bili_user`）
  - `BiliApi.fetchUpownerVideos(mid, pn, ps, order)`（`x/space/wbi/arc/search`）
  - `BiliApi.fetchUpownerInfo(mid)`（`x/space/wbi/acc/info`）

**工具链**

- `build_release.sh` 末尾新增第 6 步：当 `GH_REPO_TOKEN` 已设置时自动创建 GitHub Release 并上传 3 ABI APK（jq 不可用时降级 python -c）

**风控降级**

- 信箱检查遇 -412 / -352 自动降级（间隔 1.5s → 3s，最多 100 个 UP 主）

---

## v2.13.0 (2026-09-01，T2 UP 主功能单独提交版本)

注：实际合并到 v2.15.0 发布，单 commit 版本号保留 v2.13.0+25。

**新增**

- 白名单 UP 主 + 信箱（同 v2.15.0 描述）

---

## v2.12.1 (2026-09-01)

- 合集管理 Bug 修复 + 搜索结果翻页 / 全集号定位优化
- 视频搜索翻页 + 排序选择器（综合 / 最多播放 / 最新发布 / 最多收藏）
- `flutter_secure_storage` vivo 等国产 ROM 兼容加固（显式 `AndroidOptions`，`encryptedSharedPreferences=false` + `resetOnError=true`）

---

## v2.12.0 (2026-08-19)

**新增**

- 合集与视频拖动排序（同步 Gist）

---

## v2.11.3 (2026-08-18)

**修复**

- 移动合集弹窗超长列表可滚动

---

## v2.11.2 (2026-08-17)

**修复**

- R8 保留 ffmpeg-kit native 方法（修复 release 版插件注册中断）

---

## v2.11.1 (2026-08-15)

**修复**

- 禁用自动备份防 Keystore 密钥失效（`secure storage` 数据不可用）

---

## v2.11.0 之前

见 git tag / commit 历史。
